{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"Open Source \u00b7 Apache 2.0 License ML monitoring for all three modalities. <p>     Tabular classifiers, LLM pipelines, and AI agents \u2014 one library, one API,     narwhals backend so it works with pandas and Polars out of the box.   </p> $     pip install ayn-ml"},{"location":"#why-ayn-ml","title":"Why ayn-ml?","text":"<p>Most monitoring libraries specialise in a single modality. ayn-ml covers tabular ML, NLP/LLM pipelines, and AI agents with a single, consistent API \u2014 declarative configuration, a protocol-extensible metric registry, and automatic metric selection via the built-in Advisor.</p>"},{"location":"#key-features","title":"Key features","text":"\ud83e\udde0 <p>Auto metric selection</p> <p> <code>MetricDesigner</code> inspects your data and builds the right monitoring plan \u2014      normality tests, variance routing, imbalance detection \u2014 automatically.   </p> \u26a1 <p>narwhals backend</p> <p>     Write once, run on pandas or Polars. No conversion overhead, no backend lock-in.   </p> \ud83d\udcd0 <p>Declarative config</p> <p>     Define monitoring plans in Python or YAML. Plans are immutable Pydantic models      that round-trip perfectly.   </p> \ud83d\udd0c <p>Extensible registry</p> <p> <code>@register_metric</code> to add custom metrics. They integrate seamlessly with     the Runner, stores, and renderers.   </p> \ud83d\udcca <p>38+ built-in metrics</p> <p>     Performance, drift, statistics, CBPE estimation, and fairness \u2014 all in one registry,     all with consistent result types.   </p> \ud83d\udea8 <p>Alerts &amp; renderers</p> <p>     Threshold policies, email/webhook channels, and an HTML renderer with      snapshot and history dashboards.   </p>"},{"location":"#get-started-in-60-seconds","title":"Get started in 60 seconds","text":"PythonYAML (roadmap)Auto (MetricDesigner) <pre><code>from ayn_ml import MonitoringPlan, TabularSchema, MetricSpec\nfrom ayn_ml.runner import Runner\n\nschema = TabularSchema(label_col=\"y_true\", prediction_col=\"y_pred\")\n\nplan = MonitoringPlan(\n    name=\"churn_monitor\",\n    model_id=\"churn_v2\",\n    model_version=\"1.0\",\n    data_schema=schema,\n    metrics=[\n        MetricSpec(name=\"accuracy\", threshold=0.85, upper_bound=False),\n        MetricSpec(name=\"psi\", feature_name=\"age\", threshold=0.2),\n    ],\n)\n\nreport = Runner().run(plan, current=df_current, reference=df_reference)\nprint(report.to_dataframe())\n</code></pre> <p>Plans are declarative Pydantic models \u2014 YAML round-trip (<code>from_yaml</code> / <code>to_yaml</code>) is on the roadmap. The schema below reflects the planned config format:</p> <pre><code># churn_plan.yaml\nname: churn_monitor\nmodel_id: churn_v2\nmodel_version: \"1.0\"\ndata_schema:\n  type: tabular\n  label_col: y_true\n  prediction_col: y_pred\nmetrics:\n  - name: accuracy\n    threshold: 0.85\n    upper_bound: false\n  - name: psi\n    feature_name: age\n    threshold: 0.2\n</code></pre> <pre><code>from ayn_ml.advisor import MetricDesigner\nfrom ayn_ml.core.schema import TabularSchema\n\nschema   = TabularSchema(label_col=\"y_true\", prediction_col=\"y_pred\", proba_col=\"y_prob\")\ndesigner = MetricDesigner(schema)\n\nresult = designer.suggest(\n    df_current,\n    reference=df_reference,   # required \u2014 stable baseline\n    task_type=\"classification\",\n)\n\nplan  = result.plan      # MonitoringPlan \u2014 pass directly to Runner\nfor w in result.warnings:\n    print(\"Advisory:\", w)\n</code></pre> <p>Quick Start \u2192 API Reference \u2192</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install ayn-ml                   # core (narwhals, pandas, Pydantic, scipy, sklearn)\npip install ayn-ml[polars]           # add Polars backend (recommended for production)\npip install ayn-ml[mlflow]           # MLflow store\npip install ayn-ml[nlp]              # BLEU, ROUGE, BERTScore\npip install ayn-ml[all]              # everything\n</code></pre> <p>Python \u2265 3.10 required.</p>"},{"location":"#need-more","title":"Need more?","text":"<p>ayn-ml Pro adds advanced advisor profiles, a cloud-scale runner, Slack notifications, S3 storage, LLM safety and quality metrics, agent evaluation metrics, advanced explainability, and an analytics dashboard with RBAC and SSO.</p> <p>ayn-ml Pro is a commercial product by Kevek ML.</p>"},{"location":"advisor/","title":"Metric Advisor \u2014 <code>ayn_ml/advisor/</code>","text":"<p><code>MetricDesigner</code> analyses your data and builds a ready-to-use <code>MonitoringPlan</code> automatically. Instead of hand-picking drift tests and performance metrics, you describe the task and hand over a DataFrame \u2014 the advisor routes each feature to the statistically appropriate metrics based on column type, sample size, normality, skewness, and (optionally) variance relative to a ref</p>"},{"location":"advisor/#quick-start","title":"Quick start","text":"<p>from ayn_ml.advisor import MetricDesigner from ayn_ml.core.schema import TabularSchema schema = TabularSchema(     label_col=\"y_true\",     prediction_col=\"y_pred\",     proba_col=\"y_prob\", designer = MetricDesigner(schema) result = designer.suggest(     df_current,     ref=df_reference,   # required \u2014 training baseline or historical window     task_type=\"classification\",     name=\"fraud_v2_plan\", plan   = result.plan      # MonitoringPlan \u2014 pass directly to Runner warns  = result.warnings  # tuple[str, ...] \u2014 why each metric was + / excluded <code>MetricDesigner</code> is reusable \u2014 create one instance per schema and call <code>suggest()</code> for each time window with the appropriate cur and ref DataFrames.</p>"},{"location":"advisor/#decision-trees","title":"Decision trees","text":""},{"location":"advisor/#drift-per-feature-column","title":"Drift \u2014 per feature column","text":"<p>column type = categorical?   \u2192 PSI always   \u2192 chisquare if registered column type = numeric / binary:   n_current &lt; 30     \u2192 wasserstein only  (+W)   n_current &gt; 50 000 \u2192 PSI + wasserstein only (no hypothesis tests)     [normality and skewness computed on the REFERENCE distribution]     is_normal AND |skewness| &lt; 1.0?  \u2192 ttest (Welch, equal_var=False)     + cramervonmises + wasserstein + PSI     variance_ratio &gt; 1.5 or &lt; 0.67?  \u2192 +levene  (+W)</p> <p>Why ref for normality? The ttest / Mann-Whitney U choice should reflect the shape of the distribution you are monitoring against (the stable baseline), not the potentially-drifted cur window. Using the cur window would be circular: drift could make it non-normal, silently switching the test. When the ref column is absent, normality falls back to the cur window with a debug log. Normality test selection (thresholds from \u00a73.3 of the selection guide): | Sample size | Test used | | n &lt; 8 | non-normal (no test \u2014 too few points) | | 8 \u2264 n \u2264 300 | Shapiro-Wilk | | 300 &lt; n \u2264 5 000 | D'Agostino k\u00b2 (<code>scipy.stats.normaltest</code>) | | n &gt; 5 000 | <code>\\|skewness\\| &lt; 1.0</code> heuristic |</p>"},{"location":"advisor/#performance-classification","title":"Performance \u2014 classification","text":"<p>| Imbalance ratio | Metrics | | &gt; 10 : 1 (severe) | f1 + aucpr; accuracy excluded (+ W) | | &gt; 5 : 1 (moderate) | f1 + auc; accuracy excluded (+ W) | | \u2264 5 : 1 (balanced) | accuracy | <code>accuracy</code> is excluded when imbalance &gt; 5:1 \u2014 it is misleading in those regimes and a W is always emitted.</p>"},{"location":"advisor/#performance-regression","title":"Performance \u2014 regression","text":"<p>| mae + r2 |</p>"},{"location":"advisor/#metricdesignerschema","title":"<code>MetricDesigner(schema)</code>","text":"<p>| Parameter | Type | Description | | <code>schema</code> | <code>TabularSchema</code> | Schema shared across all <code>suggest()</code> calls |</p>"},{"location":"advisor/#metricdesignersuggestdf-ref-task_type-name-model_id-model_version","title":"<code>MetricDesigner.suggest(df, *, ref, task_type, name, model_id, model_version)</code>","text":"<p>| Parameter | Type | Default | Description | | <code>df</code> | DataFrame | \u2014 | Current-window data (pandas or Polars) | | <code>ref</code> | DataFrame | \u2014 | Reference window (training baseline). Required. Used for normality routing and variance-ratio / Levene routing | | <code>task_type</code> | str | <code>\"classification\"</code> | <code>\"classification\"</code> | <code>\"regression\"</code> | | <code>name</code> | str | <code>\"suggested_plan\"</code> | Name embedded in the generated plan | | <code>model_id</code> | str | <code>\"\"</code> | Model id embedded in the plan | | <code>model_version</code> | str | <code>\"\"</code> | Model version str embedded in the plan | Returns a <code>SuggestedPlan</code>.</p>"},{"location":"advisor/#suggestedplan","title":"<code>SuggestedPlan</code>","text":"<p>@dataclass(frozen=True) class SuggestedPlan:     plan: MonitoringPlan        # ready to pass to Runner     warnings: tuple[str, ...]   # advisory messages explaining routing decisions result.to_dict()</p>"},{"location":"advisor/#plan-monitoringplanmodel_dump","title":"\"plan\": { ... },                        # MonitoringPlan.model_dump()","text":""},{"location":"advisor/#warnings-levene-for","title":"\"warnings\": [\"levene + for ...\", ...]","text":""},{"location":"advisor/#warnings-ref","title":"Warnings ref","text":"<p>| Warning | Meaning | | <code>\"'{col}': only wasserstein suggested \u2014 n={n} is too small for hypothesis tests (&lt; 30)\"</code> | Sample size below the min for any test | | <code>\"levene + for '{col}': variance_ratio={vr:.2f}\"</code> | Variance differs substantially from ref | | <code>\"accuracy excluded: imbalance ratio {r:.1f}:1 (severe imbalance)\"</code> | Severe class imbalance (&gt; 10:1) | | <code>\"accuracy demoted: imbalance ratio {r:.1f}:1\"</code> | Moderate class imbalance (&gt; 5:1) |</p>"},{"location":"advisor/#end-to-end-example","title":"End-to-end example","text":"<p>import pandas as pd from ayn_ml.advisor import MetricDesigner from ayn_ml.core.schema import TabularSchema from ayn_ml.runner import Runner schema = TabularSchema(     label_col=\"y_true\",     prediction_col=\"y_pred\",     proba_col=\"y_prob\", designer = MetricDesigner(schema)</p>"},{"location":"advisor/#suggest-a-plan-from-a-representative-sample","title":"Suggest a plan from a representative sample","text":"<p>result = designer.suggest(     df_train_sample,     ref=df_baseline,     task_type=\"classification\",     name=\"fraud_monitor_v2\",     model_id=\"fraud_model\",     model_version=\"2.0\", for w in result.warnings:     print(\"Advisory:\", w)</p>"},{"location":"advisor/#inspect-the-generated-specs","title":"Inspect the generated specs","text":"<p>for spec in result.plan.metrics:     feat = f\" [{spec.feature_name}]\" if spec.feature_name else \"\"     print(f\"  {spec.name}{feat}\")</p>"},{"location":"advisor/#run-the-plan-on-prod-data","title":"Run the plan on prod data","text":"<p>report = Runner().run(     cur=df_production,     ref=df_baseline, print(report.to_dataframe()) \u2192 Notebook: 08 \u2014 MetricDesigner walkthrough</p>"},{"location":"advisor/#pro-tiers-ayn-ml-pro","title":"Pro Tiers \u2014 <code>ayn-ml-pro</code>","text":"<p><code>ayn-ml-pro</code> ships two <code>MetricDesigner</code> subclasses that extend the minimal tier from <code>ayn-ml</code> (Apache 2.0) with progressively richer automatic metric selection.</p> <pre><code>MetricDesigner (ayn-ml Apache 2.0)\n  \u2514\u2500\u2500 StandardMetricDesigner       \u2190 standard tier\n        \u2514\u2500\u2500 ComprehensiveMetricDesigner  \u2190 comprehensive tier\n</code></pre> <p>Both classes implement the same <code>.suggest()</code> interface as the <code>ayn-ml</code> base and return a <code>(MonitoringPlan, list[str])</code> result via <code>result.plan</code> and <code>result.warnings</code>.</p>"},{"location":"advisor/#standardmetricdesigner","title":"StandardMetricDesigner","text":"<p>Import: <code>from ayn_ml_pro.advisor import StandardMetricDesigner</code></p> <p>Adds richer performance metrics and descriptive stats on top of the minimal tier. Drift routing is identical to the Apache 2.0 minimal tier.</p>"},{"location":"advisor/#additions-over-the-minimal-tier","title":"Additions over the minimal tier","text":"Category Added metrics Regression <code>mse</code>, <code>mape</code> (alongside <code>mae</code> + <code>r2</code>) Classification (balanced) <code>f1</code>, <code>auc</code> (alongside <code>accuracy</code>) Numeric columns <code>mean</code>, <code>std</code> Categorical columns <code>top_category</code> <pre><code>from ayn_ml.core.schema import TabularSchema\nfrom ayn_ml_pro.advisor import StandardMetricDesigner\n\nschema = TabularSchema(label_col=\"y_true\", prediction_col=\"y_pred\", proba_col=\"y_prob\")\ndesigner = StandardMetricDesigner(schema)\nresult = designer.suggest(df_current, ref=df_reference, task_type=\"classification\", name=\"fraud_monitor_standard\")\n\nplan  = result.plan\nwarns = result.warnings\n</code></pre>"},{"location":"advisor/#comprehensivemetricdesigner","title":"ComprehensiveMetricDesigner","text":"<p>Import: <code>from ayn_ml_pro.advisor import ComprehensiveMetricDesigner</code></p> <p>Adds MMD drift detection, the full performance suite, and extended distributional stats on top of the standard tier.</p>"},{"location":"advisor/#additions-over-the-standard-tier","title":"Additions over the standard tier","text":"Category Added metrics Condition Drift <code>mmd</code> (Maximum Mean Discrepancy) Numeric columns with n &gt;= 200 Classification <code>precision</code>, <code>recall</code> All imbalance regimes Classification (balanced) <code>log_loss</code>, <code>brier</code> Balanced datasets only Numeric / binary columns <code>skewness</code>, <code>kurtosis</code> All numeric and binary columns <pre><code>from ayn_ml.core.schema import TabularSchema\nfrom ayn_ml_pro.advisor import ComprehensiveMetricDesigner\n\nschema = TabularSchema(label_col=\"y_true\", prediction_col=\"y_pred\", proba_col=\"y_prob\")\ndesigner = ComprehensiveMetricDesigner(schema)\nresult = designer.suggest(df_current, ref=df_reference, task_type=\"classification\", name=\"fraud_monitor_comprehensive\")\nwarns = result.warnings\n</code></pre>"},{"location":"advisor/#choosing-a-tier","title":"Choosing a tier","text":"Situation Recommended tier Fast feedback, limited compute <code>StandardMetricDesigner</code> Deep statistical monitoring, model audit <code>ComprehensiveMetricDesigner</code> Custom metric selection Use <code>MetricDesigner</code> from <code>ayn-ml</code> directly and supply your own <code>MetricSpec</code> list"},{"location":"advisor/#tier-comparison-at-a-glance","title":"Tier comparison at a glance","text":"Metric Minimal (ayn-ml) Standard Comprehensive KS test (numeric drift) \u2713 \u2713 \u2713 Chi-squared (categorical drift) \u2713 \u2713 \u2713 MMD (numeric drift, n&gt;=200) \u2713 <code>mae</code>, <code>r2</code> (regression) \u2713 \u2713 \u2713 <code>mse</code>, <code>mape</code> (regression) \u2713 \u2713 <code>accuracy</code> (classification) \u2713 \u2713 \u2713 <code>f1</code>, <code>auc</code> (balanced) \u2713 \u2713 <code>precision</code>, <code>recall</code> \u2713 <code>log_loss</code>, <code>brier</code> (balanced) \u2713 <code>mean</code>, <code>std</code> (numeric) \u2713 \u2713 <code>top_category</code> (categorical) \u2713 \u2713 <code>skewness</code>, <code>kurtosis</code> \u2713"},{"location":"architecture/","title":"ayn-ml \u2014 Architecture","text":"<p>Canonical reference for module structure, type contracts, implementation specs, and design decisions. Read this in full before implementing any new phase.</p>"},{"location":"architecture/#module-structure-target","title":"Module structure (target)","text":"<pre><code>ayn_ml/\n\u251c\u2500\u2500 core/\n\u2502   \u251c\u2500\u2500 schema.py        # BaseSchema, TabularSchema, TextSchema, AgentSchema  \u2705\n\u2502   \u251c\u2500\u2500 spec.py          # MetricSpec, MonitoringPlan                          \u2705\n\u2502   \u251c\u2500\u2500 result.py        # ExecutionContext, MetricResult, MetricError, FiredAlert, MonitoringReport  \u2705\n\u2502   \u251c\u2500\u2500 alert.py         # AlertPolicy (Protocol), ThresholdPolicy, AlertRule   \u2705\n\u2502   \u2514\u2500\u2500 data_selection.py   # WindowConfig, RandomSamplingConfig, PartitioningConfig       \u2705\n\u251c\u2500\u2500 metrics/\n\u2502   \u251c\u2500\u2500 registry.py      # @register_metric, get_metric, list_metrics           \u2705\n\u2502   \u251c\u2500\u2500 base.py          # Metric Protocol + compute_status                     \u2705\n\u2502   \u2514\u2500\u2500 tabular/\n\u2502       \u251c\u2500\u2500 _helpers.py      # _result_metric, _result_stat, extract_feature, _require_reference, to_float_array, shared extractors  \u2705\n\u2502       \u251c\u2500\u2500 performance.py   # accuracy \u2192 mape (12 metrics)                     \u2705\n\u2502       \u251c\u2500\u2500 drift.py         # psi, wasserstein, mmd, target_drift              \u2705\n\u2502       \u251c\u2500\u2500 tests.py         # ks_2samp \u2192 chisquare (6 metrics)                \u2705\n\u2502       \u251c\u2500\u2500 statistics.py    # mean \u2192 top_category (8 metrics)                 \u2705\n\u2502       \u251c\u2500\u2500 estimation.py    # cbpe_* (5 metrics)                               \u2705\n\u2502       \u251c\u2500\u2500 fairness.py      # demographic_parity, equalized_odds, disparate_impact \u2705\n\u2502       \u2514\u2500\u2500 profiler.py      # profile_columns() \u2014 standalone, not in registry   \u2705\n\u2502   \u251c\u2500\u2500 nlp/                 # Phase 8\n\u2502   \u2502   \u251c\u2500\u2500 quality.py       # BLEUMetric, ROUGEMetric, BERTScoreMetric\n\u2502   \u2502   \u251c\u2500\u2500 drift.py         # EmbeddingDriftMetric (MMD dans espace embeddings)\n\u2502   \u2502   \u2514\u2500\u2500 safety.py        # ToxicityMetric, PIIMetric, HallucinationMetric\n\u2502   \u2514\u2500\u2500 agent/               # Phase 9\n\u2502       \u251c\u2500\u2500 performance.py   # TaskCompletionMetric, ToolSuccessMetric\n\u2502       \u2514\u2500\u2500 cost.py          # AvgTokensMetric, AvgLatencyMetric, CostMetric\n\u251c\u2500\u2500 data/\n\u2502   \u251c\u2500\u2500 source.py        # DataSource (ABC) + DataFrameSource  \u2705  (ParquetSource, SqlSource, ExcelSource \u2014 Phase 5)\n\u2502   \u251c\u2500\u2500 sampling.py      # SamplingStrategy + FullData, LastN, TimeWindow, Random       \u2705\n\u2502   \u2514\u2500\u2500 partitioner.py   # DataPartitioner + FixedReference, TimeBased                  \u2705\n\u251c\u2500\u2500 models/              # Phase 5\n\u2502   \u251c\u2500\u2500 base.py          # ModelWrapper Protocol\n\u2502   \u251c\u2500\u2500 sklearn.py       # SklearnWrapper\n\u2502   \u251c\u2500\u2500 huggingface.py   # HuggingFaceWrapper\n\u2502   \u251c\u2500\u2500 langchain.py     # LangChainWrapper\n\u2502   \u2514\u2500\u2500 mlflow_loader.py # MlflowModelLoader (opt-in)\n\u251c\u2500\u2500 runner.py            # Runner (stateless) \u2705\n\u251c\u2500\u2500 renderers/           # Phase 6b \u2705\n\u2502   \u251c\u2500\u2500 base.py          # ReportRenderer Protocol, ChartBackend Protocol  \u2705\n\u2502   \u251c\u2500\u2500 html.py          # HtmlRenderer (core, jinja2)                     \u2705\n\u2502   \u251c\u2500\u2500 plotly.py        # PlotlyBackend (core)                            \u2705\n\u2502   \u2514\u2500\u2500 no_chart.py      # NoChartBackend (headless)                       \u2705\n\u251c\u2500\u2500 stores/              # Phase 6a \u2705 (InMemoryStore + SqliteStore)\n\u2502   \u251c\u2500\u2500 base.py          # ResultStore Protocol                         \u2705\n\u2502   \u251c\u2500\u2500 _helpers.py      # to_row(), extract_plan_meta(), report_to_rows() \u2705\n\u2502   \u251c\u2500\u2500 memory.py        # InMemoryStore (core) \u2014 tests et notebooks    \u2705\n\u2502   \u251c\u2500\u2500 sqlite.py        # SqliteStore (core, zero dep)                 \u2705\n\u2502   \u251c\u2500\u2500 json_store.py    # JsonStore (core) \u2014 Phase 7 suite\n\u2502   \u251c\u2500\u2500 parquet.py       # ParquetStore (opt-in) \u2014 Phase 7 suite\n\u2502   \u251c\u2500\u2500 sql.py           # SqlStore (opt-in, SQLAlchemy) \u2014 Phase 7 suite\n\u2502   \u2514\u2500\u2500 mlflow.py        # MlflowStore (opt-in) \u2014 Phase 7 suite\n\u251c\u2500\u2500 sinks/               # Phase 6b \u2705 (ResultSink protocol + channels)\n\u2502   \u251c\u2500\u2500 base.py          # ResultSink Protocol                          \u2705\n\u2502   \u251c\u2500\u2500 email.py         # EmailChannel (core, smtplib)                 \u2705\n\u2502   \u2514\u2500\u2500 webhook.py       # WebhookChannel (core, urllib)                \u2705\n\u251c\u2500\u2500 advisor/             # \u2705 automatic MonitoringPlan generation from data\n\u2502   \u251c\u2500\u2500 __init__.py      # exports: MetricDesigner, SuggestedPlan       \u2705\n\u2502   \u251c\u2500\u2500 designer.py      # MetricDesigner \u2014 public API                  \u2705\n\u2502   \u251c\u2500\u2500 _plan.py         # SuggestedPlan \u2014 output dataclass             \u2705\n\u2502   \u251c\u2500\u2500 _analysis.py     # analyze_columns(), ColumnAnalysis dataclass  \u2705\n\u2502   \u2514\u2500\u2500 _rules.py        # suggest_drift_specs(), suggest_performance_specs(), suggest_statistics_specs()  \u2705\n\u251c\u2500\u2500 explain/             # Phase 10\u201311\n\u2502   \u251c\u2500\u2500 drift_impact.py  # DriftAttributor \u2014 Apache 2.0\n\u2502   \u251c\u2500\u2500 shap_monitor.py  # SHAP distribution monitoring \u2014 commercial edition\n\u2502   \u251c\u2500\u2500 llm_judge.py     # LLM-as-judge \u2014 commercial edition\n\u2502   \u2514\u2500\u2500 experimental/    # commercial edition\n\u251c\u2500\u2500 io/\n\u2502   \u2514\u2500\u2500 plan_parser.py   # MonitoringPlan.from_yaml / to_yaml  (stub \u2014 Phase 6)\n\u2514\u2500\u2500 exceptions.py        # Hi\u00e9rarchie d'exceptions              \u2705\n</code></pre>"},{"location":"architecture/#types-core-decisions-figees","title":"Types core \u2014 d\u00e9cisions fig\u00e9es","text":""},{"location":"architecture/#metrictype","title":"MetricType","text":"<pre><code>class MetricType(str, Enum):\n    performance = \"performance\"\n    drift       = \"drift\"\n    statistics  = \"statistics\"\n    fairness    = \"fairness\"\n    nlp_quality = \"nlp_quality\"\n    nlp_drift   = \"nlp_drift\"\n    nlp_safety  = \"nlp_safety\"\n    agent_performance = \"agent_performance\"\n    agent_cost  = \"agent_cost\"\n    custom      = \"custom\"\n</code></pre>"},{"location":"architecture/#metricspec-frozen","title":"MetricSpec (frozen)","text":"<p>Ce qu'on calcule. Agnostique du mod\u00e8le et du temps \u2014 r\u00e9utilisable sur plusieurs versions. <code>model_id</code> et <code>model_version</code> sont dans <code>ExecutionContext</code>, PAS ici. Raison : la m\u00eame spec doit s'appliquer \u00e0 plusieurs versions (champion/challenger, A/B).</p> <pre><code>class MetricSpec(BaseModel):\n    name: str                                       # cl\u00e9 dans le registry\n    metric_type: MetricType | None = None           # None = le registry d\u00e9termine le type\n    feature_name: str | None = None                 # None = m\u00e9trique globale (ex: accuracy)\n    params: dict[str, Any] = Field(default_factory=dict)\n    threshold: float | list[float] | None = None\n    upper_bound: bool = True                        # True = valeur doit \u00eatre &lt;= threshold\n    model_config = ConfigDict(frozen=True)\n</code></pre>"},{"location":"architecture/#executioncontext-frozen","title":"ExecutionContext (frozen)","text":"<p>Tout ce qui varie d'un run \u00e0 l'autre. Construit par le Runner.</p> <pre><code>class ExecutionContext(BaseModel):\n    run_id: str                            # UUID hex auto-g\u00e9n\u00e9r\u00e9 \u2014 corr\u00e8le les lignes dans un store\n    model_id: str\n    model_version: str\n    eval_timestamp: datetime\n    period_start: datetime | None = None   # None si timestamp_col non configur\u00e9\n    period_end: datetime | None = None\n    n_current: int | None = None           # lignes dans la fen\u00eatre current apr\u00e8s filtrage\n    n_reference: int | None = None         # lignes dans la fen\u00eatre reference, None si absente\n    model_config = ConfigDict(frozen=True)\n</code></pre>"},{"location":"architecture/#metricresult","title":"MetricResult","text":"<p>R\u00e9sultat de calcul pur. Porte spec + valeur + statut. Pas de context \u2014 celui-ci vit dans <code>MonitoringReport</code> pour \u00e9viter la duplication.</p> <pre><code>class MetricResult(BaseModel):\n    spec: MetricSpec\n    value: float | int | str | None = None\n    status: bool | None = None                      # None si pas de threshold\n    conf_interval: tuple[float, float] | None = None\n    effect_size: float | None = None                # Cohen's d, Cliff's delta, KS D\u2026\n    effect_size_label: str | None = None            # \"cohen_d\" | \"cliff_delta\" | \"ks_statistic\"\u2026\n</code></pre>"},{"location":"architecture/#columntype","title":"ColumnType","text":"<p>Resolved classification of a DataFrame column, produced once per run by <code>classify_columns()</code> in <code>ayn_ml.metrics.tabular._helpers</code> and used by the Runner to route metrics before calling <code>compute()</code>.</p> <pre><code>class ColumnType(str, Enum):\n    numeric     = \"numeric\"      # continuous or ordinal (float or multi-valued int)\n    categorical = \"categorical\"  # string or integer-encoded categorical\n    binary      = \"binary\"       # int column whose unique values \u2286 {0, 1}\n</code></pre> <p>Resolution order per column: 1. <code>TabularSchema.feature_types</code> explicit declaration \u2014 wins. 2. numpy dtype inference: integer dtype with unique values <code>\u2286 {0, 1}</code> \u2192 <code>binary</code>; other numeric \u2192 <code>numeric</code>; everything else \u2192 <code>categorical</code>.</p> <p><code>binary</code> is intentionally distinct from <code>numeric</code> and <code>categorical</code>: it is accepted by both continuous tests (KS, Wasserstein) and the chi-square homogeneity test.</p>"},{"location":"architecture/#dataschema-hierarchie-par-modalite","title":"DataSchema \u2014 hi\u00e9rarchie par modalit\u00e9","text":"<pre><code>class BaseSchema(BaseModel):\n    # Tous \u00e0 None par d\u00e9faut \u2014 d\u00e9clarer uniquement les colonnes pr\u00e9sentes dans le DataFrame.\n    # Le Runner valide leur pr\u00e9sence au runtime (strict=True par d\u00e9faut).\n    timestamp_col: str | None = None\n    model_id_col: str | None = None\n    model_version_col: str | None = None\n\nclass TabularSchema(BaseSchema):\n    type: Literal[\"tabular\"] = \"tabular\"            # discriminator Pydantic \u2014 obligatoire\n    label_col: str = \"y_true\"\n    prediction_col: str = \"y_pred\"\n    proba_col: str | None = \"y_pred_proba\"\n    feature_types: dict[str, Literal[\"numeric\", \"categorical\"]] = {}\n    protected_cols: list[str] | None = None         # colonnes d'attributs prot\u00e9g\u00e9s (fairness)\n\nclass TextSchema(BaseSchema):\n    type: Literal[\"text\"] = \"text\"                  # discriminator Pydantic \u2014 obligatoire\n    input_col: str = \"input_text\"\n    output_col: str = \"output_text\"\n    reference_col: str | None = \"reference_text\"\n    embedding_col: str | None = None\n\nclass AgentSchema(BaseSchema):\n    type: Literal[\"agent\"] = \"agent\"                # discriminator Pydantic \u2014 obligatoire\n    input_col: str = \"input\"\n    output_col: str = \"output\"\n    trace_col: str = \"trace\"\n    success_col: str | None = \"success\"\n    tool_calls_col: str | None = \"tool_calls\"\n    tokens_used_col: str | None = \"tokens_used\"\n    latency_col: str | None = \"latency_ms\"\n    cost_col: str | None = \"cost_usd\"\n\nDataSchema = Annotated[\n    TabularSchema | TextSchema | AgentSchema,\n    Field(discriminator=\"type\"),\n]\n</code></pre>"},{"location":"architecture/#windowconfig-randomsamplingconfig-partitioningconfig-coredata_selectionpy","title":"WindowConfig, RandomSamplingConfig, PartitioningConfig (<code>core/data_selection.py</code>)","text":"<p>Configuration d\u00e9clarative pour la s\u00e9lection des donn\u00e9es. <code>WindowConfig</code> et <code>RandomSamplingConfig</code> vivent dans <code>MonitoringPlan</code> (round-trippable JSON/YAML). <code>PartitioningConfig</code> est un alias de type pour <code>TimeBasedPartitioningConfig</code> \u2014 instanci\u00e9 directement \u00e0 l'ex\u00e9cution, pas d\u00e9clar\u00e9 sur le plan.</p> <pre><code># WindowConfig \u2014 s\u00e9lection de la fen\u00eatre courante\nclass FullWindowConfig(BaseModel):\n    type: Literal[\"full\"] = \"full\"          # identit\u00e9 \u2014 DataFrame utilis\u00e9 tel quel\n\nclass LastNRowsWindowConfig(BaseModel):\n    type: Literal[\"last_n\"] = \"last_n\"\n    n: int                                  # N derni\u00e8res lignes (ordre chronologique suppos\u00e9)\n\nclass TimeWindowConfig(BaseModel):\n    type: Literal[\"time_window\"] = \"time_window\"\n    start: datetime\n    end: datetime                           # [start, end] inclus\n\nWindowConfig = Annotated[\n    FullWindowConfig | LastNRowsWindowConfig | TimeWindowConfig,\n    Field(discriminator=\"type\"),\n]\n\n# RandomSamplingConfig \u2014 sous-\u00e9chantillonnage pour la performance\nclass RandomSamplingConfig(BaseModel):\n    type: Literal[\"random\"] = \"random\"\n    n: int | None = None                    # exactement un de n ou frac requis\n    frac: float | None = None              # frac dans (0, 1]\n    seed: int | None = None\n\n# PartitioningConfig \u2014 split current / reference depuis une seule source\nclass TimeBasedPartitioningConfig(BaseModel):\n    type: Literal[\"time_based\"] = \"time_based\"\n    cutoff: datetime\n    reference_window: timedelta\n    # current   \u2192 timestamp &gt; cutoff\n    # reference \u2192 timestamp dans (cutoff - reference_window, cutoff]\n\nPartitioningConfig = TimeBasedPartitioningConfig\n</code></pre>"},{"location":"architecture/#monitoringplan","title":"MonitoringPlan","text":"<p>Config d\u00e9clarative et s\u00e9rialisable (YAML/JSON). Une instance par mod\u00e8le.</p> <pre><code>class MonitoringPlan(BaseModel):\n    name: str\n    model_id: str\n    model_version: str\n    data_schema: DataSchema\n    metrics: list[MetricSpec]\n    description: str = \"\"\n    enable_profiling: bool = False     # when True, Runner profiles watched cols and attaches to MonitoringReport.profile\n    window: WindowConfig | None = None\n    sampling: RandomSamplingConfig | None = None\n</code></pre>"},{"location":"architecture/#firedalert","title":"FiredAlert","text":"<p>Enregistrement l\u00e9ger d'une alerte d\u00e9clench\u00e9e. Distinct de <code>AlertRule</code> (config) \u2014 porte seulement ce qui est n\u00e9cessaire pour logguer ou notifier.</p> <pre><code>@dataclass\nclass FiredAlert:\n    metric_name: str          # nom de la m\u00e9trique qui a d\u00e9clench\u00e9 l'alerte\n    policy_type: str          # \"threshold\" | \"change\" | \"consecutive\"\n    details: dict[str, Any]   # contexte sp\u00e9cifique \u00e0 la policy\n</code></pre>"},{"location":"architecture/#monitoringreport","title":"MonitoringReport","text":"<p>Sortie du Runner. R\u00e9sultats + erreurs non-fatales (une m\u00e9trique qui plante n'arr\u00eate pas tout).</p> <pre><code>@dataclass\nclass MonitoringReport:\n    plan: MonitoringPlan\n    context: ExecutionContext\n    results: list[MetricResult]\n    errors: list[MetricError]\n    fired_alerts: list[FiredAlert] = field(default_factory=list)\n    profile: dict[str, dict[str, float | int | str | None]] | None = None\n    # profile is set by the Runner when plan.enable_profiling=True; None otherwise.\n    # Keys are column names; values are ColumnProfile dicts (see profiler.py).\n\n    def to_dict(self) -&gt; dict: ...\n    # Includes \"profile\" key only when profile is not None.\n    def to_dataframe(self) -&gt; Any: ...   # tidy DataFrame: une ligne par MetricResult + champs de contexte\n</code></pre>"},{"location":"architecture/#metric-protocol-registry","title":"Metric Protocol + Registry","text":"<pre><code>class Metric(Protocol):\n    name: str\n    metric_type: MetricType\n    requires_reference: bool   # True pour drift et tests statistiques; False pour performance et statistiques\n\n    # Column-type routing \u2014 checked by the Runner before compute() is called.\n    # Omitting accepted_column_types skips the feature-column check entirely.\n    accepted_column_types: frozenset[ColumnType]                  # which ColumnType values are valid for spec.feature_name\n    accepted_target_types: dict[str, frozenset[ColumnType]]       # schema attr \u2192 accepted ColumnType set (fairness metrics only)\n\n    def compute(\n        self,\n        current: Any,\n        reference: Any | None,\n        schema: DataSchema,\n        spec: MetricSpec,\n    ) -&gt; MetricResult: ...\n</code></pre>"},{"location":"architecture/#statistical-profiling-metricstabularprofilerpy","title":"Statistical Profiling \u2014 <code>metrics/tabular/profiler.py</code>","text":"<p>Standalone module. Not part of the metric registry and not configurable via <code>MetricSpec</code>. Activated by setting <code>MonitoringPlan.enable_profiling = True</code>; the Runner calls it after metric execution.</p> <pre><code>ColumnProfile = dict[str, float | int | str | None]\n\ndef profile_columns(\n    df: Any,\n    col_names: list[str],\n    schema: DataSchema,\n) -&gt; dict[str, ColumnProfile]:\n    \"\"\"Compute a statistical profile for each requested column.\n\n    Uses classify_columns() to resolve each column's ColumnType (respecting\n    TabularSchema.feature_types overrides).  Columns absent from df are\n    silently skipped.\n\n    Numeric / binary columns \u2192 min, max, mean, std, p25, p50, p75, null_count, null_pct\n    Categorical columns      \u2192 null_count, null_pct, n_unique, top_category\n    \"\"\"\n</code></pre> <p>Columns profiled by the Runner (when <code>enable_profiling=True</code>): - Every <code>spec.feature_name</code> declared across all <code>MetricSpec</code> entries in the plan - All columns declared on the schema (<code>schema.column_names</code>) \u2014 covers <code>prediction_col</code>, <code>label_col</code>, <code>proba_col</code>, <code>timestamp_col</code>, and any other schema-level columns</p> <p>The result is a <code>dict[str, ColumnProfile]</code> attached to <code>MonitoringReport.profile</code> and included in <code>to_dict()</code> under the <code>\"profile\"</code> key.</p> <p><code>accepted_column_types</code> \u2014 declared on metrics that operate on <code>spec.feature_name</code>. The Runner checks <code>column_types[spec.feature_name]</code> against this frozenset before calling <code>compute()</code>. If the resolved <code>ColumnType</code> is not in the set, a <code>MetricError(error_type=\"SchemaError\")</code> is returned without calling <code>compute()</code>. Attribute absent \u2192 check skipped (no routing).</p> <p><code>accepted_target_types</code> \u2014 declared on metrics that also validate schema-level columns such as <code>prediction_col</code> or <code>label_col</code> (currently used only by the three fairness metrics). Keys are <code>TabularSchema</code> attribute names (e.g. <code>\"prediction_col\"</code>); values are the accepted <code>ColumnType</code> frozensets for that column. Same early-return semantics as <code>accepted_column_types</code>.</p> <p>Ajouter une m\u00e9trique = une classe + <code>@register_metric(\"nom\")</code>. Aucune modification de fichiers existants.</p> <p>R\u00e8gle d'impl\u00e9mentation : preprocessing avec narwhals, computation statistique via <code>.to_numpy()</code> (scipy/sklearn). Ne pas r\u00e9impl\u00e9menter scipy en narwhals \u2014 la conversion est n\u00e9gligeable (zero-copy avec Polars/Arrow).</p>"},{"location":"architecture/#data-layer","title":"Data Layer","text":""},{"location":"architecture/#datasource","title":"DataSource","text":"<pre><code># Impl\u00e9ment\u00e9 \u2705\nclass DataSource(ABC):\n    def load(self, plan: MonitoringPlan) -&gt; Any: ...\n    # Projette automatiquement aux colonnes requises par le plan\n\nclass DataFrameSource(DataSource):\n    def __init__(self, data: pd.DataFrame | pl.DataFrame): ...\n\n# \u00c0 impl\u00e9menter \u2014 Phase 5\nclass ParquetSource(DataSource):\n    path: str\n    columns: list[str] | None = None\n    filters: list | None = None         # pushdown filtering pyarrow\n\nclass SqlSource(DataSource):            # pip install ayn-ml[sql]\n    connection_string: str              # SQLAlchemy URI\n    query: str\n\nclass ExcelSource(DataSource):          # pip install ayn-ml[excel]\n    path: str\n    sheet_name: str | int = 0\n</code></pre>"},{"location":"architecture/#samplingstrategy","title":"SamplingStrategy","text":"<pre><code>class SamplingStrategy(ABC):\n    def sample(self, df: Any, schema: DataSchema) -&gt; Any: ...\n\nclass FullDataSampling(SamplingStrategy):\n    pass  # identit\u00e9 \u2014 retourne le DataFrame tel quel\n\nclass LastNRowsSampling(SamplingStrategy):\n    def __init__(self, n: int): ...          # n doit \u00eatre positif\n\nclass TimeWindowSampling(SamplingStrategy):\n    def __init__(self, start: datetime, end: datetime): ...  # filtre [start, end] inclus\n\nclass RandomSampling(SamplingStrategy):\n    def __init__(self, n: int | None = None, frac: float | None = None, seed: int | None = None): ...\n    # exactement un de n ou frac requis\n</code></pre>"},{"location":"architecture/#datapartitioner","title":"DataPartitioner","text":"<pre><code>class DataPartitioner(ABC):\n    def partition(self, df: Any, schema: DataSchema) -&gt; tuple[Any, Any | None]: ...\n\nclass FixedReferencePartitioner(DataPartitioner):\n    def __init__(self, reference: Any): ...\n\nclass TimeBasedPartitioner(DataPartitioner):\n    def __init__(self, cutoff: datetime, reference_window: timedelta): ...\n    # current   \u2192 rows avec timestamp &gt; cutoff\n    # reference \u2192 rows avec timestamp dans (cutoff - reference_window, cutoff]\n</code></pre>"},{"location":"architecture/#model-layer","title":"Model Layer","text":"<pre><code>class ModelWrapper(Protocol):\n    def predict(self, data: Any, schema: DataSchema) -&gt; Any: ...\n\nclass SklearnWrapper:\n    def __init__(self, model: Any, predict_proba: bool = False): ...\n\nclass HuggingFaceWrapper:\n    def __init__(self, pipeline: Any): ...\n\nclass LangChainWrapper:\n    def __init__(self, chain: Any): ...\n\n# pip install ayn-ml[mlflow]\nclass MlflowModelLoader:\n    def load(self, model_uri: str) -&gt; ModelWrapper: ...\n    # model_uri = \"models:/churn_model/Production\"\n</code></pre>"},{"location":"architecture/#runner","title":"Runner","text":"<p>Stateless. <code>ExecutionContext</code> construit depuis le plan + donn\u00e9es.</p> <pre><code>class Runner:\n    def __init__(self, n_jobs: int = 1, strict: bool = True): ...\n    # n_jobs=1 s\u00e9quentiel, n_jobs=-1 tous les CPUs, n_jobs&gt;=2 pool de threads\n    # strict=True (d\u00e9faut) : SchemaError si colonne d\u00e9clar\u00e9e absente du DataFrame\n    # strict=False : warning + d\u00e9gradation gracieuse \u2014 pour l'\u00e9valuation exploratoire\n\n    def run(\n        self,\n        plan: MonitoringPlan,\n        current: pd.DataFrame | pl.DataFrame | DataSource,\n        reference: pd.DataFrame | pl.DataFrame | DataSource | None = None,\n        model: ModelWrapper | None = None,\n        store: ResultStore | None = None,\n        sinks: list[ResultSink] | None = None,\n        alert_rules: list[AlertRule] | None = None,\n    ) -&gt; MonitoringReport:\n        # 1. R\u00e9sout les DataSources (wrap DataFrames bruts si n\u00e9cessaire)\n        # 2. Applique la fen\u00eatre (plan.window)\n        # 3. Sous-\u00e9chantillonnage al\u00e9atoire (plan.sampling, en plus de la fen\u00eatre)\n        # 4. Filtre les lignes par model_id + model_version (via schema)\n        # 5. Valide les colonnes (_validate_plan_columns) \u2014 SchemaError si strict=True\n        #    \u00b7 time_window sans timestamp_col configur\u00e9 \u2192 toujours SchemaError\n        #    \u00b7 colonnes d'infrastructure absentes \u2192 SchemaError ou warning selon strict\n        #    \u00b7 feature_name absents \u2192 SchemaError ou warning selon strict\n        # 6. Construit ExecutionContext (run_id, bornes de p\u00e9riode, n_current, n_reference)\n        # 7. Classifie les colonnes une seule fois via classify_columns(current_df, schema)\n        #    \u2192 dict[str, ColumnType] r\u00e9utilis\u00e9 pour chaque m\u00e9trique du plan\n        #    \u00b7 ColumnType routing : si metric.accepted_column_types est d\u00e9fini et que\n        #      ColumnType de spec.feature_name n'y figure pas \u2192 MetricError(SchemaError)\n        #      sans appel \u00e0 compute()\n        #    \u00b7 accepted_target_types : idem pour les colonnes schema (prediction_col, label_col\u2026)\n        # 8. Ex\u00e9cute chaque MetricSpec via le registry \u2014 erreurs isol\u00e9es par m\u00e9trique\n        # 9. \u00c9value les alert_rules (ThresholdPolicy \u2014 stateless, pas de store requis)\n        #     \u00b7 FiredAlert cr\u00e9\u00e9 par rule si policy.evaluate(result) \u2192 True\n        # 10. Profiling statistique (opt-in via plan.enable_profiling) :\n        #     \u00b7 Collecte les feature_name de chaque spec + toutes les colonnes d\u00e9clar\u00e9es\n        #       sur le schema via schema.column_names\n        #     \u00b7 Appelle profile_columns(current_df, sorted(feature_cols), plan.data_schema)\n        #     \u00b7 Attache le r\u00e9sultat \u00e0 MonitoringReport.profile\n        # 11. \u00c9crit le report dans store (toujours, si fourni)\n        # 12. Dispatch sinks inconditionnels (sinks= \u2014 chaque run, peu importe les alertes)\n        # 13. Dispatch channels des AlertRule ayant fir\u00e9 (report complet, fired_alerts non vide)\n        # Note : model.predict() \u2192 Phase 5 (model= accept\u00e9 mais ignor\u00e9 avec warning)\n</code></pre> <p>A scheduled cloud runner with parallelism, retry logic, and audit trail is available in the commercial edition.</p> <p>R\u00e8gle store/sinks : - <code>store</code> re\u00e7oit toujours le report (persistance) - <code>sinks</code> re\u00e7oivent le report \u00e0 chaque run, inconditionnellement (ex : sink de logging, push dashboard) - Les canaux de notification li\u00e9s aux <code>AlertRule</code> re\u00e7oivent le report uniquement quand l'alerte se d\u00e9clenche - <code>ResultStore</code> \u00e9tend <code>ResultSink</code> mais ne doit pas figurer dans <code>sinks</code></p>"},{"location":"architecture/#alertes","title":"Alertes","text":""},{"location":"architecture/#alertpolicy","title":"AlertPolicy","text":"<pre><code>@runtime_checkable\nclass AlertPolicy(Protocol):\n    \"\"\"Stateless callable protocol: evaluate(result) -&gt; bool.\"\"\"\n    @property\n    def policy_type(self) -&gt; str: ...\n    def evaluate(self, result: MetricResult) -&gt; bool: ...\n    def details(self, result: MetricResult) -&gt; dict[str, Any]: ...\n\n@dataclass(frozen=True)\nclass ThresholdPolicy:\n    \"\"\"Fires when MetricResult.status is False, or when value crosses a custom threshold.\"\"\"\n    threshold: float | None = None   # None = delegate to result.status\n    upper_bound: bool = True         # True \u2192 fire when value &gt; threshold; False \u2192 value &lt; threshold\n</code></pre> <p>Impl\u00e9ment\u00e9 \u2705 : <code>ThresholdPolicy</code> \u2014 stateless, pas de store requis.</p> <p>D\u00e9f\u00e9r\u00e9 (v2) : - <code>ChangePolicy(pct_change: float)</code> \u2014 alerte si variation &gt; X% vs run pr\u00e9c\u00e9dent (n\u00e9cessite <code>read_history()</code>) - <code>ConsecutivePolicy(n: int)</code> \u2014 alerte si <code>status == False</code> pendant N runs cons\u00e9cutifs (n\u00e9cessite <code>read_history()</code>)</p>"},{"location":"architecture/#alertrule","title":"AlertRule","text":"<pre><code>@dataclass\nclass AlertRule:\n    metric_name: str          # doit correspondre \u00e0 un MetricSpec.name dans le plan\n    policy: AlertPolicy\n    channels: list[ResultSink] = field(default_factory=list)\n</code></pre> Channel Extra requis Usage <code>EmailChannel</code> aucun (smtplib) Alertes email <code>WebhookChannel</code> aucun (urllib) POST JSON vers n'importe quel endpoint \u2014 accepte <code>extra_headers</code> et <code>timeout</code>"},{"location":"architecture/#stores-et-sinks","title":"Stores et Sinks","text":""},{"location":"architecture/#resultsink-write-only","title":"ResultSink (write-only) \u2705","text":"<pre><code>class ResultSink(Protocol):\n    def write(self, report: MonitoringReport) -&gt; None: ...\n</code></pre> <p>Impl\u00e9ment\u00e9 dans <code>ayn_ml/sinks/base.py</code>. <code>EmailChannel</code> et <code>WebhookChannel</code> impl\u00e9mentent ce protocole.</p>"},{"location":"architecture/#resultstore-bidirectionnel","title":"ResultStore (bidirectionnel) \u2705","text":"<p><code>ResultStore</code> \u00e9tend <code>ResultSink</code> \u2014 tout store peut aussi servir de sink.</p> <pre><code>class ResultStore(ResultSink, Protocol):\n    def write(self, report: MonitoringReport) -&gt; None: ...\n\n    def read_history(\n        self,\n        model_id: str,\n        model_version: str | None = None,\n        metric_name: str | None = None,\n        limit: int | None = None,        # None = pas de limite\n        get_metadata: bool = False,\n    ) -&gt; list[dict[str, Any]]: ...       # flat rows, newest first\n\n    def get_report(self, run_id: str) -&gt; MonitoringReport | None: ...\n</code></pre> <p><code>read_history()</code> \u2014 colonnes retourn\u00e9es (sans <code>get_metadata</code>) : <code>run_id</code>, <code>model_id</code>, <code>model_version</code>, <code>metric_name</code>, <code>feature_name</code>, <code>value</code>, <code>status</code>, <code>effect_size</code>, <code>effect_size_label</code>, <code>period_start</code>, <code>period_end</code></p> <p>Avec <code>get_metadata=True</code> \u2014 colonnes additionnelles (JOIN avec <code>monitoring_runs</code>, plan JSON \u00e9clat\u00e9) : <code>plan_name</code>, <code>plan_window_type</code>, <code>plan_window_n</code>, <code>plan_sampling_type</code>, <code>plan_sampling_frac</code>, <code>run_n_current</code>, <code>run_n_reference</code></p> <p>Le retour est directement <code>pd.DataFrame()</code>-compatible.</p>"},{"location":"architecture/#stores-implementes","title":"Stores impl\u00e9ment\u00e9s \u2705","text":"Store D\u00e9pendance Statut Notes <code>InMemoryStore</code> core \u2705 <code>collections.deque</code>, <code>maxlen</code> optionnel, thread-safe \u2014 id\u00e9al tests et notebooks <code>SqliteStore</code> core (stdlib) \u2705 zero dep, 4 tables normalis\u00e9es, idempotent sur <code>run_id</code> <code>JsonStore</code> core \u00c0 faire fichier JSON local, zero dependency <code>ParquetStore</code> <code>ayn-ml[parquet]</code> \u00c0 faire efficace pour l'historique <code>SqlStore</code> <code>ayn-ml[sql]</code> \u00c0 faire SQLAlchemy, PostgreSQL/MySQL/SQLite <code>MlflowStore</code> <code>ayn-ml[mlflow]</code> \u00c0 faire JSON artifact + <code>log_metric()</code> par m\u00e9trique pour l'UI"},{"location":"architecture/#sqlitestore-schema-sqlite","title":"SqliteStore \u2014 sch\u00e9ma SQLite \u2705","text":"<p>Quatre tables normalis\u00e9es, ouvertes avec <code>check_same_thread=False</code> + <code>threading.Lock</code>.</p> <pre><code>monitoring_runs          \u2014 une ligne par ex\u00e9cution Runner\n  run_id TEXT PK\n  model_id, model_version, eval_timestamp\n  n_current, n_reference\n  plan_json TEXT         \u2014 MonitoringPlan complet s\u00e9rialis\u00e9\n\nmetric_results           \u2014 une ligne par MetricResult\n  id INTEGER PK AUTOINCREMENT\n  run_id FK\n  model_id, model_version          \u2014 d\u00e9normalis\u00e9s (pas de JOIN pour les time-series)\n  metric_name, feature_name\n  value, status, effect_size, effect_size_label\n  period_start, period_end         \u2014 d\u00e9normalis\u00e9s (requ\u00eates temporelles sans JOIN)\n\nmetric_errors            \u2014 une ligne par MetricError\n  id, run_id FK, metric_name, error_type, message\n\nfired_alerts             \u2014 une ligne par FiredAlert\n  id, run_id FK, metric_name, policy_type, details_json\n</code></pre> <p>Index : <code>(model_id, model_version)</code> et <code>(model_id, metric_name, period_start)</code>.</p> <p><code>write()</code> est idempotent \u2014 un doublon de <code>run_id</code> est d\u00e9tect\u00e9 et ignor\u00e9 silencieusement.</p>"},{"location":"architecture/#usage-des-stores-avec-le-runner","title":"Usage des stores avec le Runner","text":"<pre><code>from ayn_ml.stores import InMemoryStore, SqliteStore\n\n# In-memory (tests, notebooks)\nstore = InMemoryStore()\nreport = Runner().run(plan, df, store=store)\nrows = store.read_history(\"fraud_v2\", metric_name=\"auc\")\ndf_history = pd.DataFrame(rows)\n\n# SQLite persistant (prod locale, CI)\nwith SqliteStore(\"monitoring.db\") as store:\n    report = Runner().run(plan, df, store=store)\n    rows = store.read_history(\"fraud_v2\", get_metadata=True)\n</code></pre> Modalit\u00e9 Store recommand\u00e9 Sink d'alerte Dev / CI InMemoryStore \u2014 Prod locale (1 machine) SqliteStore EmailChannel, WebhookChannel Tabular ML (multi-machine) SqlStore, MlflowStore EmailChannel, WebhookChannel NLP / LLM JsonStore, ParquetStore WebhookChannel Agent JsonStore, MlflowStore WebhookChannel"},{"location":"architecture/#advisor-ayn_mladvisor","title":"Advisor \u2014 <code>ayn_ml/advisor/</code>","text":"<p>Automatic <code>MonitoringPlan</code> generation from data characteristics.  Not a runtime component \u2014 call it once during setup to bootstrap a plan, then pass the resulting plan to <code>Runner</code>.</p>"},{"location":"architecture/#metricdesigner","title":"MetricDesigner","text":"<pre><code>class MetricDesigner:\n    def __init__(self, schema: TabularSchema) -&gt; None: ...\n\n    def suggest(\n        self,\n        df: Any,\n        *,\n        reference: Any,\n        task_type: str = \"classification\",  # \"classification\" | \"regression\"\n        name: str = \"suggested_plan\",\n        model_id: str = \"\",\n        model_version: str = \"\",\n    ) -&gt; SuggestedPlan: ...\n</code></pre> <p>The constructor accepts a <code>TabularSchema</code> once so the same instance can be called with different DataFrames (e.g. different time windows) without re-specifying the schema.</p> <p><code>suggest()</code> analyses each feature column (normality, skewness, sample size, optional variance ratio vs. <code>reference</code>) and assembles a <code>MonitoringPlan</code> containing:</p> <ul> <li>Performance specs \u2014 selected by <code>task_type</code> and class imbalance ratio.</li> <li>Target drift \u2014 <code>target_drift</code> spec always included.</li> <li>Drift specs \u2014 one set per feature column; routes to parametric (<code>ttest</code>) or non-parametric (<code>mannwhitney</code>) tests based on normality and sample size; adds <code>levene</code> when variance ratio analysis (requires <code>reference</code>) indicates heteroscedasticity.</li> <li>Drift specs \u2014 one set per feature column based on column type and sample size.</li> </ul> <p><code>reference</code> is used only for variance-ratio analysis inside <code>suggest()</code>.  It does not affect the <code>MonitoringPlan</code> produced.</p> <p>Raises <code>ValueError</code> for invalid <code>task_type</code> or when <code>reference</code> is <code>None</code>.</p>"},{"location":"architecture/#suggestedplan","title":"SuggestedPlan","text":"<pre><code>@dataclass\nclass SuggestedPlan:\n    plan: MonitoringPlan          # ready to pass to Runner\n    warnings: list[str]           # advisory messages \u2014 advisor reasoning per column/metric\n\n    def to_dict(self) -&gt; dict[str, Any]: ...\n    # {\"plan\": plan.model_dump(), \"warnings\": [...]}\n</code></pre>"},{"location":"architecture/#internal-helpers","title":"Internal helpers","text":"<p><code>analyze_columns(df, schema, reference)</code> in <code>_analysis.py</code> produces one <code>ColumnAnalysis</code> per feature column.  <code>to_float_array(series_native)</code> in <code>metrics/tabular/_helpers.py</code> is shared between <code>_analysis.py</code> and <code>profiler.py</code> \u2014 strips NaNs and returns a <code>float64</code> numpy array.</p>"},{"location":"architecture/#hierarchie-dexceptions","title":"Hi\u00e9rarchie d'exceptions","text":"<pre><code>class AynError(Exception): ...\nclass UnknownMetricError(AynError): ...      # m\u00e9trique non enregistr\u00e9e\nclass SchemaError(AynError): ...             # colonne manquante ou type incompatible\nclass ThresholdError(AynError): ...          # threshold invalide\nclass InsufficientDataError(AynError): ...   # pas assez de donn\u00e9es\nclass MetricComputeError(AynError): ...      # erreur dans compute() \u2014 non-fatale dans Runner\n</code></pre>"},{"location":"architecture/#visualisation","title":"Visualisation","text":"<ul> <li><code>HtmlRenderer</code> est une d\u00e9pendance core (jinja2 + plotly)</li> <li>Graphiques via <code>ChartBackend</code> Protocol : <code>PlotlyBackend</code> (d\u00e9faut) ou <code>NoChartBackend</code> (headless)</li> <li>Pas de SvgBackend \u2014 Plotly est la seule option graphique</li> </ul> <pre><code>HtmlRenderer().render(report)                          # snapshot: histogramme + tableau vert/jaune/rouge\nHtmlRenderer().render_history(reports)                 # s\u00e9ries temporelles + bande de seuil + points d'alerte\nHtmlRenderer(charts=NoChartBackend()).render(report)   # tableaux seulement\n</code></pre> <p>Pas de dashboard built-in \u2014 d\u00e9l\u00e9gu\u00e9 aux sinks (Grafana, MLflow).</p>"},{"location":"architecture/#explicabilite-module-explain","title":"Explicabilit\u00e9 \u2014 module explain/","text":"<p>Niveau 1 \u2014 DriftAttributor (v1, zero overhead)</p> <pre><code>@dataclass\nclass DriftContribution:\n    feature_name: str\n    drift_score: float       # PSI ou Wasserstein de la feature\n    importance: float        # feature_importances_ du mod\u00e8le sklearn\n    attribution: float       # drift_score \u00d7 importance, normalis\u00e9 [0-1]\n\n@dataclass\nclass AttributionReport:\n    performance_metric: MetricResult\n    contributions: list[DriftContribution]\n    context: ExecutionContext\n</code></pre> <p>Usage : <code>DriftAttributor(report, model_wrapper).attribute(\"accuracy\")</code> \u2192 <code>AttributionReport</code></p> <p>Niveau 2 \u2014 SHAP distribution monitor (v2, opt-in, <code>ayn-ml-pro[shap]</code>)</p> <p>Logger les SHAP values sur un \u00e9chantillon (1\u20135%) \u2192 surveiller leur distribution dans le temps.</p> <p>Niveau 3 \u2014 LLM-as-judge (v2, opt-in, <code>ayn-ml[llm-judge]</code>)</p> <p>Via <code>ragas</code> ou <code>deepeval</code> \u2014 pas de r\u00e9impl\u00e9mentation.</p>"},{"location":"architecture/#experimental-explainexperimental","title":"Exp\u00e9rimental \u2014 explain/experimental/","text":"<p>Marqu\u00e9 <code>@experimental</code> \u2014 API instable, non couvert par semver.</p> Module M\u00e9thode Priorit\u00e9 <code>distribution_correction.py</code> Counterfactuel distribution-level par feature Premier \u00e0 impl\u00e9menter <code>xpe.py</code> XPE \u2014 Optimal Transport + Shapley (KDD 2024) Opportunit\u00e9 unique (pas d'OSS) <code>token_shap.py</code> Attribution token-level LLM \u00c0 surveiller (PyPI instable) <code>agent_causal.py</code> Attribution causale agents multi-step Gap acad\u00e9mique non r\u00e9solu"},{"location":"architecture/#backend-et-performance","title":"Backend et performance","text":"<ul> <li>Preprocessing \u2192 narwhals (backend-agnostic)</li> <li>Computation \u2192 numpy via <code>.to_numpy()</code> (scipy/sklearn)</li> <li>Zero-copy avec Polars/Arrow \u2014 la conversion narwhals \u2192 numpy est n\u00e9gligeable</li> </ul> Contexte Backend recommand\u00e9 Dev / notebooks pandas Production single-node Polars Donn\u00e9es &gt; RAM DuckDB + Parquet via DataSource <p>Rust : b\u00e9n\u00e9fice via Polars uniquement. Pas de code Rust custom en v1. Voir <code>docs/research/rust-ml-monitoring-landscape.md</code>.</p>"},{"location":"architecture/#dependances-optionnelles","title":"D\u00e9pendances optionnelles","text":"<pre><code>[tool.poetry.extras]\npolars          = [\"polars\"]\npandas          = [\"pandas\"]\nsql             = [\"sqlalchemy\", \"pymysql\"]\nparquet         = [\"pyarrow\"]\nexcel           = [\"openpyxl\"]\nmlflow          = [\"mlflow\"]\nnlp             = [\"evaluate\", \"bert-score\"]\nnlp-embeddings  = [\"sentence-transformers\"]   # lourd \u2014 tire PyTorch\nall             = [...]\n</code></pre>"},{"location":"architecture/#metriques-implementees","title":"M\u00e9triques impl\u00e9ment\u00e9es","text":"Cat\u00e9gorie M\u00e9triques Count Performance <code>accuracy</code> <code>precision</code> <code>recall</code> <code>f1</code> <code>log_loss</code> <code>auc</code> <code>aucpr</code> <code>brier</code> <code>mse</code> <code>mae</code> <code>r2</code> <code>mape</code> 12 Drift <code>psi</code> <code>wasserstein</code> <code>mmd</code> <code>target_drift</code> 4 Tests statistiques <code>ks_2samp</code> <code>ttest</code> <code>mannwhitney</code> <code>levene</code> <code>cramervonmises</code> <code>chisquare</code> 6 Statistiques <code>mean</code> <code>median</code> <code>std</code> <code>skewness</code> <code>kurtosis</code> <code>quantile</code> <code>count</code> <code>top_category</code> 8 CBPE <code>cbpe_accuracy</code> <code>cbpe_auc</code> <code>cbpe_f1</code> <code>cbpe_precision</code> <code>cbpe_recall</code> 5 Fairness <code>demographic_parity</code> <code>equalized_odds</code> <code>disparate_impact</code> 3 Total 38"},{"location":"architecture/#notes-dimplementation-a-garder-en-tete","title":"Notes d'impl\u00e9mentation \u2014 \u00e0 garder en t\u00eate","text":"<p><code>mape</code> : Mean Absolute Percentage Error = <code>mean(|y_true - y_pred| / |y_true|) * 100</code>. Diff\u00e9rent de MAE.</p> <p>PSI : clipper les valeurs proches de z\u00e9ro pour \u00e9viter division par z\u00e9ro ou inf/NaN.</p> <p><code>chisquare</code> : attend des fr\u00e9quences pr\u00e9-calcul\u00e9es, pas des donn\u00e9es brutes. Pr\u00e9-binning explicite requis.</p> <p><code>kl_div</code> (si ajout\u00e9) : <code>scipy.special.kl_div</code> retourne un tableau. Wrapper obligatoire avec <code>sum()</code> sur distributions normalis\u00e9es.</p> <p>CBPE : aveugle au concept drift. Toujours coupler avec <code>psi</code>/<code>wasserstein</code>.</p>"},{"location":"architecture/#metriques-a-implementer","title":"M\u00e9triques \u00e0 impl\u00e9menter","text":""},{"location":"architecture/#bootstrap-de-performance-phase-2c","title":"Bootstrap de performance \u2014 Phase 2c","text":"<p>Bootstrap le reference window pour d\u00e9river un intervalle de confiance <code>[p2.5, p97.5]</code>, puis v\u00e9rifie si la valeur du current window tombe en dehors. R\u00e9sultat dans <code>MetricResult.conf_interval</code>.</p> <p>D\u00e9cision ouverte : logique bootstrap dans <code>compute()</code> (m\u00e9trique auto-contenue) ou dans le Runner (config bootstrap au niveau plan). Trancher via <code>architect-reviewer</code>.</p>"},{"location":"architecture/#nlp-phase-8-pip-install-ayn-mlnlp","title":"NLP \u2014 Phase 8 (<code>pip install ayn-ml[nlp]</code>)","text":"<p><code>bleu</code>, <code>rouge1</code>, <code>rouge2</code>, <code>rougeL</code>, <code>bert_score</code>, <code>exact_match</code>, <code>toxicity</code>, <code>embedding_drift</code></p>"},{"location":"architecture/#agent-phase-9","title":"Agent \u2014 Phase 9","text":"<p><code>task_completion_rate</code>, <code>tool_success_rate</code>, <code>step_count</code>, <code>retry_rate</code>, <code>avg_tokens</code>, <code>avg_latency_ms</code>, <code>avg_cost_usd</code>, <code>p95_latency_ms</code></p>"},{"location":"architecture/#defere-v2","title":"D\u00e9f\u00e9r\u00e9 v2","text":"<ul> <li>DLE (Direct Loss Estimation) \u2014 requiert LightGBM, feature_cols, lifecycle stateful</li> <li>DiagnosticReport \u2014 pattern-matching cross-m\u00e9trique sur MonitoringReport</li> <li><code>bftest</code> (Brown-Forsythe) \u2014 initialement pr\u00e9vu dans les tests statistiques, couvert en pratique par <code>levene</code> (variante robuste). D\u00e9f\u00e9r\u00e9 v2.</li> </ul>"},{"location":"architecture/#recherche-et-references","title":"Recherche et r\u00e9f\u00e9rences","text":"<ul> <li><code>docs/research/rust-ml-monitoring-landscape.md</code> \u2014 pourquoi pas de Rust custom en v1</li> <li><code>docs/research/ml-monitoring-landscape-2026.md</code> \u2014 analyse concurrentielle, positionnement ayn-ml</li> <li><code>docs/research/explainability-xai-monitoring-2026.md</code> \u2014 XAI dans le monitoring, XPE, gaps march\u00e9</li> </ul>"},{"location":"data-layer/","title":"Data layer","text":"<p>The data layer sits between your raw data and the runner. It has three independent concerns \u2014 compose them as needed:</p> <pre><code>DataSource  \u2192  WindowStrategy  \u2192  RandomSampling  \u2192  current\n   load         select window      reduce size\n\nDataSource  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2192  reference\n   load (separate \u2014 training baseline or frozen snapshot)\n</code></pre>"},{"location":"data-layer/#data-sources","title":"Data sources","text":"<p>A <code>DataSource</code> loads a DataFrame and projects it to the minimal column set required by the plan. Column selection is automatic \u2014 driven by the schema's column declarations, any <code>feature_name</code> values in metric specs, and any <code>profile_cols</code> declared on the plan.</p> <pre><code>from ayn_ml.data.source import DataFrameSource, required_columns\n\nsource = DataFrameSource(df)\ndf_loaded = source.load(plan)   # projects to exactly what the plan needs\n</code></pre> <p><code>required_columns(plan)</code> computes the projected column list explicitly when you need it:</p> <pre><code>cols = required_columns(plan)  # schema cols + metric feature_names + profile_cols\n</code></pre> <p>Statistical profiling columns \u2014 to load columns beyond what metrics reference (e.g. for default statistics even without explicit <code>MetricSpec</code> entries), declare them on the plan:</p> <pre><code>plan = MonitoringPlan(\n    ...,\n    profile_cols=[\"age\", \"income\", \"tenure\"],  # loaded even if no metric targets them\n)\n</code></pre>"},{"location":"data-layer/#window-selection","title":"Window selection","text":"<p>A window strategy narrows a full DataFrame to the rows that represent the current monitoring period. Strategies are configured on the plan's <code>window</code> field so they round-trip through YAML.</p> Config type Strategy When to use <code>FullWindowConfig</code> Pass through unchanged DataFrame is already pre-filtered <code>LastNRowsWindowConfig(n)</code> Last N rows by position Chronologically ordered DataFrame <code>TimeWindowConfig(start, end)</code> Filter by timestamp range Explicit date boundaries <pre><code>from datetime import datetime, timezone\nfrom ayn_ml import MonitoringPlan, LastNRowsWindowConfig, TimeWindowConfig\n\n# last 1000 rows\nplan = MonitoringPlan(\n    ...,\n    window=LastNRowsWindowConfig(n=1_000),\n)\n\n# or with a time window\nplan = MonitoringPlan(\n    ...,\n    window=TimeWindowConfig(\n        start=datetime(2024, 6, 1, tzinfo=timezone.utc),\n        end=datetime(2024, 6, 30, tzinfo=timezone.utc),\n    ),\n)\n</code></pre> <p>The strategy implementations can also be used directly at runtime:</p> <pre><code>from ayn_ml.data.sampling import LastNRowsSampling, TimeWindowSampling\n\nwindow = LastNRowsSampling(n=1_000).sample(df, schema)\nwindow = TimeWindowSampling(start, end).sample(df, schema)\n</code></pre> <p>Both raise <code>InsufficientDataError</code> when the resulting window is empty.</p>"},{"location":"data-layer/#random-subsampling","title":"Random subsampling","text":"<p><code>RandomSamplingConfig</code> reduces the current window size for performance, applied after window selection. Exactly one of <code>n</code> or <code>frac</code> must be provided.</p> <pre><code>from ayn_ml import MonitoringPlan, RandomSamplingConfig\n\nplan = MonitoringPlan(\n    ...,\n    sampling=RandomSamplingConfig(n=1_000, seed=42),   # absolute count\n    # or:\n    # sampling=RandomSamplingConfig(frac=0.1, seed=42), # 10% of the window\n)\n</code></pre> <p>Can also be used directly:</p> <pre><code>from ayn_ml.data.sampling import RandomSampling\n\nsubsampled = RandomSampling(n=1_000, seed=42).sample(window_df, schema)\n</code></pre> <p>Raises <code>InsufficientDataError</code> when the window is empty.</p>"},{"location":"data-layer/#full-pipeline-examples","title":"Full pipeline examples","text":"<p>Separate current and reference DataFrames \u2014 pass each source independently; the reference is typically a training baseline or a frozen snapshot:</p> <pre><code>from ayn_ml import MonitoringPlan, TabularSchema, MetricSpec\nfrom ayn_ml.data.source import DataFrameSource\n\nplan = MonitoringPlan(\n    name=\"churn_monitor\",\n    model_id=\"churn_v2\",\n    model_version=\"1.0\",\n    data_schema=TabularSchema(),\n    metrics=[MetricSpec(name=\"psi\", feature_name=\"age\")],\n    profile_cols=[\"income\", \"tenure\"],\n)\n\n# load each source projected to the columns the plan needs, then pass directly\ndf_current = DataFrameSource(raw_current).load(plan)\ndf_reference = DataFrameSource(raw_reference).load(plan)\n\n# report = Runner().run(plan, current=df_current, reference=df_reference)\n</code></pre>"},{"location":"explain/","title":"Explain \u2014 SHAP Monitoring and LLM Drift Explanation","text":"<p>Status: API and class structure are defined. <code>ShapDriftMonitor.compare()</code> and <code>LlmDriftExplainer.explain()</code> are not yet implemented.</p>"},{"location":"explain/#shapdriftmonitor","title":"ShapDriftMonitor","text":"<p>Import: <code>from ayn_ml_pro.explain import ShapDriftMonitor</code></p> <p>Extra required: <code>pip install \"ayn-ml-pro[shap]\"</code></p> <p><code>ShapDriftMonitor</code> computes SHAP feature-importance values for both the current and reference windows, then applies drift tests (Wasserstein distance, PSI) to each feature's SHAP distribution to rank which features have shifted most.</p> <p>This answers a different question than standard feature drift: not \"has the input distribution of feature X changed?\" but \"has the model's reliance on feature X changed?\". A feature whose values are stable can still show SHAP drift if the model started weighting it differently.</p>"},{"location":"explain/#constructor","title":"Constructor","text":"<pre><code>ShapDriftMonitor(\n    model: Any,                  # Fitted model \u2014 sklearn, XGBoost, LightGBM, PyTorch\n    feature_names: list[str],    # Column names of the feature matrix (must match model input order)\n    max_samples: int = 500,      # Max samples per window used for SHAP computation\n)\n</code></pre> <p><code>max_samples</code> caps computation cost. For large windows, the monitor samples randomly before computing SHAP values.</p>"},{"location":"explain/#usage","title":"Usage","text":"<pre><code>from ayn_ml_pro.explain import ShapDriftMonitor\n\nmonitor = ShapDriftMonitor(\n    model=fitted_clf,\n    feature_names=[\"age\", \"income\", \"score\", \"region\"],\n    max_samples=500,\n)\n\nreport = monitor.compare(df_current, ref=df_reference)\n\nprint(report.top_drifting_features)  # ranked list of features by SHAP drift score\n</code></pre>"},{"location":"explain/#report-structure","title":"Report structure","text":"<p>The SHAP drift report exposes per-feature drift scores and a ranked list of the most drifted features:</p> <pre><code>report.top_drifting_features    # list[str] \u2014 features ranked by drift magnitude\nreport.feature_scores           # dict[str, float] \u2014 drift score per feature\nreport.method                   # str \u2014 drift test used (e.g. \"wasserstein\")\n</code></pre>"},{"location":"explain/#choosing-max_samples","title":"Choosing <code>max_samples</code>","text":"Dataset size Recommended <code>max_samples</code> &lt; 1 000 rows Use all rows (set to dataset size) 1 000 \u2013 10 000 500 (default) &gt; 10 000 300 \u2013 500 <p>Tree-based explainers (XGBoost, LightGBM, sklearn tree ensembles) are significantly faster than kernel SHAP \u2014 for those models you can safely increase <code>max_samples</code>.</p>"},{"location":"explain/#llmdriftexplainer","title":"LlmDriftExplainer","text":"<p>Import: <code>from ayn_ml_pro.explain import LlmDriftExplainer</code></p> <p>Extra required: <code>pip install \"ayn-ml-pro[llm-openai]\"</code> or <code>pip install \"ayn-ml-pro[llm-anthropic]\"</code></p> <p><code>LlmDriftExplainer</code> takes a <code>MonitoringReport</code> containing detected drift signals and produces a concise natural-language explanation of what drifted, by how much, and what the likely operational impact is.</p>"},{"location":"explain/#constructor_1","title":"Constructor","text":"<pre><code>LlmDriftExplainer(\n    provider: str,                           # \"openai\" or \"anthropic\"\n    model: str,                              # Model name \u2014 \"gpt-4o\", \"claude-opus-4\", etc.\n    api_key: str | None = None,              # Falls back to OPENAI_API_KEY / ANTHROPIC_API_KEY\n    langfuse_secret_key: str | None = None,  # Optional Langfuse tracing\n    langfuse_public_key: str | None = None,\n)\n</code></pre> <p>API keys must not be hardcoded. Use environment variables:</p> <pre><code>import os\nfrom ayn_ml_pro.explain import LlmDriftExplainer\n\nexplainer = LlmDriftExplainer(\n    provider=\"anthropic\",\n    model=\"claude-opus-4-7\",\n    # api_key falls back to os.environ[\"ANTHROPIC_API_KEY\"]\n)\n</code></pre>"},{"location":"explain/#usage_1","title":"Usage","text":"<pre><code># Run monitoring first to get a report\nreport = runner.run_once(df_current, ref=df_reference)\n\n# Generate natural-language explanation\nexplanation = explainer.explain(report)\n\nprint(explanation.summary)           # one-paragraph overview of detected drift\nprint(explanation.recommendations)  # list[str] \u2014 suggested remediation steps\n</code></pre>"},{"location":"explain/#tracing-with-langfuse","title":"Tracing with Langfuse","text":"<p>Pass Langfuse credentials to log every LLM call for observability and cost tracking:</p> <pre><code>explainer = LlmDriftExplainer(\n    provider=\"openai\",\n    model=\"gpt-4o\",\n    langfuse_secret_key=os.environ[\"LANGFUSE_SECRET_KEY\"],\n    langfuse_public_key=os.environ[\"LANGFUSE_PUBLIC_KEY\"],\n)\n</code></pre>"},{"location":"explain/#supported-providers","title":"Supported providers","text":"Provider Extra Default env var <code>\"openai\"</code> <code>llm-openai</code> <code>OPENAI_API_KEY</code> <code>\"anthropic\"</code> <code>llm-anthropic</code> <code>ANTHROPIC_API_KEY</code>"},{"location":"explain/#combining-shap-and-llm-explanation","title":"Combining SHAP and LLM explanation","text":"<p>A typical workflow runs SHAP monitoring first to rank drifted features, then feeds the standard drift report to the LLM explainer for a human-readable summary:</p> <pre><code>from ayn_ml_pro.explain import ShapDriftMonitor, LlmDriftExplainer\n\n# Step 1 \u2014 rank drifted features by SHAP importance shift\nshap_monitor = ShapDriftMonitor(model=clf, feature_names=feature_cols)\nshap_report = shap_monitor.compare(df_current, ref=df_reference)\nprint(\"Top drifting features:\", shap_report.top_drifting_features)\n\n# Step 2 \u2014 explain the monitoring report in natural language\nexplainer = LlmDriftExplainer(provider=\"anthropic\", model=\"claude-opus-4-7\")\nmonitoring_report = runner.run_once(df_current, ref=df_reference)\nexplanation = explainer.explain(monitoring_report)\nprint(explanation.summary)\n</code></pre>"},{"location":"metrics/","title":"Tabular Metrics Reference","text":"<p>All metrics are invoked via <code>get_metric(name).compute(cur, ref, schema, spec)</code> and ret a <code>MetricResult</code> with fields <code>val</code>, <code>status</code>, <code>effect_size</code>, and <code>effect_size_label</code>.</p>"},{"location":"metrics/#quick-ref","title":"Quick ref","text":""},{"location":"metrics/#performance","title":"Performance","text":"<p>| Name | What it measures | Requires ref | sklearn required | | <code>accuracy</code> | Fraction of correct predictions | No | Yes | | <code>precision</code> | TP / (TP + FP) | No | Yes | | <code>recall</code> | TP / (TP + FN) | No | Yes | | <code>f1</code> | Harmonic mean of precision and recall | No | Yes | | <code>log_loss</code> | Cross-entropy loss | No | Yes | | <code>auc</code> | Area under the ROC curve | No | Yes | | <code>aucpr</code> | Area under the precision-recall curve | No | Yes | | <code>brier</code> | Mean squared err of calibrated probabilities | No | Yes | | <code>mse</code> | Mean squared err | No | No | | <code>mae</code> | Mean absolute err | No | No | | <code>r2</code> | Coefficient of determination | No | No | | <code>mape</code> | Mean absolute percentage err | No | No |</p>"},{"location":"metrics/#drift-distribution-distance","title":"Drift \u2014 distribution distance","text":"<p>| Name | What it measures | Numeric | Categorical | Requires <code>feature_name</code> | | <code>psi</code> | Population Stability Index on a feature | Yes | Yes | Yes | | <code>wasserstein</code> | Earth Mover's Distance on a feature | Yes | No | Yes | | <code>mmd</code> | Maximum Mean Discrepancy (RBF kernel) on a feature | Yes | No | Yes | | <code>target_drift</code> | PSI on <code>schema.label_col</code> \u2014 detects shifts in P(Y) | Yes | Yes | No | | Name | What it tests | Effect size | <code>effect_size_label</code> | | <code>ks_2samp</code> | CDF equality (KS statistic) | KS D-statistic | <code>ks_statistic</code> | | <code>ttest</code> | Mean equality (Welch's by default) | Cohen's d | <code>cohen_d</code> | | <code>mannwhitney</code> | Rank distribution equality | Cliff's delta | <code>cliff_delta</code> | | <code>levene</code> | Variance equality | Variance ratio | <code>variance_ratio</code> | | <code>cramervonmises</code> | CDF equality (CvM statistic) | \u2014 | \u2014 |</p>"},{"location":"metrics/#statistics-descriptive","title":"Statistics \u2014 descriptive","text":"<p>| Name | What it computes | Requires <code>feature_name</code> | | <code>mean</code> | Arithmetic mean | Yes | | <code>median</code> | 50th percentile | Yes | | <code>std</code> | Sample standard deviation (ddof=1) | Yes | | <code>skewness</code> | Fisher's skewness | Yes | | <code>kurtosis</code> | Excess kurtosis (Fisher's, normal = 0) | Yes | | <code>quantile</code> | Arbitrary quantile | Yes | | <code>count</code> | Row count (including nulls) | Yes | | <code>top_category</code> | Most frequent category (frequency as val) | Yes |</p>"},{"location":"metrics/#performance-estimation-without-ground-truth-cbpe","title":"Performance estimation \u2014 without ground truth (CBPE)","text":"<p>| Name | What it estimates | Requires ref | sklearn required | | <code>cbpe_accuracy</code> | Estimated accuracy | Yes | Yes | | <code>cbpe_auc</code> | Estimated ROC AUC | Yes | Yes | | <code>cbpe_f1</code> | Estimated F1 score | Yes | Yes | | <code>cbpe_precision</code> | Estimated precision | Yes | Yes | | <code>cbpe_recall</code> | Estimated recall | Yes | Yes | | Name | What it measures | Requires <code>feature_name</code> | Requires ref | | <code>demographic_parity</code> | Max difference in positive prediction rates across groups | Yes | No | | <code>equalized_odds</code> | Max gap in TPR and FPR across groups | Yes | No | | <code>disparate_impact</code> | Ratio of lowest to highest positive prediction rate | Yes | No |</p>"},{"location":"metrics/#performance-metrics","title":"Performance metrics","text":"<p>All performance metrics read column names from <code>TabularSchema</code> (<code>label_col</code>, <code>prediction_col</code>, <code>proba_col</code>). They do not require a ref window \u2014 they evaluate only the cur batch. accuracy = correct predictions / total predictions Reads <code>schema.label_col</code> and <code>schema.prediction_col</code>. Delegates to <code>sklearn.metrics.accuracy_score</code>.</p>"},{"location":"metrics/#precision-recall-f1","title":"<code>precision</code>, <code>recall</code>, <code>f1</code>","text":"<p>precision = TP / (TP + FP) f1        = 2 \u00d7 precision \u00d7 recall / (precision + recall) - <code>average</code> (default <code>\"weighted\"</code>) \u2014 averaging strategy for multi-class:   <code>\"binary\"</code>, <code>\"micro\"</code>, <code>\"macro\"</code>, <code>\"weighted\"</code>, <code>\"samples\"</code>. Delegates to <code>sklearn.metrics.precision_score</code>, <code>recall_score</code>, <code>f1_score</code>. log_loss = -1/n \u00d7 \u03a3 [y log(p) + (1-y) log(1-p)] Reads <code>schema.proba_col</code>. Lower is better. Sensitive to overconfident predictions. Delegates to <code>sklearn.metrics.log_loss</code>. Area under the ROC curve. Reads <code>schema.proba_col</code>. - <code>multi_class</code> (default <code>\"raise\"</code>) \u2014 pass <code>\"ovr\"</code> or <code>\"ovo\"</code> for multi-class. Raises <code>MetricComputeError</code> if the cur batch contains only one class (ROC curve undefined). Area under the precision-recall curve. More informative than AUC on imbalanced datasets. Reads <code>schema.proba_col</code>. Raises <code>MetricComputeError</code> if the cur batch contains only one class. brier = 1/n \u00d7 \u03a3 (p_i - y_i)\u00b2 Proper scoring rule for calibration. Range [0, 1]; lower is better. Reads <code>schema.proba_col</code>. Delegates to <code>sklearn.metrics.brier_score_loss</code>. Standard regression errors. Read <code>schema.label_col</code> and <code>schema.prediction_col</code>. mse = mean((y_true - y_pred)\u00b2) mae = mean(|y_true - y_pred|)</p>"},{"location":"metrics/#r2","title":"<code>r2</code>","text":"<p>r2 = 1 - SS_res / SS_tot Range (-\u221e, 1]; 1 = perfect fit, 0 = mean-only baseline. Raises <code>MetricComputeError</code> when all <code>y_true</code> values are identical (<code>SS_tot = 0</code>; R\u00b2 is undefined in that case \u2014 not 1.0). mape = mean(|y_true - y_pred| / |y_true|) \u00d7 100 Note: MAPE is undefined when <code>y_true</code> is zero or near-zero. Rows where <code>|y_true| \u2264 eps</code> are excluded from the mean and a W is logged. - <code>eps</code> (default <code>1e-8</code>) \u2014 exclusion threshold for near-zero <code>y_true</code> values.</p>"},{"location":"metrics/#drift-distribution-distance_1","title":"Drift \u2014 distribution distance","text":"<p>All distance metrics require a ref window and operate on a single feature column (<code>spec.feature_name</code>).</p>"},{"location":"metrics/#psi-population-stability-index","title":"<code>psi</code> \u2014 Population Stability Index","text":"<p>PSI = \u03a3 (p_cur - p_ref) \u00d7 ln(p_cur / p_ref) Measures how much a feature's distribution has shifted. Supports both numeric and categorical features. Numeric features: bin edges are derived from the union of ref and cur values so no data points are silently dropped. When cur values fall outside the ref range, those bins will have <code>p_ref \u2248 0</code> after clipping \u2014 their contribution is valid but sensitive to the <code>eps</code> param. A W is emitted with the out-of-range count, the sensitivity range, and a suggestion to cross-check with <code>wasserstein</code> or <code>ks_2samp</code>. Categorical features: bins are the union of all categories seen in either window; new categories in cur receive the full treatment without out-of-range warnings. - <code>n_bins</code> (default <code>10</code>) \u2014 num of bins for numeric features. - <code>eps</code> (default <code>1e-4</code>) \u2014 zero-clipping floor applied before the log ratio.   Controls the PSI contribution of empty ref bins. Changing this by   one order of magnitude shifts the out-of-range bin contribution by   ~0.7 units \u2014 see the W msg for the exact sensitivity range. Interpretation (rule of thumb): - PSI &lt; 0.1 \u2014 stable - 0.1 \u2264 PSI &lt; 0.25 \u2014 moderate drift - PSI \u2265 0.25 \u2014 significant drift Limitation: PSI on out-of-range bins is sensitive to <code>eps</code>. When the W fires, treat the absolute PSI val with caution and validate with <code>wasserstein</code> or <code>ks_2samp</code>. PSI on <code>schema.label_col</code> \u2014 measures how much P(Y) has shifted between ref and cur windows.  Does not require <code>spec.feature_name</code>. Target type inference (override with <code>params[\"treat_as\"]</code>): - Integer or str labels \u2192 categorical PSI (class frequencies) - Float labels \u2192 numeric PSI (histogram binning) - <code>n_bins</code> (default <code>10</code>) \u2014 bins for numeric (regression) targets. - <code>eps</code> (default <code>1e-4</code> numeric, <code>1e-8</code> categorical) \u2014 zero-clipping floor. - <code>treat_as</code> (default <code>\"auto\"</code>) \u2014 <code>\"numeric\"</code> | <code>\"categorical\"</code> to override dtype inference. Same PSI interpretation thresholds as <code>psi</code>: - PSI &lt; 0.1 \u2014 stable - 0.1 \u2264 PSI &lt; 0.25 \u2014 moderate drift - PSI \u2265 0.25 \u2014 significant drift Known limitation: insensitive to symmetric concept drifts (e.g. boundary inversion on a balanced dataset leaves P(Y=1) \u2248 0.5, so PSI \u2248 0). See the CBPE notebook for scenarios B and D.</p>"},{"location":"metrics/#wasserstein-earth-movers-distance","title":"<code>wasserstein</code> \u2014 Earth Mover's Distance","text":"<p>Minimum \"work\" needed to transform the ref distribution into the cur distribution. Units are the same as the feature (e.g., \"the age distribution shifted by 3.4 years on average\"). Numeric only. No natural threshold \u2014 set one based on the feature's domain (e.g., <code>threshold=0.05</code> for a probability score, <code>threshold=5.0</code> for age in years). Delegates to <code>scipy.stats.wasserstein_distance</code>.</p>"},{"location":"metrics/#mmd-maximum-mean-discrepancy","title":"<code>mmd</code> \u2014 Maximum Mean Discrepancy","text":"<p>Kernel two-sample statistic. Detects subtle shape differences that histogram-based measures can miss (e.g., variance shifts without mean shift). Numeric only. - <code>max_samples</code> (default <code>500</code>) \u2014 caps both arrays before kernel computation   to bound O(n\u00b2) cost. - <code>random_state</code> (default <code>42</code>) \u2014 seed for reproducible subsampling. Kernel bandwidth is estimated via the median heuristic on the ref data. All tests ret the two-tailed p-value as <code>val</code>. For threshold-based alerting, set <code>spec.threshold</code> and <code>spec.upper_bound=True</code> to fire when <code>p-value &lt; threshold</code> (e.g., <code>threshold=0.05</code>). All tests require a ref window and <code>spec.feature_name</code>. Effect sizes are attached to <code>MetricResult.effect_size</code> alongside a label in <code>MetricResult.effect_size_label</code> that identifies the scale. This allows downstream code to interpret the num correctly without knowing which metric produced it. | Metric | <code>effect_size_label</code> | Scale | Rule of thumb | | <code>ks_2samp</code> | <code>ks_statistic</code> | [0, 1] | No universal rule; &gt; 0.1 often notable | | <code>ttest</code> | <code>cohen_d</code> | (\u2212\u221e, +\u221e), sign = direction | &lt; 0.2 small \u00b7 &lt; 0.5 medium \u00b7 \u2265 0.8 large | | <code>mannwhitney</code> | <code>cliff_delta</code> | [\u22121, 1] | &lt; 0.11 small \u00b7 &lt; 0.28 medium \u00b7 \u2265 0.43 large | | <code>levene</code> | <code>variance_ratio</code> | (0, +\u221e) | 1.0 = no change \u00b7 &gt; 1.5 or &lt; 0.67 notable | | <code>cramervonmises</code> | \u2014 | \u2014 | No effect size | Effect size and p-value complement each other: - Low p-value + small effect = statistically significant but operationally   negligible (common with large n). Do not alert. - Large effect + high p-value = practically meaningful shift but too few   observations to confirm statistically. Investigate the sample. - Both low p-value and large effect = genuine drift.</p>"},{"location":"metrics/#ks_2samp-kolmogorov-smirnov-two-sample-test","title":"<code>ks_2samp</code> \u2014 Kolmogorov-Smirnov two-sample test","text":"<p>Tests whether two samples come from the same continuous distribution. Sensitive to location, scale, and shape differences. <code>effect_size</code> = KS D-statistic = max absolute difference between the two empirical CDFs. Range [0, 1]. Delegates to <code>scipy.stats.ks_2samp</code>.</p>"},{"location":"metrics/#ttest-welchs-t-test-default-students-t-test","title":"<code>ttest</code> \u2014 Welch's t-test (default) / Student's t-test","text":"<p>Parametric test for equality of means. Assumes approximately normal data. Default is Welch's (<code>equal_var=False</code>) \u2014 safe when the two groups have different variances. Student's t-test (<code>equal_var=True</code>) is slightly more powerful when variances are provably equal, but biased otherwise. - <code>equal_var</code> (default <code>False</code>) \u2014 set to <code>True</code> for Student's t-test. <code>effect_size</code> = Cohen's d using the df-weighted pooled standard deviation: pooled_std = sqrt(((n1-1)\u00d7s1\u00b2 + (n2-1)\u00d7s2\u00b2) / (n1+n2-2)) cohen_d    = (mean_ref - mean_cur) / pooled_std Sign convention: positive d means the ref mean is higher than cur. Delegates to <code>scipy.stats.ttest_ind</code>.</p>"},{"location":"metrics/#mannwhitney-mann-whitney-u-test","title":"<code>mannwhitney</code> \u2014 Mann-Whitney U test","text":"<p>Non-parametric alternative to the t-test. Compares rank distributions; robust to non-normality and outliers. Prefer over <code>ttest</code> when <code>|skewness| &gt; 1.0</code> or for ordinal data. - <code>alternative</code> (default <code>\"two-sided\"</code>) \u2014 <code>\"less\"</code> or <code>\"greater\"</code> for <code>effect_size</code> = Cliff's delta: cliff_delta = 2U / (n_ref \u00d7 n_cur) \u2212 1 Range [\u22121, 1]. Positive = ref tends to be larger than cur. Delegates to <code>scipy.stats.mannwhitneyu</code>.</p>"},{"location":"metrics/#levene-levenes-test-for-variance-equality","title":"<code>levene</code> \u2014 Levene's test for variance equality","text":"<p>Tests whether variances (scale) have changed between ref and cur. Less sensitive to departures from normality than Bartlett's test. <code>effect_size</code> = variance ratio: variance_ratio = var_cur / var_ref   (ddof=1) Values above 1 mean cur has more spread; below 1 means less. <code>None</code> when <code>var_ref = 0</code> (constant ref). Delegates to <code>scipy.stats.levene</code>.</p>"},{"location":"metrics/#cramervonmises-cramer-von-mises-two-sample-test","title":"<code>cramervonmises</code> \u2014 Cram\u00e9r-von Mises two-sample test","text":"<p>Tests CDF equality by integrating squared CDF differences \u2014 more powerful than <code>ks_2samp</code> for sustained shifts across the full distribution (e.g., a location shift without a spike). No effect size. Delegates to <code>scipy.stats.cramervonmises_2samp</code>.</p>"},{"location":"metrics/#statistics-descriptive_1","title":"Statistics \u2014 descriptive","text":"<p>Descriptive metrics do not require a ref window. They operate on a single feature column (<code>spec.feature_name</code> is mandatory for all). Standard summary stats. <code>std</code> uses <code>ddof=1</code> (sample standard deviation). Fisher's skewness (third standardised moment). 0 = symmetric; &gt; 0 = right-tailed; &lt; 0 = left-tailed. Rule of thumb: <code>|skewness| &gt; 1</code> \u2192 prefer <code>mannwhitney</code> over <code>ttest</code> for drift detection on this feature. Excess kurtosis (Fisher's definition; normal distribution = 0). High positive values indicate heavy tails. Rule of thumb: <code>excess_kurtosis &gt; 7</code> \u2192 prefer <code>cramervonmises</code> over <code>ks_2samp</code>. - <code>q</code> (default <code>0.5</code>) \u2014 quantile probability in (0, 1). <code>q=0.5</code> returns   the median; <code>q=0.95</code> returns the 95th percentile. Total row count of the feature column, including null values. Returns <code>0</code> on an empty batch (no err).</p>"},{"location":"metrics/#top_category","title":"<code>top_category</code>","text":"<p>Most frequent category. <code>val</code> is the frequency (proportion in [0, 1]) of the top category, not the category label itself. Tie-breaking: when two categories share the same count, the lexicographically earliest one (alphabetically first) is returned. This is deterministic but arbitrary \u2014 do not rely on it for business logic.</p>"},{"location":"metrics/#performance-estimation-without-ground-truth-cbpe_1","title":"Performance estimation \u2014 without ground truth (CBPE)","text":"<p>Confidence-Based Performance Estimation estimates classification metrics on unlabelled windows.  No ground-truth labels are needed on the cur window \u2014 the ref window (which has labels) is used only to fit a probability Core idea: if a model assigns p\u0302 = 0.9 to an observation, there is a 90% chance that observation is correctly classified.  Summing these fractional certainties reconstructs an estimated confusion matrix and derived metrics. Reference: Vandewiele et al., NannyML (2022).</p>"},{"location":"metrics/#schema-requirements","title":"Schema requirements","text":"<ul> <li><code>TabularSchema.proba_col</code> \u2014 must be set and present on both windows.</li> <li><code>TabularSchema.label_col</code> \u2014 must be present on the ref window only.</li> <li><code>TabularSchema.prediction_col</code> \u2014 required for <code>cbpe_accuracy</code>, <code>cbpe_f1</code>,   <code>cbpe_precision</code>, <code>cbpe_recall</code>; not used by <code>cbpe_auc</code>. Both windows require at least 100 rows. The calibrator needs enough ref data to fit reliably; estimates on small windows are noisy. estimated_accuracy = (\u03a3 p\u0302\u1d62 [y_pred=1]  +  \u03a3 (1\u2212p\u0302\u1d62) [y_pred=0]) / N Element-wise weighted sum of fractional correct predictions divided by total rows. Each row contributes <code>p\u0302\u1d62</code> if the model predicted positive (probability it is correct) or <code>1\u2212p\u0302\u1d62</code> if predicted negative. Estimated ROC AUC via cumulative sum over calibrated probabilities sorted descending.  O(n log n). Raises <code>MetricComputeError</code> if estimated positive or negative mass is near zero (single-class degenerate case).</li> </ul>"},{"location":"metrics/#cbpe_f1-cbpe_precision-cbpe_recall","title":"<code>cbpe_f1</code>, <code>cbpe_precision</code>, <code>cbpe_recall</code>","text":"<p>Estimated from a fractional confusion matrix: estimated_TP = \u03a3 p\u0302_i  for i where y_pred = 1 estimated_FP = \u03a3 (1 - p\u0302_i)  for i where y_pred = 1 estimated_FN = \u03a3 (1 - p\u0302_i)  for i where y_pred = 0 | param | default | effect | | <code>calibrate</code> | <code>True</code> | Fit isotonic regression calibrator on ref before estimating. Set to <code>False</code> to use raw probabilities (faster but less accurate if the model is miscalibrated). | - Concept drift blindness: if the relationship P(Y|X) changes but the   model's probabilities do not reflect it, CBPE will report stable performance.   Always pair with drift metrics (<code>psi</code>, <code>wasserstein</code>) to detect covariate - Calibration assumption: CBPE assumes the model is well-calibrated (after   isotonic correction on the ref). A badly miscalibrated model will   produce unreliable estimates even with <code>calibrate=True</code>. - Binary only: all five metrics assume binary classification.</p>"},{"location":"metrics/#fairness-metrics","title":"Fairness metrics","text":"<p>Fairness metrics measure group-level disparity in model predictions.  They operate on the cur window only (no ref required) and identify the protected attr via <code>spec.feature_name</code>. Declare sensitive attributes in <code>TabularSchema.protected_cols</code> to enable early validation (the Runner rejects an unknown column before any metric schema = TabularSchema(     label_col=\"y_true\",     prediction_col=\"y_pred\",     protected_cols=[\"gender\", \"age_group\"], When <code>protected_cols</code> is <code>None</code>, the declaration check is skipped but the column must still exist in the DataFrame.</p>"},{"location":"metrics/#using-specfeature_name","title":"Using <code>spec.feature_name</code>","text":"<p>Each fairness <code>MetricSpec</code> names the protected column via <code>feature_name</code>, exactly as drift and stats metrics do: MetricSpec(name=\"demographic_parity\", feature_name=\"gender\", threshold=0.1) MetricSpec(name=\"equalized_odds\",     feature_name=\"gender\", threshold=0.1) MetricSpec(name=\"disparate_impact\",   feature_name=\"gender\",            threshold=0.8, upper_bound=False)</p>"},{"location":"metrics/#demographic_parity-demographic-parity-difference","title":"<code>demographic_parity</code> \u2014 Demographic Parity Difference","text":"<p>DPD = max_g P(\u0176=1 | A=g) \u2212 min_g P(\u0176=1 | A=g) Maximum difference in positive prediction rates between any two groups. Range [0, 1].  Lower is better.  A val of 0 means perfect parity. Threshold guidance: values above 0.1 are worth investigating; above 0.2 indicate substantial disparity. Uses <code>schema.prediction_col</code> only \u2014 no ground-truth labels required.</p>"},{"location":"metrics/#equalized_odds-equalized-odds-difference","title":"<code>equalized_odds</code> \u2014 Equalized Odds Difference","text":"<p>EOD = max(max_g TPR_g \u2212 min_g TPR_g,  max_g FPR_g \u2212 min_g FPR_g) Takes the larger of the TPR gap and FPR gap across groups.  Range [0, 1]. Lower is better.  A val of 0 means all groups have identical true positive and false positive rates. Groups with no positive (or no negative) labels in the cur window contribute <code>nan</code> to their TPR (or FPR) and are excluded from the gap via <code>nanmax</code> / <code>nanmin</code>. Uses both <code>schema.label_col</code> and <code>schema.prediction_col</code>. Threshold guidance: same as demographic parity \u2014 0.1 as a soft limit.</p>"},{"location":"metrics/#disparate_impact-disparate-impact-ratio","title":"<code>disparate_impact</code> \u2014 Disparate Impact Ratio","text":"<p>DIR = min_g P(\u0176=1 | A=g) / max_g P(\u0176=1 | A=g) Ratio of the lowest to the highest positive prediction rate.  Range [0, 1]. Higher is better (closer to 1.0 = no disparity). 80% rule (EEOC guideline): a DIR below 0.8 is considered potentially discriminatory.  To apply it:     name=\"disparate_impact\",     feature_name=\"gender\",     threshold=0.8,     upper_bound=False,   # passes when val &gt;= threshold Returns 1.0 (no disparity) when all groups have a near-zero positive prediction rate to avoid division-by-zero.</p>"},{"location":"metrics/#nlp-llm-planned-pip-install-ayn-mlnlp","title":"NLP / LLM (planned \u2014 pip install ayn-ml[nlp])","text":"<p><code>bleu</code> <code>rouge1</code> <code>rouge2</code> <code>rougeL</code> <code>bert_score</code> <code>exact_match</code> <code>embedding_drift</code></p>"},{"location":"metrics/#premium-metrics-ayn-ml-pro","title":"Premium Metrics \u2014 <code>ayn-ml-pro</code>","text":"<p>Status: All premium metrics are registered automatically when <code>ayn_ml_pro.metrics</code> is imported. Individual metric implementations are not yet complete \u2014 registration placeholders are in place.</p> <p>Premium metrics are registered into the <code>ayn-ml</code> metric registry on import. Once registered, use them via <code>MetricSpec</code> exactly like any built-in metric.</p> <pre><code>import ayn_ml_pro.metrics  # registers all premium metrics\nfrom ayn_ml.core.spec import MetricSpec, MonitoringPlan\n\nplan = MonitoringPlan(\n    name=\"my_monitor\",\n    metrics=[MetricSpec(name=\"toxicity\"), MetricSpec(name=\"llm_faithfulness\")],\n)\n</code></pre>"},{"location":"metrics/#safety-metrics","title":"Safety metrics","text":"<p>Module: <code>ayn_ml_pro.metrics.safety</code></p> <p>Extra required: <code>pip install \"ayn-ml-pro[llm-openai]\"</code> or <code>pip install \"ayn-ml-pro[llm-anthropic]\"</code></p> Metric name Description <code>toxicity</code> Fraction of model outputs classified as toxic by a safety model <code>hallucination_rate</code> Fraction of outputs flagged as hallucinations against a grounding corpus"},{"location":"metrics/#llm-judge-metrics","title":"LLM-judge metrics","text":"<p>Module: <code>ayn_ml_pro.metrics.llm_judge</code></p> <p>Extra required: <code>pip install \"ayn-ml-pro[llm-openai]\"</code> or <code>pip install \"ayn-ml-pro[llm-anthropic]\"</code></p> <p>Scores model outputs using a language model judge. Suitable for RAG pipelines, chatbots, and generative models where traditional metrics are not applicable.</p> Metric name Description <code>llm_relevance</code> Relevance of the model output to the input prompt, scored by an LLM judge <code>llm_faithfulness</code> Factual faithfulness of the output to a grounding context, scored by an LLM judge <code>llm_coherence</code> Linguistic coherence and fluency score"},{"location":"metrics/#agent-evaluation-metrics","title":"Agent evaluation metrics","text":"<p>Module: <code>ayn_ml_pro.metrics.agent</code></p> <p>Evaluates agentic systems where the model takes sequences of actions (tool calls, reasoning steps) to complete a task.</p> <p><code>task_completion_rate</code>, <code>tool_success_rate</code>, <code>step_count</code>, <code>avg_tokens</code>, <code>avg_latency_ms</code>, and <code>avg_cost_usd</code> are registered by the <code>ayn-ml</code> package \u2014 use them directly without importing <code>ayn_ml_pro.metrics</code>.</p> Metric name Description Ground truth required <code>step_efficiency</code> Ratio of actual steps to optimal step count Yes (ground-truth traces) <code>goal_adherence</code> LLM-judged score for how closely the agent followed the stated goal No"},{"location":"metrics/#llm-metric-timeouts","title":"LLM metric timeouts","text":"<p>LLM-backed metrics (safety, judge, agent <code>goal_adherence</code>) involve external API calls and may be slow. Set a timeout via <code>MetricSpec.params</code>:</p> <pre><code>MetricSpec(name=\"llm_faithfulness\", params={\"timeout\": 30})  # 30 seconds\n</code></pre> <p>If the call exceeds the timeout, the metric result is recorded as an error in <code>MonitoringReport.errors</code> \u2014 the run continues for all other metrics.</p>"},{"location":"pro/","title":"ayn-ml Pro","text":"<p>ayn-ml Pro is a commercial extension of ayn-ml for teams that need advanced monitoring capabilities in production. It depends on <code>ayn-ml</code> and implements its public protocols \u2014 no Apache 2.0 code is duplicated.</p>"},{"location":"pro/#advanced-advisor","title":"Advanced Advisor","text":"<p>The built-in <code>MetricDesigner</code> generates monitoring plans automatically from your data schema and task type. ayn-ml Pro unlocks <code>standard</code> and <code>comprehensive</code> advisor profiles: deeper statistical analysis, broader drift coverage, and risk-weighted metric selection calibrated to your model type and data volume.</p>"},{"location":"pro/#cloud-runner","title":"Cloud Runner","text":"<p>The open-source runner executes monitoring plans in a single synchronous call. Pro's Cloud Runner adds cron-based scheduling, parallel metric execution, configurable retry logic, and a full audit trail \u2014 built for teams running monitoring inside CI/CD pipelines or production batch jobs.</p>"},{"location":"pro/#slack-notifications","title":"Slack Notifications","text":"<p>Pro adds a Slack sink: when drift or performance degradation is detected, a formatted monitoring summary is posted directly to the channel of your choice, with per-metric status and threshold breach details.</p>"},{"location":"pro/#s3-storage","title":"S3 Storage","text":"<p>Pro's <code>S3Store</code> persists monitoring history to any S3-compatible object store (AWS S3, MinIO, GCS via S3 adapter), replacing the default SQLite store for distributed or cloud-native deployments.</p>"},{"location":"pro/#llm-safety-and-quality-metrics","title":"LLM Safety and Quality Metrics","text":"<p>For teams monitoring generative AI outputs, Pro adds:</p> <ul> <li>Safety metrics \u2014 toxicity detection and hallucination rate, scored by a model-based classifier</li> <li>LLM-judge quality metrics \u2014 relevance, faithfulness, and coherence, scored by a configurable   LLM judge (OpenAI or Anthropic)</li> </ul> <p>These complement the deterministic NLP metrics (BLEU, ROUGE, BERTScore, embedding drift) available in the open-source package.</p>"},{"location":"pro/#agent-metrics","title":"Agent Metrics","text":"<p>For multi-step agentic workflows, Pro tracks reasoning quality beyond raw outputs: step efficiency against optimal traces, goal adherence scored by an LLM judge, and cost and latency breakdowns per tool call.</p>"},{"location":"pro/#explainability","title":"Explainability","text":"<p>Pro extends the open-source <code>DriftAttributor</code> with:</p> <ul> <li>SHAP-based drift monitoring \u2014 track feature importance shifts over time alongside distribution drift</li> <li>LLM drift explanation \u2014 natural-language summaries of what drove a drift alert, generated by a judge model</li> <li>Experimental XAI modules \u2014 post-hoc analysis tools for deep-dive investigation of model behaviour changes</li> </ul>"},{"location":"pro/#analytics-dashboard","title":"Analytics Dashboard","text":"<p>A hosted dashboard for teams managing multiple models and monitoring plans: historical trend charts, per-feature drill-down, multi-team access control (RBAC), and SSO integration.</p> <p>ayn-ml Pro is a commercial product by Kevek ML.</p>"},{"location":"quickstart/","title":"Quick Start","text":"<p>This page walks you through the three main entry points to ayn-ml: building a plan manually, loading one from YAML, and generating one automatically with <code>MetricDesigner</code>.</p>"},{"location":"quickstart/#installation","title":"Installation","text":"<pre><code>pip install ayn-ml\n</code></pre> <p>For production workloads with Polars:</p> <pre><code>pip install ayn-ml[polars]\n</code></pre>"},{"location":"quickstart/#1-define-a-monitoring-plan","title":"1 \u2014 Define a monitoring plan","text":"<p>A <code>MonitoringPlan</code> binds a data schema, model identity, and the list of metrics to compute. Plans are immutable Pydantic models that round-trip through YAML.</p> <pre><code>from ayn_ml import MonitoringPlan, TabularSchema, MetricSpec\n\nschema = TabularSchema(\n    label_col=\"y_true\",\n    prediction_col=\"y_pred\",\n    proba_col=\"y_prob\",\n    feature_types={\"tenure\": \"categorical\"},   # override int-encoded columns\n)\n\nplan = MonitoringPlan(\n    name=\"churn_model_monitoring\",\n    model_id=\"churn_model\",\n    model_version=\"2.1\",\n    data_schema=schema,\n    metrics=[\n        MetricSpec(name=\"accuracy\", threshold=0.85, upper_bound=False),\n        MetricSpec(name=\"psi\", feature_name=\"age\", threshold=0.2),\n        MetricSpec(name=\"f1\", threshold=0.80, upper_bound=False),\n    ],\n)\n</code></pre>"},{"location":"quickstart/#2-declarative-yaml-config-roadmap","title":"2 \u2014 Declarative YAML config (roadmap)","text":"<p><code>MonitoringPlan</code> is a Pydantic model \u2014 YAML round-trip (<code>from_yaml</code> / <code>to_yaml</code>) is on the roadmap. The schema below shows the planned config format for reference:</p> <pre><code># churn_plan.yaml \u2014 planned declarative config format\nname: churn_model_monitoring\nmodel_id: churn_model\nmodel_version: \"2.1\"\ndata_schema:\n  type: tabular\n  label_col: y_true\n  prediction_col: y_pred\nmetrics:\n  - name: accuracy\n    threshold: 0.85\n    upper_bound: false\n  - name: psi\n    feature_name: age\n    threshold: 0.2\n</code></pre> <p>Until YAML I/O ships, build the plan in Python as shown in step 1 above. <code>MonitoringPlan</code> is a standard Pydantic model, so <code>plan.model_dump()</code> and <code>MonitoringPlan.model_validate(d)</code> work today for dict-based serialisation.</p>"},{"location":"quickstart/#3-run-and-inspect-results","title":"3 \u2014 Run and inspect results","text":"<pre><code>from ayn_ml.runner import Runner\nfrom ayn_ml.stores import InMemoryStore\n\nstore  = InMemoryStore()\nreport = Runner().run(plan, current=df_current, reference=df_reference, store=store)\n\nprint(report.to_dataframe())\n#    metric_name  metric_type  value   status  threshold\n# 0     accuracy  performance   0.82    False       0.85\n# 1          psi        drift   0.31    False       0.20\n# 2           f1  performance   0.79    False       0.80\n</code></pre>"},{"location":"quickstart/#4-let-metricdesigner-build-the-plan","title":"4 \u2014 Let MetricDesigner build the plan","text":"<p>Don't know which drift test to use for a skewed feature, or whether F1 or accuracy is right for an imbalanced dataset? <code>MetricDesigner</code> answers both from the data itself.</p> <pre><code>from ayn_ml.advisor import MetricDesigner\nfrom ayn_ml.core.schema import TabularSchema\n\nschema   = TabularSchema(label_col=\"y_true\", prediction_col=\"y_pred\", proba_col=\"y_prob\")\ndesigner = MetricDesigner(schema)\n\nresult = designer.suggest(\n    df_current,\n    reference=df_reference,   # required \u2014 training baseline or historical window\n    task_type=\"classification\",\n    name=\"fraud_monitor\",\n    model_id=\"fraud_v2\",\n    model_version=\"1.0\",\n)\n\nplan  = result.plan      # MonitoringPlan \u2014 pass directly to Runner\nfor w in result.warnings:\n    print(\"Advisory:\", w)\n# Advisory: accuracy demoted: imbalance ratio 7.3:1\n# Advisory: levene added for 'income': variance_ratio=1.91\n</code></pre> <p>The advisor inspects every feature column independently:</p> <ul> <li>Numeric \u2192 normality test (Shapiro-Wilk / D'Agostino / skewness heuristic) \u2192 t-test or   Mann-Whitney U + CvM + Wasserstein + PSI; Levene added when variance shifts</li> <li>Categorical \u2192 PSI + chi-square</li> <li>Performance \u2192 accuracy demoted or excluded when class imbalance is detected; F1 + AUCPR promoted</li> <li>Sample-size guards \u2192 Wasserstein only for n &lt; 30; PSI + Wasserstein only for n &gt; 50 000</li> </ul> <p>\u2192 Full advisor reference</p>"},{"location":"quickstart/#5-works-with-pandas-and-polars","title":"5 \u2014 Works with pandas and Polars","text":"<pre><code>import pandas as pd\nimport polars as pl\nfrom ayn_ml.runner import Runner\n\n# Both work \u2014 narwhals handles the backend\nreport_pd = Runner().run(plan, current=df_pandas,  reference=df_ref_pandas)\nreport_pl = Runner().run(plan, current=df_polars,  reference=df_ref_polars)\n</code></pre>"},{"location":"quickstart/#6-persist-results","title":"6 \u2014 Persist results","text":"<pre><code>from ayn_ml.stores import SqliteStore\n\nstore  = SqliteStore(\"monitoring.db\")\nreport = Runner().run(plan, current=df_current, reference=df_reference, store=store)\n\n# Read back the history as flat rows\nrows = store.read_history(\"churn_model\", limit=30)\n</code></pre> <p>For cloud deployments, an S3-compatible store is available in the commercial edition.</p>"},{"location":"quickstart/#7-add-custom-metrics","title":"7 \u2014 Add custom metrics","text":"<pre><code>from typing import Any\nfrom ayn_ml.metrics import register_metric\nfrom ayn_ml.core.result import MetricResult\nfrom ayn_ml.core.schema import DataSchema\nfrom ayn_ml.core.spec import MetricSpec, MetricType\n\n@register_metric(\"business_accuracy\")\nclass BusinessAccuracyMetric:\n    \"\"\"Accuracy weighted by business cost per error type.\"\"\"\n\n    name = \"business_accuracy\"\n    metric_type = MetricType.custom\n    requires_reference = False\n\n    def compute(\n        self,\n        current: Any,\n        reference: Any | None,\n        schema: DataSchema,\n        spec: MetricSpec,\n    ) -&gt; MetricResult:\n        ...\n</code></pre>"},{"location":"quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Schemas reference \u2014 <code>TabularSchema</code>, <code>TextSchema</code>, <code>AgentSchema</code></li> <li>Metrics reference \u2014 all 38+ built-in metrics</li> <li>Metric Advisor \u2014 routing rules, imbalance handling, API reference</li> <li>Data Layer \u2014 <code>DataSource</code>, <code>SamplingStrategy</code>, <code>DataPartitioner</code></li> <li>Stores \u2014 <code>InMemoryStore</code>, <code>SqliteStore</code></li> <li>Architecture \u2014 full design overview</li> </ul>"},{"location":"runner/","title":"Runner \u2014 CloudRunner Scheduling","text":"<p>Status: API and class structure are defined. <code>run_once()</code>, <code>start()</code>, <code>stop()</code>, and <code>audit_log()</code> are not yet implemented.</p>"},{"location":"runner/#cloudrunner","title":"CloudRunner","text":"<p>Import: <code>from ayn_ml_pro.runner import CloudRunner</code></p> <p><code>CloudRunner</code> extends the synchronous <code>Runner</code> from <code>ayn-ml</code> (Apache 2.0) with scheduling, parallelism, retry logic, and an immutable audit trail. It is the recommended execution layer for production deployments.</p>"},{"location":"runner/#feature-overview","title":"Feature overview","text":"Feature Description Scheduling Cron expressions (<code>\"0 * * * *\"</code>) or shorthands (<code>@hourly</code>, <code>@daily</code>) Parallelism Concurrent metric computation across feature columns (<code>max_workers</code>) Retry logic Configurable exponential back-off on transient failures Audit trail Append-only log of every run: ID, timestamps, status, errors"},{"location":"runner/#constructor","title":"Constructor","text":"<pre><code>CloudRunner(\n    plan: MonitoringPlan,\n    store: ResultStore,\n    sinks: list[ResultSink] | None = None,\n    schedule: str | None = None,        # Cron expr or @shorthand\n    max_workers: int = 4,               # Parallel workers\n    max_retries: int = 3,               # Max retry attempts per run\n    retry_backoff: float = 2.0,         # Base back-off in seconds (exponential)\n)\n</code></pre>"},{"location":"runner/#schedule-syntax","title":"Schedule syntax","text":"Expression Meaning <code>\"0 * * * *\"</code> Every hour <code>\"0 8 * * *\"</code> Daily at 08:00 <code>\"@hourly\"</code> Shorthand for every hour <code>\"@daily\"</code> Shorthand for daily at midnight <code>None</code> No automatic scheduling \u2014 use <code>run_once()</code> manually"},{"location":"runner/#usage","title":"Usage","text":""},{"location":"runner/#scheduled-execution","title":"Scheduled execution","text":"<pre><code>from ayn_ml_pro.runner import CloudRunner\nfrom ayn_ml_pro.stores import S3Store\nfrom ayn_ml_pro.sinks import SlackChannel\nimport os\n\nstore = S3Store(bucket=\"my-bucket\", region_name=\"eu-west-1\")\nsink = SlackChannel(token=os.environ[\"SLACK_BOT_TOKEN\"], channel=\"#ml-alerts\")\n\nrunner = CloudRunner(\n    plan,\n    store=store,\n    sinks=[sink],\n    schedule=\"0 * * * *\",   # hourly\n    max_workers=8,\n    max_retries=3,\n    retry_backoff=2.0,\n)\n\nrunner.start()   # blocks until runner.stop() is called\n</code></pre>"},{"location":"runner/#one-shot-execution","title":"One-shot execution","text":"<pre><code>runner = CloudRunner(plan, store=store)\nreport = runner.run_once(df_current, ref=df_reference)\nprint(report.summary)\n</code></pre>"},{"location":"runner/#stopping-the-runner","title":"Stopping the runner","text":"<pre><code>import signal\n\ndef handle_sigterm(sig, frame):\n    runner.stop()   # waits for any in-flight run to complete\n\nsignal.signal(signal.SIGTERM, handle_sigterm)\nrunner.start()\n</code></pre>"},{"location":"runner/#audit-trail","title":"Audit trail","text":"<p><code>audit_log()</code> returns an immutable list of run records. Each record contains:</p> Field Type Description <code>run_id</code> <code>str</code> Unique identifier for the run <code>started_at</code> <code>str</code> ISO 8601 timestamp <code>finished_at</code> <code>str</code> ISO 8601 timestamp <code>status</code> <code>str</code> <code>\"success\"</code> or <code>\"error\"</code> <code>err</code> <code>str \\| None</code> Error message if <code>status == \"error\"</code> <pre><code>for record in runner.audit_log():\n    print(record[\"run_id\"], record[\"status\"], record.get(\"err\"))\n</code></pre>"},{"location":"runner/#error-handling","title":"Error handling","text":"<p><code>CloudRunner</code> catches per-metric exceptions and stores them in <code>MonitoringReport.errors</code> \u2014 a single metric failure never aborts the run. If a run fails after all retries, <code>CloudRunnerError</code> is raised and recorded in the audit trail.</p>"},{"location":"runner/#exception-reference","title":"Exception reference","text":"Exception When raised <code>CloudRunnerError</code> Run fails after all retry attempts, or the scheduler cannot start"},{"location":"schemas/","title":"Data schemas","text":"<p>A schema maps logical column roles to physical column names in your DataFrame. ayn-ml supports three modalities \u2014 pick the one that matches your model type.</p>"},{"location":"schemas/#tabularschema","title":"TabularSchema","text":"<p>For supervised ML models (classification and regression).</p> Field Default Description <code>label_col</code> <code>\"y_true\"</code> Ground-truth labels <code>prediction_col</code> <code>\"y_pred\"</code> Model predictions <code>proba_col</code> <code>\"y_pred_proba\"</code> Predicted probabilities (set to <code>None</code> to disable) <code>feature_types</code> <code>{}</code> Maps column names to <code>\"numeric\"</code> or <code>\"categorical\"</code> \u2014 required to correctly handle integer-encoded categoricals <code>timestamp_col</code> <code>None</code> Observation timestamp. When set, the Runner derives <code>period_start</code>/<code>period_end</code> from min/max values. Required when <code>plan.window.type == \"time_window\"</code>. <code>model_id_col</code> <code>None</code> Column identifying the model per row. When set, the Runner filters rows to <code>MonitoringPlan.model_id</code>. <code>None</code> skips filtering. <code>model_version_col</code> <code>None</code> Column identifying the model version per row. Same semantics as <code>model_id_col</code>. <p>Declare only what exists in your DataFrame. All three optional columns default to <code>None</code>. The Runner validates at runtime that every configured column is present and raises <code>SchemaError</code> if it is missing (configurable via <code>Runner(strict=False)</code>).</p> <p>Infer types from a DataFrame:</p> <pre><code>schema = TabularSchema.from_dataframe(\n    df_reference,\n    feature_types={\"region\": \"categorical\"},  # correct int-encoded categoricals\n    label_col=\"y_true\",\n    prediction_col=\"y_pred\",\n    model_id_col=None,        # not in the data \u2014 falls back to MonitoringPlan.model_id\n    model_version_col=None,\n)\n</code></pre> <p>Schema columns (<code>label_col</code>, <code>prediction_col</code>, etc.) are automatically excluded from type inference \u2014 only feature columns appear in <code>feature_types</code>.</p>"},{"location":"schemas/#textschema","title":"TextSchema","text":"<p>For NLP / LLM text-in / text-out models.</p> Field Default Description <code>input_col</code> <code>\"input_text\"</code> Model input text <code>output_col</code> <code>\"output_text\"</code> Model output text <code>reference_col</code> <code>\"reference_text\"</code> Expected output for quality metrics (ROUGE, BLEU); set to <code>None</code> for unsupervised mode <code>embedding_col</code> <code>None</code> Pre-computed embeddings for semantic similarity metrics"},{"location":"schemas/#agentschema","title":"AgentSchema","text":"<p>For AI agent traces (tool-calling, multi-step reasoning).</p> Field Default Description <code>trace_col</code> <code>\"trace\"</code> Full execution trace <code>success_col</code> <code>\"success\"</code> Boolean task-completion flag <code>tool_calls_col</code> <code>\"tool_calls\"</code> Serialized tool-call records <code>tokens_used_col</code> <code>\"tokens_used\"</code> Total token consumption per run <code>latency_col</code> <code>\"latency_ms\"</code> End-to-end latency in milliseconds <code>cost_col</code> <code>\"cost_usd\"</code> Cost in USD per run <p>All optional fields default to <code>None</code> when not present in the data.</p>"},{"location":"sinks/","title":"Sinks \u2014 Slack Notifications","text":"<p>Status: API and class structure are defined. <code>SlackChannel.send()</code> is not yet implemented.</p>"},{"location":"sinks/#slackchannel","title":"SlackChannel","text":"<p>Import: <code>from ayn_ml_pro.sinks import SlackChannel</code></p> <p>Extra required: <code>pip install \"ayn-ml-pro[slack]\"</code></p> <p>Stub in <code>ayn-ml</code>: <code>ayn_ml.sinks</code> exports <code>SlackChannel: type[Any] | None = None</code>. This stub is populated by <code>ayn_ml_pro._extension</code> when <code>ayn-ml-pro</code> is installed; it is <code>None</code> otherwise. Import from <code>ayn_ml_pro.sinks</code> directly in all production code.</p> <p><code>SlackChannel</code> implements the <code>ResultSink</code> protocol from <code>ayn-ml</code> (Apache 2.0). Pass it to any <code>Runner</code> and it will post a summary of each <code>MonitoringReport</code> to a Slack channel via the Slack Web API.</p>"},{"location":"sinks/#constructor","title":"Constructor","text":"<pre><code>SlackChannel(\n    token: str,           # Slack bot token \u2014 xoxb-...\n    channel: str,         # Channel name or ID \u2014 \"#ml-alerts\" or \"C012AB3CD\"\n    mention: str | None,  # Optional mention prepended to each alert \u2014 \"@here\" or \"&lt;@U123456&gt;\"\n)\n</code></pre> <p>Credentials must not be hardcoded. Use an environment variable:</p> <pre><code>import os\nfrom ayn_ml_pro.sinks import SlackChannel\n\nsink = SlackChannel(\n    token=os.environ[\"SLACK_BOT_TOKEN\"],\n    channel=\"#ml-alerts\",\n    mention=\"@here\",\n)\n</code></pre>"},{"location":"sinks/#wiring-to-a-runner","title":"Wiring to a Runner","text":"<pre><code>from ayn_ml.runner import Runner\nfrom ayn_ml_pro.sinks import SlackChannel\nfrom ayn_ml_pro.stores import S3Store\n\nstore = S3Store(bucket=\"my-monitoring-bucket\")\nsink = SlackChannel(token=os.environ[\"SLACK_BOT_TOKEN\"], channel=\"#ml-alerts\")\n\nrunner = Runner(plan, store=store, sinks=[sink])\nrunner.run(df_current, ref=df_reference)\n</code></pre>"},{"location":"sinks/#error-handling","title":"Error handling","text":"<p><code>SlackChannel.send()</code> raises <code>SlackDeliveryError</code> (from <code>ayn_ml_pro.exceptions</code>) if the Slack API call fails. The <code>CloudRunner</code> catches per-sink errors and records them in <code>MonitoringReport.errors</code> \u2014 a delivery failure never stops the run.</p>"},{"location":"sinks/#required-slack-bot-scopes","title":"Required Slack bot scopes","text":"<p>The bot token must have the <code>chat:write</code> scope. If you use <code>@here</code> or <code>@channel</code> in <code>mention</code>, the bot also needs <code>chat:write.public</code> for public channels.</p>"},{"location":"sinks/#exception-reference","title":"Exception reference","text":"Exception When raised <code>SlackDeliveryError</code> Slack API call failed (network error, invalid token, channel not found) <code>ImportError</code> <code>slack-sdk</code> not installed \u2014 install with <code>pip install \"ayn-ml-pro[slack]\"</code>"},{"location":"stores/","title":"Stores","text":"<p>Stores are the persistence layer of ayn-ml. They implement two protocols: | Protocol | Method | Role | | <code>ResultSink</code> | <code>write(report)</code> | Write-only \u2014 notification channels | | <code>ResultStore</code> | <code>write</code> + <code>read_history</code> + <code>get_report</code> | Read + write \u2014 full persistence | Every <code>ResultStore</code> can also act as a <code>ResultSink</code>. Pass it to the Runner via <code>store=</code> and it handles persistence automatically after each run.</p>"},{"location":"stores/#choosing-a-store","title":"Choosing a store","text":"<p>| Store | Dependency | Use case | | <code>InMemoryStore</code> | core | Tests, notebooks, quick exploration | | <code>SqliteStore</code> | core (stdlib) | Local prod, CI, single-machine | | <code>JsonStore</code> | core | (coming soon) | | <code>ParquetStore</code> | <code>ayn-ml[parquet]</code> | (coming soon) | | <code>SqlStore</code> | <code>ayn-ml[sql]</code> | (coming soon) Multi-machine, PostgreSQL/MySQL | | <code>MlflowStore</code> | <code>ayn-ml[mlflow]</code> | (coming soon) | | <code>S3Store</code> | <code>ayn-ml-pro[s3]</code> | Cloud S3-compatible storage \u2014 see below |</p>"},{"location":"stores/#inmemorystore","title":"InMemoryStore","text":"<p>Stores <code>MonitoringReport</code> objects in an in-memory <code>deque</code>. Thread-safe. All data is lost when the process exits. from ayn_ml.stores import InMemoryStore store = InMemoryStore() report = Runner().run(plan, df, store=store) rows = store.read_history(\"fraud_v3\") df   = pd.DataFrame(rows) <code>maxlen</code> \u2014 keep only the N most recent reports. Useful for bounding memory usage in long-running pipelines: store = InMemoryStore(maxlen=100)  # evicts oldest when full Stores reports in a local SQLite file. No external deps (stdlib <code>sqlite3</code>). Thread-safe via <code>threading.Lock</code>. Idempotent: a duplicate <code>run_id</code> is silently ignored. from ayn_ml.stores import SqliteStore</p>"},{"location":"stores/#standard-usage","title":"Standard usage","text":"<p>store = SqliteStore(\"monitoring.db\") report = Runner().run(plan, df, store=store)</p>"},{"location":"stores/#context-manager-close-called-automatically","title":"Context manager \u2014 close() called automatically","text":"<p>with SqliteStore(\"monitoring.db\") as store:     report = Runner().run(plan, df, store=store)</p>"},{"location":"stores/#in-memory-db-for-tests-sql-behaviour-no-file-on-disk","title":"In-memory db for tests (SQL behaviour, no file on disk)","text":"<p>store = SqliteStore(\":memory:\") Upgrading from an older version \u2014 the schema has evolved between releases. A db created before the <code>metric_type</code> column was + (v0.x) will raise <code>sqlite3.OperationalError: table metric_results has no column named metric_type</code> on the first <code>write()</code>. Fix: recreate the db, or run manually: ALTER TABLE metric_results ADD COLUMN metric_type TEXT; Cross-session persistence \u2014 reopening the same file restores the full history: store1 = SqliteStore(\"monitoring.db\") store1.write(report) store1.close() store2 = SqliteStore(\"monitoring.db\")  # new conn rows = store2.read_history(\"fraud_v3\")  # retrieves all data</p>"},{"location":"stores/#read_history-querying-the-history","title":"<code>read_history()</code> \u2014 querying the history","text":"<p>Returns a list of flat dicts, newest first. Pass directly to <code>pd.DataFrame()</code>. rows = store.read_history(     model_id=\"fraud_v3\",         # required     model_version=\"3.1\",         # opt     metric_name=\"roc_auc\",       # opt     metric_type=\"performance\",   # opt \u2014 see below     limit=50,                    # opt \u2014 None returns everything     get_metadata=False,          # opt \u2014 see below df = pd.DataFrame(rows) Columns always present: | Column | Description | | <code>run_id</code> | Run id | | <code>model_id</code> | Model id | | <code>model_version</code> | Model version | | <code>metric_name</code> | Metric name (or stat name for profile rows) | | <code>feature_name</code> | Target column (drift, stats, profile) | | <code>val</code> | Computed val | | <code>status</code> | <code>True</code> / <code>False</code> / <code>None</code> (pass / fail / no threshold) | | <code>effect_size</code> | Normalised effect size (when applicable) | | <code>effect_size_label</code> | Effect size label | | <code>period_start</code> | Start of the observation window (ISO 8601) | | <code>period_end</code> | End of the observation window (ISO 8601) | | <code>metric_type</code> | Row type \u2014 see next section |</p>"},{"location":"stores/#filtering-by-metric_type","title":"Filtering by <code>metric_type</code>","text":"<p>The <code>metric_type</code> column discriminates rows in <code>metric_results</code>: | Value | Origin | | <code>\"performance\"</code> | <code>MetricSpec</code> with <code>metric_type=MetricType.performance</code> | | <code>\"drift\"</code> | <code>MetricSpec</code> with <code>metric_type=MetricType.drift</code> | | <code>\"statistics\"</code> | <code>MetricSpec</code> with <code>metric_type=MetricType.stats</code> | | <code>\"fairness\"</code> | <code>MetricSpec</code> with <code>metric_type=MetricType.fairness</code> | | <code>None</code> | <code>MetricSpec</code> with no explicit <code>metric_type</code> | | <code>\"profile\"</code> | Column profile stat (<code>enable_profiling=True</code>) |</p>"},{"location":"stores/#performance-metrics-only","title":"Performance metrics only","text":"<p>perf = store.read_history(\"fraud_v3\", metric_type=\"performance\")</p>"},{"location":"stores/#column-profile-stats-only","title":"Column profile stats only","text":"<p>profile = store.read_history(\"fraud_v3\", metric_type=\"profile\") df_profile = pd.DataFrame(profile)</p>"},{"location":"stores/#df_profilepivotindexperiod_start-columnsfeature_name-metric_name-valuesvalue","title":"df_profile.pivot(index=\"period_start\", columns=[\"feature_name\", \"metric_name\"], values=\"value\")","text":"<p>Without <code>metric_type</code>, all rows are returned (metrics + profile).</p>"},{"location":"stores/#enriching-rows-with-get_metadatatrue","title":"Enriching rows with <code>get_metadata=True</code>","text":"<p>Performs a JOIN with <code>monitoring_runs</code> and expands the plan JSON into columns prefixed <code>plan_</code> / <code>run_</code>: rows = store.read_history(\"fraud_v3\", get_metadata=True) Additional columns: <code>plan_name</code>, <code>plan_window_type</code>, <code>plan_window_n</code>, <code>plan_sampling_type</code>, <code>plan_sampling_frac</code>, <code>run_n_current</code>, <code>run_n_reference</code></p>"},{"location":"stores/#get_report-retrieving-a-full-report","title":"<code>get_report()</code> \u2014 retrieving a full report","text":"<p>report = store.get_report(run_id)  # MonitoringReport | None Reconstructs the complete <code>MonitoringReport</code> from the store: <code>plan</code>, <code>ctx</code> (including <code>period_start</code> / <code>period_end</code>), <code>results</code>, <code>errors</code>, <code>fired_alerts</code>, and <code>profile</code> (if <code>enable_profiling=True</code> was set at run time).</p>"},{"location":"stores/#time-series-pattern","title":"Time-series pattern","text":"<p>import pandas as pd import matplotlib.pyplot as plt rows = store.read_history(\"fraud_v3\", metric_name=\"roc_auc\") df = pd.DataFrame(rows) df[\"period_start\"] = pd.to_datetime(df[\"period_start\"]) df = df.sort_values(\"period_start\") plt.plot(df[\"period_start\"], df[\"value\"], marker=\"o\") plt.title(\"AUC over time \u2014 fraud_v3\") plt.show()</p>"},{"location":"stores/#thread-safety-and-idempotence","title":"Thread safety and idempotence","text":"<ul> <li>Thread-safe: both stores use <code>threading.Lock</code> \u2014 concurrent calls from   multiple threads carry no risk of data corruption.</li> <li>Idempotent: calling <code>write(report)</code> twice with the same <code>run_id</code> has   no effect \u2014 the second call is silently ignored. Useful in pipelines with   retry logic. store.write(report)  # ignored \u2014 run_id already present assert len(store.read_history(\"m\")) == len(original_results)</li> </ul>"},{"location":"stores/#s3store-ayn-ml-pro","title":"S3Store \u2014 <code>ayn-ml-pro</code>","text":"<p>Status: API and class structure are defined. <code>S3Store.save()</code>, <code>.load()</code>, and <code>.list_runs()</code> are not yet implemented.</p> <p>Import: <code>from ayn_ml_pro.stores import S3Store</code></p> <p>Stub in <code>ayn-ml</code>: <code>ayn_ml.stores</code> exports <code>S3Store: type[Any] | None = None</code>. This stub is populated by <code>ayn_ml_pro._extension</code> when <code>ayn-ml-pro</code> is installed; it is <code>None</code> otherwise. Accessing a <code>None</code> value raises <code>TypeError</code> at call time with a clear message. Import from <code>ayn_ml_pro.stores</code> directly in all production code.</p> <p>Extra required: <code>pip install \"ayn-ml-pro[s3]\"</code></p> <p><code>S3Store</code> implements the <code>ResultStore</code> protocol from <code>ayn-ml</code> (Apache 2.0). It serialises each <code>MonitoringReport</code> to JSON and writes it to any S3-compatible bucket (AWS S3, MinIO, GCS interop layer, etc.).</p>"},{"location":"stores/#key-format","title":"Key format","text":"<p>Reports are stored under:</p> <pre><code>{prefix}{plan_name}/{run_id}.json\n</code></pre> <p>Example with <code>prefix=\"ayn-ml/\"</code> and <code>plan_name=\"fraud_monitor\"</code>:</p> <pre><code>ayn-ml/fraud_monitor/2024-06-01T12:00:00Z_abc123.json\n</code></pre>"},{"location":"stores/#constructor","title":"Constructor","text":"<pre><code>S3Store(\n    bucket: str,                        # S3 bucket name\n    prefix: str = \"ayn-ml/\",           # Key prefix for all reports\n    region_name: str | None = None,     # AWS region\n    endpoint_url: str | None = None,    # Custom endpoint (MinIO, GCS, etc.)\n    aws_access_key_id: str | None = None,\n    aws_secret_access_key: str | None = None,\n)\n</code></pre> <p>Credentials must not be hardcoded. Prefer IAM roles or env variables (<code>AWS_ACCESS_KEY_ID</code>, <code>AWS_SECRET_ACCESS_KEY</code>):</p> <pre><code>from ayn_ml_pro.stores import S3Store\n\n# AWS \u2014 credentials from env / IAM role\nstore = S3Store(bucket=\"my-monitoring-bucket\", prefix=\"ayn-ml/reports/\", region_name=\"eu-west-1\")\n\n# MinIO or other S3-compatible store\nstore = S3Store(\n    bucket=\"my-bucket\",\n    endpoint_url=\"http://minio:9000\",\n    aws_access_key_id=os.environ[\"MINIO_ACCESS_KEY\"],\n    aws_secret_access_key=os.environ[\"MINIO_SECRET_KEY\"],\n)\n\n# Persist a report\nstore.write(report)\n\n# Retrieve a report by run ID\nreport = store.load(\"abc123\")            # -&gt; MonitoringReport\n\n# List all run IDs for a plan (oldest first)\nrun_ids = store.list_runs(\"fraud_monitor\")  # -&gt; list[str]\n</code></pre>"},{"location":"stores/#wiring-to-a-runner","title":"Wiring to a Runner","text":"<pre><code>from ayn_ml.runner import Runner\nfrom ayn_ml_pro.stores import S3Store\n\nstore = S3Store(bucket=\"my-monitoring-bucket\", region_name=\"us-east-1\")\nrunner = Runner(plan, store=store)\nrunner.run(df_current, ref=df_reference)\n</code></pre>"},{"location":"stores/#exception-reference","title":"Exception reference","text":"Exception When raised <code>StoreConnectionError</code> S3 write, read, or list call failed <code>KeyError</code> <code>load()</code> called with a run ID that does not exist in the bucket <code>ImportError</code> <code>boto3</code> not installed \u2014 install with <code>pip install \"ayn-ml-pro[s3]\"</code>"},{"location":"api/","title":"API Reference","text":"<p>Complete auto-generated reference for all public symbols in ayn-ml. Docstrings follow the Google style.</p> Module Contents Core <code>MonitoringPlan</code>, <code>MetricSpec</code>, schemas, <code>MetricResult</code>, <code>MonitoringReport</code>, alerts Metrics Registry, <code>@register_metric</code>, built-in metric list Advisor <code>MetricDesigner</code>, <code>SuggestedPlan</code> Runner <code>Runner</code> Data <code>DataFrameSource</code>, <code>SamplingStrategy</code>, <code>DataPartitioner</code> Stores <code>InMemoryStore</code>, <code>SqliteStore</code> Sinks <code>ResultSink</code>, <code>EmailChannel</code>, <code>WebhookChannel</code> Renderers <code>HtmlRenderer</code> <p>Public API surface</p> <p>Only symbols in <code>__all__</code> of each module are documented here. Internal helpers (prefixed <code>_</code>) are excluded.</p>"},{"location":"api/advisor/","title":"Advisor","text":"<p><code>MetricDesigner</code> analyses your data and generates a ready-to-use <code>MonitoringPlan</code>. It routes each feature to statistically appropriate drift metrics based on column type, sample size, normality, skewness, and variance relative to a reference window.</p> <p>\u2192 Full advisor guide with decision trees</p>"},{"location":"api/advisor/#metricdesigner","title":"MetricDesigner","text":""},{"location":"api/advisor/#ayn_ml.advisor.MetricDesigner","title":"MetricDesigner","text":"<p>Automatic <code>MonitoringPlan</code> generator from data characteristics.</p> <p>Analyses each feature column in a DataFrame (column type, sample size, normality, skewness, optional variance ratio vs. reference) and builds a <code>MonitoringPlan</code> with appropriate drift and performance metrics.</p> <p>The constructor takes the schema so it can be reused across multiple <code>suggest()</code> calls (e.g. different time windows) without re-specifying it each time.</p> <p>Parameters:</p> Name Type Description Default <code>schema</code> <code>TabularSchema</code> <p><code>TabularSchema</code> describing label, prediction, probability, and feature columns.</p> required <p>Example::</p> <pre><code>designer = MetricDesigner(schema)\nresult = designer.suggest(df, reference=ref_df, task_type=\"classification\")\n</code></pre>"},{"location":"api/advisor/#ayn_ml.advisor.MetricDesigner.suggest","title":"suggest","text":"<pre><code>suggest(df: Any, *, reference: Any, task_type: str = 'classification', name: str = 'suggested_plan', model_id: str = '', model_version: str = '') -&gt; SuggestedPlan\n</code></pre> <p>Analyse df and generate a <code>MonitoringPlan</code>.</p> <p>The generated plan contains:</p> <ul> <li>Performance specs \u2014 selected based on <code>task_type</code> and the   class imbalance ratio of <code>df</code>.</li> <li>Target drift \u2014 monitors label distribution shift over time.</li> <li>Drift specs \u2014 one set per feature column, routing to   parametric (ttest) or non-parametric (mannwhitney) tests based on   normality, sample size, and (when <code>reference</code> is provided)   variance ratio.</li> </ul> <p><code>reference</code> is used for normality routing (skewness and normality tests run on the reference distribution \u2014 the stable baseline you are monitoring against) and for variance-ratio computation (Levene routing).</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Current-window DataFrame (narwhals-compatible: pandas, Polars, or any narwhals-supported backend).</p> required <code>reference</code> <code>Any</code> <p>Reference DataFrame (training baseline or historical window).  Must use the same schema as df.</p> required <code>task_type</code> <code>str</code> <p><code>\"classification\"</code> (default) or <code>\"regression\"</code>. Regression detection from a float label is ambiguous, so this must be declared explicitly.</p> <code>'classification'</code> <code>name</code> <code>str</code> <p>Human-readable name for the generated <code>MonitoringPlan</code>.</p> <code>'suggested_plan'</code> <code>model_id</code> <code>str</code> <p>Model identifier embedded in the plan.</p> <code>''</code> <code>model_version</code> <code>str</code> <p>Model version string embedded in the plan.</p> <code>''</code> <p>Returns:</p> Type Description <code>SuggestedPlan</code> <p><code>SuggestedPlan</code> containing the generated <code>MonitoringPlan</code></p> <code>SuggestedPlan</code> <p>and a list of advisory warning strings.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If task_type has an invalid value or reference is <code>None</code>.</p>"},{"location":"api/advisor/#suggestedplan","title":"SuggestedPlan","text":""},{"location":"api/advisor/#ayn_ml.advisor.SuggestedPlan","title":"SuggestedPlan  <code>dataclass</code>","text":"<p>Output of <code>MetricDesigner.suggest()</code>.</p> <p>Wraps a ready-to-use <code>MonitoringPlan</code> together with human-readable warnings that explain the advisor's decisions \u2014 which metrics were added, demoted, or excluded, and why.</p> <p>Parameters:</p> Name Type Description Default <code>plan</code> <code>MonitoringPlan</code> <p>The generated <code>MonitoringPlan</code>, ready to pass to <code>Runner</code>.</p> required <code>warnings</code> <code>tuple[str, ...]</code> <p>Ordered tuple of advisory messages.  Each message names the affected column or metric and gives the quantitative reason (e.g. <code>\"levene added for 'age': variance_ratio=1.83\"</code>).</p> <code>()</code> <p>Example::</p> <pre><code>result = designer.suggest(df, reference=ref_df)\nplan   = result.plan\nfor w in result.warnings:\n    print(w)\n</code></pre>"},{"location":"api/advisor/#ayn_ml.advisor.SuggestedPlan.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Serialise to a JSON-compatible dict.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict with <code>\"plan\"</code> (from <code>MonitoringPlan.model_dump()</code>) and</p> <code>dict[str, Any]</code> <p><code>\"warnings\"</code> (list of strings).</p>"},{"location":"api/advisor/#column-analysis-internals","title":"Column analysis internals","text":"<p>Internal API</p> <p><code>ColumnAnalysis</code> is an internal data class exposed here for transparency. Its fields and behaviour are not covered by semantic versioning guarantees.</p>"},{"location":"api/advisor/#ayn_ml.advisor._analysis.ColumnAnalysis","title":"ColumnAnalysis  <code>dataclass</code>","text":"<p>Statistical summary of a single feature column.</p> <p>Used by <code>_rules.py</code> to route each feature to the appropriate drift metrics.  Numeric stats (skewness, kurtosis, is_normal) are set to neutral defaults for categorical columns.</p> <p>Parameters:</p> Name Type Description Default <code>col_name</code> <code>str</code> <p>Column name as it appears in the DataFrame.</p> required <code>col_type</code> <code>ColumnType</code> <p>Inferred or declared column type.</p> required <code>n</code> <code>int</code> <p>Number of non-null rows used for analysis.</p> required <code>skewness</code> <code>float</code> <p>Sample skewness (0.0 for categoricals).</p> required <code>kurtosis</code> <code>float</code> <p>Excess kurtosis (0.0 for categoricals).</p> required <code>is_normal</code> <code>bool</code> <p>Whether the normality test passed at \u03b1=0.05. Always <code>False</code> for categoricals.</p> required <code>variance_ratio</code> <code>float | None</code> <p><code>std_current / std_reference</code>.  <code>None</code> when no reference was provided or when std_reference is zero.</p> required"},{"location":"api/core/","title":"Core","text":"<p>The <code>ayn_ml.core</code> package contains all shared types: plans, schemas, specs, results, reports, and alert primitives.</p>"},{"location":"api/core/#plans","title":"Plans","text":""},{"location":"api/core/#ayn_ml.core.spec.MonitoringPlan","title":"MonitoringPlan","text":"<p>               Bases: <code>BaseModel</code></p> <p>Top-level configuration for a monitoring run.</p> <p>A MonitoringPlan binds together the data schema, model identity, the list of metrics to evaluate, and optional data-loading directives. It is the primary input to the Runner.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Human-readable name for this plan (used in reports and storage).</p> <code>model_id</code> <code>str</code> <p>Identifier of the model being monitored.</p> <code>model_version</code> <code>str</code> <p>Version string of the model (e.g. <code>\"1.2.0\"</code>).</p> <code>data_schema</code> <code>DataSchema</code> <p>Schema describing the structure of the evaluation DataFrame. Accepts TabularSchema, TextSchema, or AgentSchema.</p> <code>metrics</code> <code>list[MetricSpec]</code> <p>Ordered list of metric specifications to compute.</p> <code>enable_profiling</code> <code>bool</code> <p>When <code>True</code>, the Runner computes a statistical profile (min, max, mean, std, percentiles, null rate, etc.) for every feature column referenced by the configured metrics plus the schema target columns (<code>prediction_col</code>, <code>label_col</code>, <code>proba_col</code>) and attaches the result to <code>MonitoringReport.profile</code>.  Defaults to <code>False</code> \u2014 no profiling overhead on standard runs.</p> <code>window</code> <code>WindowConfig | None</code> <p>How to narrow the loaded DataFrame to the current monitoring window.  <code>None</code> means the source is already pre-filtered.</p> <code>sampling</code> <code>RandomSamplingConfig | None</code> <p>Optional random subsampling applied after window selection, to reduce the current window size for performance.  <code>None</code> means all rows in the window are used.</p> <code>description</code> <code>str</code> <p>Optional free-text description stored alongside reports.</p>"},{"location":"api/core/#metric-specs","title":"Metric specs","text":""},{"location":"api/core/#ayn_ml.core.spec.MetricSpec","title":"MetricSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>Immutable specification for a single metric computation.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Registry name used to look up the metric implementation.</p> <code>metric_type</code> <code>MetricType | None</code> <p>Category of this metric (see MetricType).  Defaults to <code>None</code>; the runner resolves the type from the registry at runtime.  Set explicitly only for custom/unregistered metrics.</p> <code>feature_name</code> <code>str | None</code> <p>Column name for drift and statistics metrics that operate on a single feature.  Not required for performance metrics.</p> <code>params</code> <code>dict[str, Any]</code> <p>Arbitrary keyword arguments forwarded to the metric's <code>compute()</code> method (e.g. <code>{\"average\": \"macro\"}</code> for F1, <code>{\"q\": 0.95}</code> for a quantile).</p> <code>threshold</code> <code>float | list[float] | None</code> <p>Pass/fail threshold for the computed value.  Accepts: - <code>None</code>: no evaluation, <code>MetricResult.status</code> will be <code>None</code>. - <code>float</code>: scalar bound (direction controlled by <code>upper_bound</code>). - <code>list[float]</code>: two-element <code>[lo, hi]</code> range; passes when   <code>lo &lt;= value &lt;= hi</code>.</p> <code>upper_bound</code> <code>bool</code> <p>When <code>True</code> (default), the metric passes when <code>value &lt;= threshold</code>.  When <code>False</code>, it passes when <code>value &gt;= threshold</code>.  Ignored when <code>threshold</code> is a list.</p> <code>metric_type</code> <code>MetricType | None</code> <p>Category of this metric.  When <code>None</code> (default), the type is resolved at runtime from the metric registry using the <code>name</code>.  Provide an explicit value only for custom metrics that are not yet registered.</p>"},{"location":"api/core/#ayn_ml.core.spec.MetricType","title":"MetricType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Enumeration of metric categories supported by ayn-ml.</p> <p>Attributes:</p> Name Type Description <code>performance</code> <p>Supervised performance metrics (accuracy, F1, MSE, \u2026).</p> <code>drift</code> <p>Distribution-shift metrics (PSI, Wasserstein, MMD, \u2026).</p> <code>statistics</code> <p>Descriptive statistics (mean, std, quantile, \u2026).</p> <code>fairness</code> <p>Group-level disparity metrics (demographic parity, equalized odds, \u2026).</p> <code>nlp_quality</code> <p>Text-quality metrics (ROUGE, BLEU, METEOR, \u2026).</p> <code>nlp_drift</code> <p>Embedding- or token-level distribution shift.</p> <code>nlp_safety</code> <p>Toxicity, bias, and safety classifiers.</p> <code>agent_performance</code> <p>Agent task-completion and quality scores.</p> <code>agent_cost</code> <p>Token usage and cost tracking for agent runs.</p> <code>custom</code> <p>User-defined metrics registered at runtime.</p>"},{"location":"api/core/#schemas","title":"Schemas","text":""},{"location":"api/core/#ayn_ml.core.schema.TabularSchema","title":"TabularSchema","text":"<p>               Bases: <code>BaseSchema</code></p> <p>Schema for supervised tabular ML models (classification and regression).</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['tabular']</code> <p>Discriminator literal, always <code>\"tabular\"</code>.</p> <code>label_col</code> <code>str</code> <p>Column containing ground-truth labels (y_true).</p> <code>prediction_col</code> <code>str</code> <p>Column containing model predictions (y_pred).</p> <code>proba_col</code> <code>str | None</code> <p>Column containing predicted probabilities; required for AUC, log-loss, and Brier score.  Set to <code>None</code> to disable.</p> <code>feature_types</code> <code>dict[str, Literal['numeric', 'categorical']]</code> <p>Maps column names to <code>\"numeric\"</code> or <code>\"categorical\"</code>. Used by drift metrics to determine the appropriate computation path. Columns absent from this dict fall back to numpy dtype inference, which is correct for float/string columns but wrong for integer-encoded categoricals.  Use <code>from_dataframe</code> to populate this automatically with optional overrides.</p> <code>protected_cols</code> <code>list[str] | None</code> <p>Columns representing sensitive or protected attributes (e.g. <code>[\"gender\", \"age_group\"]</code>).  Fairness metrics read <code>spec.feature_name</code> and require it to be listed here when this field is set.  <code>None</code> disables the declaration check (the column must still exist in the DataFrame).</p>"},{"location":"api/core/#ayn_ml.core.schema.TabularSchema.column_names","title":"column_names  <code>property</code>","text":"<pre><code>column_names: list[str]\n</code></pre> <p>Return all non-None column name values declared by this schema.</p>"},{"location":"api/core/#ayn_ml.core.schema.TabularSchema.from_dataframe","title":"from_dataframe  <code>classmethod</code>","text":"<pre><code>from_dataframe(df: Any, feature_types: dict[str, Literal['numeric', 'categorical']] | None = None, **kwargs: Any) -&gt; TabularSchema\n</code></pre> <p>Build a TabularSchema by inferring column types from a DataFrame.</p> <p>Only feature columns are inspected \u2014 schema columns (<code>label_col</code>, <code>prediction_col</code>, <code>proba_col</code>, <code>timestamp_col</code>, <code>model_id_col</code>, <code>model_version_col</code>) are excluded so they never appear in <code>feature_types</code> and cannot accidentally be fed to drift metrics.</p> <p>Remaining columns are classified by numpy dtype:</p> <ul> <li>Numeric dtypes (int, float) \u2192 <code>\"numeric\"</code></li> <li>All other dtypes (object, string, category) \u2192 <code>\"categorical\"</code></li> </ul> <p>The optional <code>feature_types</code> argument overrides inferred values for specific columns.  Use it to correct integer-encoded categoricals that dtype inference cannot distinguish from genuine numeric features.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame (eager pandas or Polars DataFrame). Polars <code>LazyFrame</code> is not supported; call <code>.collect()</code> first.</p> required <code>feature_types</code> <code>dict[str, Literal['numeric', 'categorical']] | None</code> <p>Explicit type overrides applied on top of inference. Only columns that would be inferred incorrectly need to be listed here (e.g. <code>{\"region\": \"categorical\"}</code>).</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional <code>TabularSchema</code> fields (<code>label_col</code>, <code>prediction_col</code>, <code>proba_col</code>, etc.).</p> <code>{}</code> <p>Returns:</p> Type Description <code>TabularSchema</code> <p>TabularSchema with <code>feature_types</code> populated from inference</p> <code>TabularSchema</code> <p>merged with any explicit overrides.</p>"},{"location":"api/core/#ayn_ml.core.schema.TextSchema","title":"TextSchema","text":"<p>               Bases: <code>BaseSchema</code></p> <p>Schema for NLP / LLM text-in / text-out models.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['text']</code> <p>Discriminator literal, always <code>\"text\"</code>.</p> <code>input_col</code> <code>str</code> <p>Column containing the model input text.</p> <code>output_col</code> <code>str</code> <p>Column containing the model output text.</p> <code>reference_col</code> <code>str | None</code> <p>Column containing reference / expected output for quality metrics such as ROUGE and BLEU.</p> <code>embedding_col</code> <code>str | None</code> <p>Column containing pre-computed embeddings; used by semantic similarity and embedding-drift metrics.</p>"},{"location":"api/core/#ayn_ml.core.schema.TextSchema.column_names","title":"column_names  <code>property</code>","text":"<pre><code>column_names: list[str]\n</code></pre> <p>Return all non-None column name values declared by this schema.</p>"},{"location":"api/core/#ayn_ml.core.schema.AgentSchema","title":"AgentSchema","text":"<p>               Bases: <code>BaseSchema</code></p> <p>Schema for AI agent traces (tool-calling, multi-step reasoning).</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['agent']</code> <p>Discriminator literal, always <code>\"agent\"</code>.</p> <code>input_col</code> <code>str</code> <p>Column containing the user instruction / prompt.</p> <code>output_col</code> <code>str</code> <p>Column containing the agent's final response.</p> <code>trace_col</code> <code>str</code> <p>Column containing the full execution trace (tool calls, intermediate steps, etc.).</p> <code>success_col</code> <code>str | None</code> <p>Optional column with a boolean task-completion flag.</p> <code>tool_calls_col</code> <code>str | None</code> <p>Optional column with serialized tool-call records.</p> <code>tokens_used_col</code> <code>str | None</code> <p>Optional column with total token consumption per run.</p> <code>latency_col</code> <code>str | None</code> <p>Optional column with end-to-end latency in milliseconds.</p> <code>cost_col</code> <code>str | None</code> <p>Optional column with cost in USD per run.</p>"},{"location":"api/core/#ayn_ml.core.schema.AgentSchema.column_names","title":"column_names  <code>property</code>","text":"<pre><code>column_names: list[str]\n</code></pre> <p>Return all non-None column name values declared by this schema.</p>"},{"location":"api/core/#results","title":"Results","text":""},{"location":"api/core/#ayn_ml.core.result.MetricResult","title":"MetricResult","text":"<p>               Bases: <code>BaseModel</code></p> <p>Computed output for a single metric.</p> <p>Context (model_id, timestamp, \u2026) lives on MonitoringReport, not here. MetricResult is a pure computation result: spec in \u2192 value + status out.</p> <p>Attributes:</p> Name Type Description <code>spec</code> <code>MetricSpec</code> <p>The MetricSpec that drove this computation.</p> <code>value</code> <code>float | int | str | None</code> <p>Numeric or string result.  <code>None</code> when the metric could not produce a value but did not raise (edge-case only).</p> <code>status</code> <code>bool | None</code> <p><code>True</code> if the value passes the spec's threshold, <code>False</code> if it fails, or <code>None</code> when no threshold is defined.</p> <code>conf_interval</code> <code>tuple[float, float] | None</code> <p>Optional <code>(lower, upper)</code> confidence interval for metrics that support bootstrapping (reserved for future use).</p> <code>effect_size</code> <code>float | None</code> <p>Standardised effect size where applicable (e.g. Cohen's d for t-test, Cliff's delta for Mann-Whitney, variance ratio for Levene, KS D-statistic for ks_2samp).  <code>None</code> when the metric does not produce an effect size.</p> <code>effect_size_label</code> <code>str | None</code> <p>Human-readable identifier for the effect size scale (e.g. <code>\"cohen_d\"</code>, <code>\"cliff_delta\"</code>, <code>\"variance_ratio\"</code>, <code>\"ks_statistic\"</code>).  Always set when <code>effect_size</code> is not <code>None</code>; <code>None</code> otherwise.</p>"},{"location":"api/core/#ayn_ml.core.result.MetricError","title":"MetricError  <code>dataclass</code>","text":"<p>Structured record of a metric that raised during computation.</p> <p>The Runner catches exceptions per-metric and converts them to MetricError rather than aborting the whole run.</p> <p>Attributes:</p> Name Type Description <code>metric_name</code> <code>str</code> <p>Registry name of the metric that failed.</p> <code>error_type</code> <code>str</code> <p>Class name of the exception (e.g. <code>\"SchemaError\"</code>).</p> <code>message</code> <code>str</code> <p>Human-readable error message.</p>"},{"location":"api/core/#ayn_ml.core.result.MonitoringReport","title":"MonitoringReport  <code>dataclass</code>","text":"<p>Top-level output of a Runner execution.</p> <p>Aggregates all metric results, errors, and fired alerts from a single monitoring run, bound to a plan and execution context.</p> <p>Attributes:</p> Name Type Description <code>plan</code> <code>MonitoringPlan</code> <p>The MonitoringPlan that was executed.</p> <code>context</code> <code>ExecutionContext</code> <p>Identity and time metadata for this run.</p> <code>results</code> <code>list[MetricResult]</code> <p>Successful MetricResult objects, one per spec.</p> <code>errors</code> <code>list[MetricError]</code> <p>MetricError objects for specs that failed to compute.</p> <code>fired_alerts</code> <code>list[FiredAlert]</code> <p>Alerts triggered by AlertRule evaluation (empty when no alert rules are configured).</p> <code>profile</code> <code>dict[str, dict[str, float | int | str | None]] | None</code> <p>Statistical profile of feature and target columns, populated when <code>plan.enable_profiling=True</code>.  <code>None</code> otherwise.</p>"},{"location":"api/core/#ayn_ml.core.result.MonitoringReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Serialise the report to a plain Python dictionary.</p> <p>Nested Pydantic models are serialised with <code>model_dump()</code>. Dataclass fields are converted to dicts manually to avoid a Pydantic dependency in the serialisation path.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A JSON-serialisable dict with keys <code>plan</code>, <code>context</code>,</p> <code>dict[str, Any]</code> <p><code>results</code>, <code>errors</code>, <code>fired_alerts</code>, and (when</p> <code>dict[str, Any]</code> <p><code>self.profile</code> is not <code>None</code>) <code>profile</code>.  The key</p> <code>dict[str, Any]</code> <p><code>profile</code> is present only when <code>plan.enable_profiling</code> is</p> <code>dict[str, Any]</code> <p><code>True</code> and profiling succeeded.</p>"},{"location":"api/core/#ayn_ml.core.result.MonitoringReport.to_dataframe","title":"to_dataframe","text":"<pre><code>to_dataframe() -&gt; Any\n</code></pre> <p>Convert metric results to a tidy pandas DataFrame.</p> <p>Each row represents one MetricResult.  Context fields (model_id, model_version, eval_timestamp) are broadcast from the report-level ExecutionContext rather than stored per-result.</p> <p>Returns:</p> Type Description <code>Any</code> <p>A pandas DataFrame with columns: metric_name, metric_type,</p> <code>Any</code> <p>feature_name, value, status, effect_size, effect_size_label,</p> <code>Any</code> <p>threshold, model_id, model_version, eval_timestamp.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If pandas is not installed.</p>"},{"location":"api/core/#execution-context","title":"Execution context","text":""},{"location":"api/core/#ayn_ml.core.result.ExecutionContext","title":"ExecutionContext","text":"<p>               Bases: <code>BaseModel</code></p> <p>Immutable identity and time metadata for a single monitoring execution.</p> <p>One ExecutionContext is created per Runner invocation and attached to the resulting MonitoringReport.  It is NOT duplicated on individual MetricResult objects \u2014 retrieve it from the report instead.</p> <p>Attributes:</p> Name Type Description <code>run_id</code> <code>str</code> <p>Unique identifier for this run, auto-generated as a hex UUID. Stable within a run; use it to correlate report entries in a store.</p> <code>model_id</code> <code>str</code> <p>Identifier of the model being monitored.</p> <code>model_version</code> <code>str</code> <p>Version string of the model.</p> <code>eval_timestamp</code> <code>datetime</code> <p>Wall-clock time at which the run was executed (UTC).</p> <code>period_start</code> <code>datetime | None</code> <p>Start of the data window (min of timestamp_col).  <code>None</code> when <code>timestamp_col</code> is not configured in the schema.</p> <code>period_end</code> <code>datetime | None</code> <p>End of the data window (max of timestamp_col).  <code>None</code> when <code>timestamp_col</code> is not configured in the schema.</p> <code>n_current</code> <code>int | None</code> <p>Row count of the current window after all filtering.</p> <code>n_reference</code> <code>int | None</code> <p>Row count of the reference window, or <code>None</code> when no reference was provided.</p>"},{"location":"api/core/#alerts","title":"Alerts","text":""},{"location":"api/core/#ayn_ml.core.alert.AlertRule","title":"AlertRule  <code>dataclass</code>","text":"<p>Binding between a metric, an alert policy, and notification channels.</p> <p>When the Runner evaluates <code>alert_rules</code>, it matches each rule's <code>metric_name</code> to a computed <code>MetricResult</code> and calls <code>policy.evaluate(result)</code>.  If the policy fires, every channel in <code>channels</code> receives <code>write(report)</code> with the completed <code>MonitoringReport</code>.</p> <p>Parameters:</p> Name Type Description Default <code>metric_name</code> <code>str</code> <p>Registry name of the metric to watch (must match a <code>MetricSpec.name</code> in the monitoring plan).</p> required <code>policy</code> <code>AlertPolicy</code> <p>Alert policy that decides whether the alert fires.</p> required <code>channels</code> <code>list[ResultSink]</code> <p>List of <code>ResultSink</code> implementations to notify when the alert fires.  Typically <code>[EmailChannel(...)]</code>, <code>[WebhookChannel(...)]</code>, or a mix of both.</p> <code>list()</code> <p>Example::</p> <pre><code>from ayn_ml.core.alert import AlertRule, ThresholdPolicy\nfrom ayn_ml.sinks.email import EmailChannel\n\nchannel = EmailChannel(host=\"smtp.example.com\", to_addrs=[\"ops@example.com\"])\nrule = AlertRule(\n    metric_name=\"psi\",\n    policy=ThresholdPolicy(threshold=0.2),\n    channels=[channel],\n)\nrunner.run(plan, df, alert_rules=[rule])\n</code></pre>"},{"location":"api/core/#ayn_ml.core.alert.ThresholdPolicy","title":"ThresholdPolicy  <code>dataclass</code>","text":"<p>Stateless threshold-based alert policy.</p> <p>Fires when a <code>MetricResult</code>'s value crosses the configured threshold. When <code>threshold</code> is <code>None</code>, delegates to the result's pre-computed <code>status</code> field \u2014 i.e., fires whenever <code>status is False</code> (meaning the metric's own spec threshold was breached).</p> <p>This is the default policy for simple \"alert me when the metric is bad\" use cases.  It requires no historical data and no store access.</p> <p>Parameters:</p> Name Type Description Default <code>threshold</code> <code>float | None</code> <p>Custom alert threshold.  When <code>None</code> (default), fires when <code>MetricResult.status is False</code>.  When set, compares the result's <code>value</code> directly, independently of the spec threshold.</p> <code>None</code> <code>upper_bound</code> <code>bool</code> <p>Direction of the custom threshold check.  When <code>True</code> (default), fires when <code>value &gt; threshold</code>.  When <code>False</code>, fires when <code>value &lt; threshold</code>.  Ignored when <code>threshold</code> is <code>None</code>.</p> <code>True</code> <p>Example::</p> <pre><code># Fire when the metric's own threshold is breached\npolicy = ThresholdPolicy()\n\n# Fire when accuracy drops below 0.80 (regardless of spec)\npolicy = ThresholdPolicy(threshold=0.80, upper_bound=False)\n\n# Fire when PSI exceeds 0.2\npolicy = ThresholdPolicy(threshold=0.2, upper_bound=True)\n</code></pre>"},{"location":"api/core/#ayn_ml.core.alert.ThresholdPolicy.policy_type","title":"policy_type  <code>property</code>","text":"<pre><code>policy_type: str\n</code></pre> <p>Policy type identifier.</p>"},{"location":"api/core/#ayn_ml.core.alert.ThresholdPolicy.evaluate","title":"evaluate","text":"<pre><code>evaluate(result: MetricResult) -&gt; bool\n</code></pre> <p>Return True when the alert condition is met.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>MetricResult</code> <p>MetricResult to evaluate.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the alert should fire, <code>False</code> otherwise.</p>"},{"location":"api/core/#ayn_ml.core.alert.ThresholdPolicy.details","title":"details","text":"<pre><code>details(result: MetricResult) -&gt; dict[str, Any]\n</code></pre> <p>Return alert detail dict for the FiredAlert record.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>MetricResult</code> <p>MetricResult that triggered the alert.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict with <code>value</code>, <code>threshold</code>, and <code>upper_bound</code> keys.</p>"},{"location":"api/core/#ayn_ml.core.alert.AlertPolicy","title":"AlertPolicy","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol satisfied by all alert policies.</p> <p>An AlertPolicy is a stateless callable object: given a single <code>MetricResult</code> it returns <code>True</code> when the alert should fire.</p> <p>Implementations must also expose:</p> <ul> <li><code>policy_type</code> \u2014 short string identifier (e.g. <code>\"threshold\"</code>).</li> <li><code>details(result)</code> \u2014 dict of policy-specific context attached to   the <code>FiredAlert</code> record.</li> </ul> <p>The Runner calls <code>evaluate()</code> once per (AlertRule, MetricResult) pair.</p>"},{"location":"api/core/#ayn_ml.core.alert.AlertPolicy.policy_type","title":"policy_type  <code>property</code>","text":"<pre><code>policy_type: str\n</code></pre> <p>Short string identifying the policy kind (e.g. <code>\"threshold\"</code>).</p>"},{"location":"api/core/#ayn_ml.core.alert.AlertPolicy.evaluate","title":"evaluate","text":"<pre><code>evaluate(result: MetricResult) -&gt; bool\n</code></pre> <p>Return True when the alert condition is met for result.</p>"},{"location":"api/core/#ayn_ml.core.alert.AlertPolicy.details","title":"details","text":"<pre><code>details(result: MetricResult) -&gt; dict[str, Any]\n</code></pre> <p>Return policy-specific context for the FiredAlert record.</p>"},{"location":"api/core/#ayn_ml.core.result.FiredAlert","title":"FiredAlert  <code>dataclass</code>","text":"<p>Lightweight record of an alert triggered during a monitoring run.</p> <p>FiredAlert is intentionally minimal \u2014 it carries only what is needed to log or notify.  Full AlertRule configuration lives in the alert layer.</p> <p>Attributes:</p> Name Type Description <code>metric_name</code> <code>str</code> <p>Registry name of the metric that triggered the alert.</p> <code>policy_type</code> <code>str</code> <p>Policy kind: <code>\"threshold\"</code>, <code>\"change\"</code>, or <code>\"consecutive\"</code>.</p> <code>details</code> <code>dict[str, Any]</code> <p>Policy-specific context, e.g. <code>{\"threshold\": 0.8, \"value\": 0.7}</code> for a threshold alert.</p> <code>feature_name</code> <code>str | None</code> <p>Feature column the result belongs to, or <code>None</code> for global (plan-level) metrics such as performance and target drift. Populated when a single alert rule matches multiple per-feature results (e.g. <code>psi/age</code> and <code>psi/income</code>).</p>"},{"location":"api/core/#exceptions","title":"Exceptions","text":""},{"location":"api/core/#ayn_ml.exceptions.AynError","title":"AynError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all ayn-ml errors.</p> <p>Catch this class to handle any library-specific error uniformly.</p>"},{"location":"api/core/#ayn_ml.exceptions.SchemaError","title":"SchemaError","text":"<p>               Bases: <code>AynError</code></p> <p>Raised when a required column is missing or the schema is incompatible.</p> <p>Examples:</p> <p>Raised when TabularSchema.label_col does not exist in the DataFrame, or when a drift metric is given a TextSchema.</p>"},{"location":"api/core/#ayn_ml.exceptions.MetricComputeError","title":"MetricComputeError","text":"<p>               Bases: <code>AynError</code></p> <p>Raised inside compute() for unexpected errors not covered by other types.</p> <p>The Runner catches this per-metric and records it as a MetricError rather than aborting the full monitoring run.</p>"},{"location":"api/core/#ayn_ml.exceptions.UnknownMetricError","title":"UnknownMetricError","text":"<p>               Bases: <code>AynError</code></p> <p>Raised when a metric name is not found in the registry.</p> <p>Attributes:</p> Name Type Description <code>message</code> <p>Human-readable description including the list of known metrics.</p>"},{"location":"api/core/#ayn_ml.exceptions.InsufficientDataError","title":"InsufficientDataError","text":"<p>               Bases: <code>AynError</code></p> <p>Raised when the DataFrame has too few rows to compute a metric reliably.</p> <p>Examples:</p> <p>Raised when fewer than 2 rows are passed to a performance metric, or when all y_true values are zero for MAPE.</p>"},{"location":"api/core/#ayn_ml.exceptions.ThresholdError","title":"ThresholdError","text":"<p>               Bases: <code>AynError</code></p> <p>Raised when a MetricSpec contains an invalid threshold configuration.</p> <p>Examples:</p> <p>Raised when a list threshold has fewer than two elements.</p>"},{"location":"api/data/","title":"Data","text":"<p>The data layer loads, samples, and partitions DataFrames to feed the Runner.</p> <pre><code>DataSource \u2192 SamplingStrategy \u2192 DataPartitioner \u2192 (current, reference)\n   load          narrow window          split\n</code></pre> <p>\u2192 Full data layer guide</p>"},{"location":"api/data/#sources","title":"Sources","text":""},{"location":"api/data/#ayn_ml.data.source.DataFrameSource","title":"DataFrameSource","text":"<p>               Bases: <code>DataSource</code></p> <p>Wraps an already-loaded in-memory DataFrame.</p> <p>This is the primary entry point for users who load data themselves before passing it to the runner.  <code>load</code> projects the DataFrame to only the columns required by the plan, avoiding unnecessary copies on wide tables.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame (pandas or Polars).</p> required"},{"location":"api/data/#ayn_ml.data.source.DataFrameSource.load","title":"load","text":"<pre><code>load(plan: MonitoringPlan) -&gt; Any\n</code></pre> <p>Return the wrapped DataFrame projected to the columns needed by <code>plan</code>.</p> <p>Parameters:</p> Name Type Description Default <code>plan</code> <code>MonitoringPlan</code> <p>MonitoringPlan providing schema and metrics.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The source DataFrame restricted to the required columns.  Columns</p> <code>Any</code> <p>listed by <code>required_columns</code> that are absent from the source are</p> <code>Any</code> <p>silently skipped.</p>"},{"location":"api/data/#sampling-strategies","title":"Sampling strategies","text":""},{"location":"api/data/#ayn_ml.data.sampling.SamplingStrategy","title":"SamplingStrategy","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for sampling strategies.</p> <p>All strategies accept a DataFrame and a DataSchema and return a (potentially smaller) DataFrame representing the current window.</p>"},{"location":"api/data/#ayn_ml.data.sampling.SamplingStrategy.sample","title":"sample  <code>abstractmethod</code>","text":"<pre><code>sample(df: Any, schema: DataSchema) -&gt; Any\n</code></pre> <p>Extract the current window from a DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame (pandas or Polars).</p> required <code>schema</code> <code>DataSchema</code> <p>DataSchema providing column name mappings.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A narwhals-compatible DataFrame containing the sampled rows.</p> <p>Raises:</p> Type Description <code>InsufficientDataError</code> <p>If the resulting window is empty or too small to be meaningful.</p> <code>SchemaError</code> <p>If a required column (e.g. timestamp) is absent.</p>"},{"location":"api/data/#ayn_ml.data.sampling.LastNRowsSampling","title":"LastNRowsSampling","text":"<p>               Bases: <code>SamplingStrategy</code></p> <p>Returns the last N rows by position (no sorting applied).</p> <p>Assumes the DataFrame is already in chronological order.  Use <code>TimeWindowSampling</code> when an explicit timestamp range is preferred.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>Number of rows to keep from the tail of the DataFrame.</p> required"},{"location":"api/data/#ayn_ml.data.sampling.LastNRowsSampling.sample","title":"sample","text":"<pre><code>sample(df: Any, schema: DataSchema) -&gt; Any\n</code></pre> <p>Return the last <code>n</code> rows.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame.</p> required <code>schema</code> <code>DataSchema</code> <p>DataSchema (unused).</p> required <p>Returns:</p> Type Description <code>Any</code> <p>DataFrame containing the last <code>n</code> rows.</p> <p>Raises:</p> Type Description <code>InsufficientDataError</code> <p>If the DataFrame has fewer than <code>n</code> rows.</p>"},{"location":"api/data/#ayn_ml.data.sampling.TimeWindowSampling","title":"TimeWindowSampling","text":"<p>               Bases: <code>SamplingStrategy</code></p> <p>Returns rows where the timestamp column falls within [start, end].</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>datetime</code> <p>Window start (inclusive).</p> required <code>end</code> <code>datetime</code> <p>Window end (inclusive).</p> required"},{"location":"api/data/#ayn_ml.data.sampling.TimeWindowSampling.sample","title":"sample","text":"<pre><code>sample(df: Any, schema: DataSchema) -&gt; Any\n</code></pre> <p>Filter rows to those within [start, end] on the timestamp column.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame.</p> required <code>schema</code> <code>DataSchema</code> <p>DataSchema; <code>timestamp_col</code> must be present.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>Filtered DataFrame.</p> <p>Raises:</p> Type Description <code>SchemaError</code> <p>If <code>schema.timestamp_col</code> is absent from the frame.</p> <code>InsufficientDataError</code> <p>If no rows fall within the window.</p>"},{"location":"api/data/#ayn_ml.data.sampling.RandomSampling","title":"RandomSampling","text":"<p>               Bases: <code>SamplingStrategy</code></p> <p>Randomly subsamples rows for performance.</p> <p>Applied after window selection to reduce the current window to a manageable size before metrics are computed.  Exactly one of <code>n</code> or <code>frac</code> must be provided.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int | None</code> <p>Absolute number of rows to sample.  Must be positive.</p> <code>None</code> <code>frac</code> <code>float | None</code> <p>Fraction of rows to sample.  Must be in <code>(0, 1]</code>.</p> <code>None</code> <code>seed</code> <code>int | None</code> <p>Random seed for reproducibility.  <code>None</code> means non-deterministic.</p> <code>None</code>"},{"location":"api/data/#ayn_ml.data.sampling.RandomSampling.sample","title":"sample","text":"<pre><code>sample(df: Any, schema: DataSchema) -&gt; Any\n</code></pre> <p>Return a random subset of rows.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame.</p> required <code>schema</code> <code>DataSchema</code> <p>DataSchema (unused).</p> required <p>Returns:</p> Type Description <code>Any</code> <p>DataFrame containing the sampled rows.</p> <p>Raises:</p> Type Description <code>InsufficientDataError</code> <p>If the DataFrame is empty or the computed sample size is zero.</p>"},{"location":"api/data/#partitioning","title":"Partitioning","text":""},{"location":"api/data/#ayn_ml.data.partitioner.TimeBasedPartitioner","title":"TimeBasedPartitioner","text":"<p>               Bases: <code>DataPartitioner</code></p> <p>Splits a single DataFrame on a timestamp cutoff.</p> <p>Rows with timestamp after <code>cutoff</code> form the current window. Rows with timestamp in <code>(cutoff - reference_window, cutoff]</code> form the reference window.  Both windows are derived from the same source frame.</p> <p>Parameters:</p> Name Type Description Default <code>cutoff</code> <code>datetime</code> <p>Boundary between reference and current data.</p> required <code>reference_window</code> <code>timedelta</code> <p>Duration of the reference window before the cutoff.</p> required"},{"location":"api/data/#ayn_ml.data.partitioner.TimeBasedPartitioner.partition","title":"partition","text":"<pre><code>partition(df: Any, schema: DataSchema) -&gt; tuple[Any, Any | None]\n</code></pre> <p>Split <code>df</code> into current (post-cutoff) and reference (pre-cutoff) windows.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>Source DataFrame containing both windows.</p> required <code>schema</code> <code>DataSchema</code> <p>DataSchema; <code>timestamp_col</code> must be present.</p> required <p>Returns:</p> Type Description <code>Any</code> <p><code>(current, reference)</code> tuple.  <code>reference</code> is <code>None</code> when</p> <code>Any | None</code> <p>no rows fall within the reference period.  Rows timestamped</p> <code>tuple[Any, Any | None]</code> <p>exactly at <code>cutoff</code> are assigned to the reference window</p> <code>tuple[Any, Any | None]</code> <p>(<code>ts &lt;= cutoff</code>); the current window uses a strict greater-than</p> <code>tuple[Any, Any | None]</code> <p>(<code>ts &gt; cutoff</code>) so no row is double-counted.</p> <p>Raises:</p> Type Description <code>SchemaError</code> <p>If <code>schema.timestamp_col</code> is absent from the frame.</p> <code>InsufficientDataError</code> <p>If no rows fall after the cutoff.</p>"},{"location":"api/metrics/","title":"Metrics","text":"<p>ayn-ml ships with 38+ built-in tabular metrics across four categories. All metrics are resolved from the registry by name at runtime.</p>"},{"location":"api/metrics/#registry","title":"Registry","text":""},{"location":"api/metrics/#ayn_ml.metrics.registry.register_metric","title":"register_metric","text":"<pre><code>register_metric(name: str) -&gt; Callable[[type], type]\n</code></pre> <p>Class decorator that registers a metric implementation by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Registry key (e.g. <code>\"accuracy\"</code>).  Must be unique across the entire library; re-registering the same name raises immediately.</p> required <p>Returns:</p> Type Description <code>Callable[[type], type]</code> <p>A decorator that registers the decorated class and returns it unchanged.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>name</code> is already present in the registry.</p> Example <p>@register_metric(\"my_metric\") class MyMetric:     ...</p>"},{"location":"api/metrics/#ayn_ml.metrics.registry.list_metrics","title":"list_metrics","text":"<pre><code>list_metrics() -&gt; list[str]\n</code></pre> <p>Return a sorted list of all registered metric names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Alphabetically sorted list of registry keys.</p>"},{"location":"api/metrics/#ayn_ml.metrics.registry.get_metric","title":"get_metric","text":"<pre><code>get_metric(name: str) -&gt; Metric\n</code></pre> <p>Look up and instantiate a metric by its registry name.</p> <p>A fresh instance is returned on every call so metrics remain stateless across concurrent runs.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Registry key of the desired metric.</p> required <p>Returns:</p> Type Description <code>Metric</code> <p>A new instance of the corresponding metric class.</p> <p>Raises:</p> Type Description <code>UnknownMetricError</code> <p>If <code>name</code> is not found in the registry.  The error message includes the sorted list of available names.</p>"},{"location":"api/metrics/#base-protocol","title":"Base protocol","text":""},{"location":"api/metrics/#ayn_ml.metrics.base.Metric","title":"Metric","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural protocol that every metric implementation must satisfy.</p> <p>Using <code>@runtime_checkable</code> allows <code>isinstance(obj, Metric)</code> checks, which the Runner uses to validate metrics returned from the registry.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Registry key matching the <code>@register_metric</code> decorator name.</p> <code>metric_type</code> <code>MetricType</code> <p>Category of the metric (see MetricType).</p> <code>requires_reference</code> <code>bool</code> <p><code>True</code> when <code>reference</code> must not be <code>None</code> (drift and statistical-test metrics); <code>False</code> for performance and statistics metrics.</p>"},{"location":"api/metrics/#ayn_ml.metrics.base.Metric.compute","title":"compute","text":"<pre><code>compute(current: Any, reference: Any | None, schema: DataSchema, spec: MetricSpec) -&gt; MetricResult\n</code></pre> <p>Compute the metric and return a result.</p> <p>Parameters:</p> Name Type Description Default <code>current</code> <code>Any</code> <p>Current-window DataFrame (any narwhals-compatible frame).</p> required <code>reference</code> <code>Any | None</code> <p>Reference-window DataFrame, or <code>None</code> for metrics that do not require one.</p> required <code>schema</code> <code>DataSchema</code> <p>Data schema describing column names and modality.</p> required <code>spec</code> <code>MetricSpec</code> <p>MetricSpec with name, params, and optional threshold.</p> required <p>Returns:</p> Type Description <code>MetricResult</code> <p>MetricResult containing the computed value and pass/fail status.</p> <p>Raises:</p> Type Description <code>SchemaError</code> <p>If required columns are missing or the schema type is incompatible with this metric.</p> <code>InsufficientDataError</code> <p>If the DataFrame has too few rows.</p> <code>MetricComputeError</code> <p>For unexpected errors during computation.</p>"},{"location":"api/metrics/#built-in-metrics","title":"Built-in metrics","text":""},{"location":"api/metrics/#performance","title":"Performance","text":"Name Description <code>accuracy</code> Classification accuracy <code>f1</code> F1 score (macro) <code>precision</code> Precision (macro) <code>recall</code> Recall (macro) <code>auc</code> ROC-AUC <code>aucpr</code> Area under precision-recall curve <code>log_loss</code> Log loss <code>brier</code> Brier score <code>mae</code> Mean absolute error (regression) <code>mse</code> Mean squared error (regression) <code>r2</code> R\u00b2 score (regression) <code>mape</code> Mean absolute percentage error (regression)"},{"location":"api/metrics/#drift","title":"Drift","text":"Name Description <code>psi</code> Population Stability Index <code>wasserstein</code> Wasserstein distance (Earth Mover's Distance) <code>cramervonmises</code> Cram\u00e9r-von Mises test <code>ks</code> Kolmogorov-Smirnov test <code>ttest</code> Welch's t-test <code>mannwhitney</code> Mann-Whitney U test <code>levene</code> Levene's test (variance shift) <code>chisquare</code> Chi-square test (categorical) <code>mmd</code> Maximum Mean Discrepancy <code>target_drift</code> Label distribution drift"},{"location":"api/metrics/#statistics","title":"Statistics","text":"Name Description <code>mean</code> Column mean <code>std</code> Column standard deviation <code>skewness</code> Sample skewness <code>kurtosis</code> Excess kurtosis <code>top_category</code> Most frequent category and its frequency <code>missing_rate</code> Fraction of null values"},{"location":"api/metrics/#cbpe-estimation","title":"CBPE Estimation","text":"Name Description <code>cbpe_accuracy</code> CBPE-estimated accuracy (no ground truth required) <code>cbpe_f1</code> CBPE-estimated F1 <code>cbpe_auc</code> CBPE-estimated ROC-AUC <code>cbpe_precision</code> CBPE-estimated precision <code>cbpe_recall</code> CBPE-estimated recall"},{"location":"api/metrics/#fairness","title":"Fairness","text":"Name Description <code>demographic_parity</code> Demographic parity difference <code>equalized_odds</code> Equalized odds difference <code>disparate_impact</code> Disparate impact ratio <p>LLM safety metrics (toxicity, hallucination detection, LLM-as-a-judge scoring) are available in the commercial edition.</p>"},{"location":"api/renderers/","title":"Renderers","text":"<p>Renderers turn <code>MonitoringReport</code> objects into human-readable output.</p>"},{"location":"api/renderers/#htmlrenderer","title":"HtmlRenderer","text":""},{"location":"api/renderers/#ayn_ml.renderers.html.HtmlRenderer","title":"HtmlRenderer","text":"<p>HTML report renderer for MonitoringReport objects.</p> <p>Produces complete, self-contained HTML documents.  The default chart backend is <code>PlotlyBackend</code> \u2014 swap in <code>NoChartBackend</code> for headless environments that cannot load the Plotly CDN.</p> <p>Parameters:</p> Name Type Description Default <code>charts</code> <code>ChartBackend | None</code> <p>Chart backend to use for generating chart HTML fragments. Defaults to <code>PlotlyBackend()</code>.</p> <code>None</code> <p>Example::</p> <pre><code>from ayn_ml.renderers import HtmlRenderer, NoChartBackend\n\n# With interactive Plotly charts (default)\nrenderer = HtmlRenderer()\nhtml = renderer.render(report)\n\n# Tables only \u2014 useful for email bodies or offline environments\nrenderer = HtmlRenderer(charts=NoChartBackend())\nhtml = renderer.render(report)\n</code></pre>"},{"location":"api/renderers/#ayn_ml.renderers.html.HtmlRenderer.render","title":"render","text":"<pre><code>render(report: MonitoringReport) -&gt; str\n</code></pre> <p>Render a single monitoring report snapshot as a complete HTML document.</p> <p>The document contains:</p> <ul> <li>Run metadata header (model, run ID, timestamp, row counts).</li> <li>Fired alerts section (if any alerts triggered).</li> <li>Metric results table (name, value, pass/fail status, threshold,   effect size).</li> <li>Errors section (if any metrics failed).</li> <li>Column profile table (if <code>enable_profiling=True</code> in the plan).</li> <li>Bar chart of metric values (when using <code>PlotlyBackend</code>).</li> </ul> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>Completed <code>MonitoringReport</code> from a Runner execution.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Complete HTML document as a string.</p>"},{"location":"api/renderers/#ayn_ml.renderers.html.HtmlRenderer.render_history","title":"render_history","text":"<pre><code>render_history(reports: list[MonitoringReport]) -&gt; str\n</code></pre> <p>Render a history of reports as a time-series dashboard HTML page.</p> <p>Reports are sorted internally by <code>context.eval_timestamp</code> (oldest first).  One time-series chart is generated per distinct metric name, showing value over time with a dashed threshold reference line when the metric spec includes a scalar threshold.</p> <p>Parameters:</p> Name Type Description Default <code>reports</code> <code>list[MonitoringReport]</code> <p>List of <code>MonitoringReport</code> objects to visualise. Must share the same <code>model_id</code> and <code>model_version</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Complete HTML document as a string.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If reports is empty.</p>"},{"location":"api/runner/","title":"Runner","text":"<p><code>Runner</code> is the stateless orchestrator that executes a <code>MonitoringPlan</code> against data windows and returns a <code>MonitoringReport</code>.</p>"},{"location":"api/runner/#ayn_ml.runner.Runner","title":"Runner","text":"<p>Stateless orchestrator for a MonitoringPlan.</p> <p>Executes every MetricSpec in the plan against the supplied data windows, collects results and non-fatal errors, and returns a MonitoringReport. A single Runner instance may be reused across multiple runs.</p> <p>Parameters:</p> Name Type Description Default <code>n_jobs</code> <code>int</code> <p>Number of parallel workers for metric computation. <code>1</code> (default) runs metrics sequentially.  Any integer <code>&gt;= 2</code> spawns a <code>ThreadPoolExecutor</code> with that many workers.  <code>-1</code> uses all available CPUs.  scipy and numpy release the GIL for most operations, so compute-heavy metrics (KS, Wasserstein, MMD, CBPE) benefit from thread-level parallelism.</p> <code>1</code>"},{"location":"api/runner/#ayn_ml.runner.Runner.run","title":"run","text":"<pre><code>run(plan: MonitoringPlan, current: Any, reference: Any | None = None, model: Any | None = None, store: Any | None = None, sinks: list[ResultSink] | None = None, alert_rules: list[AlertRule] | None = None) -&gt; MonitoringReport\n</code></pre> <p>Execute the monitoring plan and return a MonitoringReport.</p> <p>All data configuration (window selection, sampling) is read from <code>plan</code>.  Pass data as <code>current</code> and optionally <code>reference</code>; everything else is controlled by <code>MonitoringPlan</code>.</p> <p>Execution order: 1. Resolve DataSources \u2014 wrap raw DataFrames if necessary, project to    required columns via <code>DataSource.load(plan)</code>. 2. Window selection \u2014 apply <code>plan.window</code> config. 3. Random sub-sampling \u2014 apply <code>plan.sampling</code> on top of the window. 4. Model filtering \u2014 drop rows where <code>model_id_col</code> or    <code>model_version_col</code> do not match the plan. 5. Context construction \u2014 build ExecutionContext with UTC timestamp    and period bounds derived from the current window's timestamp column. 6. Metric loop \u2014 execute each MetricSpec; per-metric errors are    isolated and stored in <code>MonitoringReport.errors</code>. 7. Alert evaluation \u2014 <code>ThresholdPolicy</code> rules are evaluated    against computed results; <code>FiredAlert</code> records are collected. 8. Statistical profiling \u2014 compute column profiles when    <code>plan.enable_profiling</code> is <code>True</code>; skipped otherwise. 9. Store write \u2014 <code>store.write(report)</code> is called when a store is    provided; errors are logged but do not abort the run. 10. Unconditional sink dispatch \u2014 every sink in <code>sinks</code> receives     <code>write(report)</code> on every run, regardless of alert status. 11. Alert channel dispatch \u2014 channels bound to fired <code>AlertRule</code>     objects receive <code>write(report)</code>; errors are logged but do not     abort the run. 12. Return MonitoringReport.</p> <p>Parameters:</p> Name Type Description Default <code>plan</code> <code>MonitoringPlan</code> <p>MonitoringPlan defining schema, metrics, and data configuration (window, sampling).</p> required <code>current</code> <code>Any</code> <p>Current-window data.  Accepts a pandas DataFrame, Polars DataFrame, or any <code>DataSource</code> implementation.</p> required <code>reference</code> <code>Any | None</code> <p>Reference-window data.  Same types as <code>current</code>. May be <code>None</code> for metrics that do not require a reference.</p> <code>None</code> <code>model</code> <code>Any | None</code> <p>ModelWrapper for on-the-fly inference.  Reserved for Phase 5; currently ignored with a warning if supplied.</p> <code>None</code> <code>store</code> <code>Any | None</code> <p>ResultStore for persisting the report.  Called via <code>store.write(report)</code> unconditionally when provided.</p> <code>None</code> <code>sinks</code> <code>list[ResultSink] | None</code> <p>ResultSink list dispatched unconditionally on every run (e.g. a logging sink or a dashboard push).  Pass alert channels via <code>alert_rules</code> instead to limit dispatch to fired alerts only.</p> <code>None</code> <code>alert_rules</code> <code>list[AlertRule] | None</code> <p>AlertRule list evaluated after metric computation. Each rule binds a metric name to a policy and one or more notification channels.</p> <code>None</code> <p>Returns:</p> Type Description <code>MonitoringReport</code> <p>MonitoringReport containing all MetricResults, MetricErrors, and</p> <code>MonitoringReport</code> <p>fired alerts.</p> <p>Raises:</p> Type Description <code>AynError</code> <p>Re-raised only for errors outside the per-metric loop (e.g. DataSource resolution failures).  Per-metric errors are collected into the report rather than raised.</p>"},{"location":"api/sinks/","title":"Sinks","text":"<p>Result sinks fire notifications when alerts are triggered. They implement the <code>ResultSink</code> protocol and receive only the reports that contain fired alerts.</p>"},{"location":"api/sinks/#protocol","title":"Protocol","text":""},{"location":"api/sinks/#ayn_ml.sinks.base.ResultSink","title":"ResultSink","text":"<p>               Bases: <code>Protocol</code></p> <p>Write-only interface for notification channels.</p> <p>Implementations must be stateless between calls \u2014 each <code>write()</code> is an independent dispatch.  Errors raised by <code>write()</code> are caught by the Runner and logged as warnings; they do not abort the run.</p>"},{"location":"api/sinks/#ayn_ml.sinks.base.ResultSink.write","title":"write","text":"<pre><code>write(report: MonitoringReport) -&gt; None\n</code></pre> <p>Dispatch a monitoring report to the sink destination.</p> <p>Any exception raised by this method is caught and logged by the Runner as a warning \u2014 it does not abort the run.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>The completed MonitoringReport from a Runner execution.</p> required"},{"location":"api/sinks/#emailchannel","title":"EmailChannel","text":""},{"location":"api/sinks/#ayn_ml.sinks.email.EmailChannel","title":"EmailChannel","text":"<p>ResultSink that sends a MonitoringReport summary via SMTP.</p> <p>Uses Python's built-in <code>smtplib</code> and <code>email</code> libraries \u2014 no third-party mail library is required.</p> <p>When <code>renderer</code> is provided, the email is multipart (HTML + plain text fallback).  Otherwise only a plain-text body is sent.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>str</code> <p>SMTP server hostname (e.g. <code>\"smtp.gmail.com\"</code>).</p> required <code>to_addrs</code> <code>list[str]</code> <p>List of recipient email addresses.</p> required <code>from_addr</code> <code>str</code> <p>Sender email address.  Defaults to the first element of to_addrs when not set.</p> <code>''</code> <code>port</code> <code>int</code> <p>SMTP port.  587 for STARTTLS (default), 465 for SSL, 25 for unencrypted.</p> <code>587</code> <code>username</code> <code>str</code> <p>SMTP authentication username.  Required by most cloud mail providers.</p> <code>''</code> <code>password</code> <code>str</code> <p>SMTP authentication password or app-specific token.</p> <code>''</code> <code>subject_template</code> <code>str</code> <p>Python format string for the email subject. Available keys: <code>{plan_name}</code>, <code>{model_id}</code>, <code>{model_version}</code>, <code>{n_alerts}</code>.</p> <code>'[ayn-ml] {plan_name} \u2014 {n_alerts} alert(s) fired'</code> <code>use_tls</code> <code>bool</code> <p>When <code>True</code> (default), use STARTTLS.  Set to <code>False</code> for SSL-wrapped connections (port 465) or for local development SMTP servers.</p> <code>True</code> <code>timeout</code> <code>int</code> <p>SMTP connection timeout in seconds.  Defaults to 30.</p> <code>30</code> <code>renderer</code> <code>HtmlRenderer | None</code> <p>Optional <code>HtmlRenderer</code> instance.  When supplied, sends a multipart email with an HTML body plus a plain-text fallback.</p> <code>None</code> <p>Example::</p> <pre><code>channel = EmailChannel(\n    host=\"smtp.gmail.com\",\n    to_addrs=[\"ops@example.com\"],\n    from_addr=\"monitoring@example.com\",\n    username=\"monitoring@example.com\",\n    password=\"app-password\",\n)\n</code></pre>"},{"location":"api/sinks/#ayn_ml.sinks.email.EmailChannel.write","title":"write","text":"<pre><code>write(report: MonitoringReport) -&gt; None\n</code></pre> <p>Send the MonitoringReport summary via SMTP.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>Completed <code>MonitoringReport</code> from a Runner execution.</p> required <p>Raises:</p> Type Description <code>SMTPException</code> <p>On SMTP protocol errors.</p> <code>OSError</code> <p>On network failures.</p>"},{"location":"api/sinks/#webhookchannel","title":"WebhookChannel","text":"<p>A Slack notification channel is available in the commercial edition.</p>"},{"location":"api/sinks/#ayn_ml.sinks.webhook.WebhookChannel","title":"WebhookChannel","text":"<p>ResultSink that POSTs a MonitoringReport as JSON to an HTTP endpoint.</p> <p>Uses Python's built-in <code>urllib.request</code> \u2014 no third-party HTTP library required.  The full <code>MonitoringReport.to_dict()</code> payload is sent with <code>Content-Type: application/json</code>.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Destination URL.  Must include the scheme (e.g. <code>\"https://hooks.example.com/monitoring\"</code>).</p> required <code>extra_headers</code> <code>dict[str, str] | None</code> <p>Additional HTTP headers to include in the request (e.g. <code>{\"Authorization\": \"Bearer token\"}</code>).  Merged with the default <code>Content-Type</code> header; caller-supplied values take precedence.</p> <code>None</code> <code>timeout</code> <code>int</code> <p>Request timeout in seconds.  Defaults to 30.</p> <code>30</code> <p>Example::</p> <pre><code>channel = WebhookChannel(\n    url=\"https://hooks.example.com/monitoring\",\n    extra_headers={\"X-API-Key\": \"secret\"},\n    timeout=10,\n)\n</code></pre>"},{"location":"api/sinks/#ayn_ml.sinks.webhook.WebhookChannel.write","title":"write","text":"<pre><code>write(report: MonitoringReport) -&gt; None\n</code></pre> <p>POST the MonitoringReport as JSON to the configured URL.</p> <p>Serialises the report with <code>MonitoringReport.to_dict()</code>.  Raises on HTTP errors (4xx/5xx) and network failures; the Runner catches and logs these.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>Completed <code>MonitoringReport</code> from a Runner execution.</p> required <p>Raises:</p> Type Description <code>URLError</code> <p>On network failures or DNS resolution errors.</p> <code>HTTPError</code> <p>On HTTP 4xx/5xx responses.</p>"},{"location":"api/stores/","title":"Stores","text":"<p>Result stores persist <code>MonitoringReport</code> objects and expose a history query interface.</p> <p>\u2192 Full stores guide</p>"},{"location":"api/stores/#protocol","title":"Protocol","text":""},{"location":"api/stores/#ayn_ml.stores.base.ResultStore","title":"ResultStore","text":"<p>               Bases: <code>ResultSink</code>, <code>Protocol</code></p> <p>Bidirectional persistence interface for MonitoringReport objects.</p> <p>Extends <code>ResultSink</code> with <code>read_history()</code> and <code>get_report()</code>. Every store can also act as a sink \u2014 pass a store as <code>store=</code> to the Runner and it handles both persistence and downstream dispatch.</p> <p>The <code>write()</code> method (inherited from <code>ResultSink</code>) must be thread-safe: the Runner may call it from a thread pool in future parallel execution modes.</p>"},{"location":"api/stores/#ayn_ml.stores.base.ResultStore.read_history","title":"read_history","text":"<pre><code>read_history(model_id: str, model_version: str | None = None, metric_name: str | None = None, limit: int | None = None, get_metadata: bool = False, metric_type: str | None = None) -&gt; list[dict[str, Any]]\n</code></pre> <p>Retrieve historical metric results as flat dictionaries.</p> <p>Each returned dict always contains the following keys:</p> <p><code>run_id</code>, <code>model_id</code>, <code>model_version</code>, <code>metric_name</code>, <code>feature_name</code>, <code>value</code>, <code>status</code>, <code>effect_size</code>, <code>effect_size_label</code>, <code>period_start</code>, <code>period_end</code>, <code>metric_type</code>.</p> <p>The <code>metric_type</code> column discriminates row kinds:</p> <ul> <li><code>None</code> or a <code>MetricType</code> value (<code>\"performance\"</code>,   <code>\"drift\"</code>, etc.) \u2014 rows from <code>MetricSpec</code>-driven computation.</li> <li><code>\"profile\"</code> \u2014 statistical profile rows produced when   <code>enable_profiling=True</code>; <code>status</code> and <code>effect_size</code> are   always <code>None</code> for these rows.</li> </ul> <p>When <code>get_metadata=True</code>, the dict is enriched with plan-level fields (prefixed <code>plan_</code>) and run-level fields (prefixed <code>run_</code>):</p> <p><code>plan_name</code>, <code>plan_window_type</code>, <code>plan_window_n</code>, <code>plan_sampling_type</code>, <code>plan_sampling_frac</code>, <code>run_n_current</code>, <code>run_n_reference</code>.</p> <p>Results are ordered by <code>period_start</code> descending (newest first). The list is directly convertible to a pandas DataFrame via <code>pd.DataFrame(store.read_history(...))</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_id</code> <code>str</code> <p>Filter by model identifier.</p> required <code>model_version</code> <code>str | None</code> <p>Optional filter by model version.</p> <code>None</code> <code>metric_name</code> <code>str | None</code> <p>Optional filter by metric name (e.g. <code>\"auc\"</code>).</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Maximum number of rows to return.  <code>None</code> means no limit \u2014 the caller receives the full history.</p> <code>None</code> <code>get_metadata</code> <code>bool</code> <p>When <code>True</code>, JOIN with the run table and include plan and run metadata fields in each dict.</p> <code>False</code> <code>metric_type</code> <code>str | None</code> <p>Optional filter by metric type (e.g. <code>\"performance\"</code>, <code>\"drift\"</code>, <code>\"profile\"</code>).  <code>None</code> returns all types.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of flat dicts, newest first.</p>"},{"location":"api/stores/#ayn_ml.stores.base.ResultStore.get_report","title":"get_report","text":"<pre><code>get_report(run_id: str) -&gt; MonitoringReport | None\n</code></pre> <p>Retrieve a complete MonitoringReport by run identifier.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>The <code>ExecutionContext.run_id</code> of the target run.</p> required <p>Returns:</p> Type Description <code>MonitoringReport | None</code> <p>The full <code>MonitoringReport</code> if found, <code>None</code> otherwise.</p>"},{"location":"api/stores/#inmemorystore","title":"InMemoryStore","text":""},{"location":"api/stores/#ayn_ml.stores.memory.InMemoryStore","title":"InMemoryStore","text":"<p>ResultStore backed by an in-memory deque.</p> <p>Thread-safe: a single <code>threading.Lock</code> guards all reads and writes. Safe to use with the Runner in a multi-threaded context.</p> <p>Parameters:</p> Name Type Description Default <code>maxlen</code> <code>int | None</code> <p>Maximum number of <code>MonitoringReport</code> objects to retain. When the deque is full, the oldest report is evicted automatically on each new <code>write()</code>.  <code>None</code> means unbounded \u2014 all reports are kept for the lifetime of the store instance.</p> <code>None</code> <p>Example::</p> <pre><code>store = InMemoryStore()\nrunner = Runner()\nreport = runner.run(plan, df, store=store)\n\nrows = store.read_history(\"fraud_v2\", metric_name=\"auc\")\ndf = pd.DataFrame(rows)\n</code></pre>"},{"location":"api/stores/#ayn_ml.stores.memory.InMemoryStore.write","title":"write","text":"<pre><code>write(report: MonitoringReport) -&gt; None\n</code></pre> <p>Append a MonitoringReport to the in-memory store.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>The completed MonitoringReport from a Runner execution.</p> required"},{"location":"api/stores/#ayn_ml.stores.memory.InMemoryStore.get_report","title":"get_report","text":"<pre><code>get_report(run_id: str) -&gt; MonitoringReport | None\n</code></pre> <p>Retrieve a complete MonitoringReport by run identifier.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>The <code>ExecutionContext.run_id</code> to look up.</p> required <p>Returns:</p> Type Description <code>MonitoringReport | None</code> <p>The matching <code>MonitoringReport</code>, or <code>None</code> if not found.</p>"},{"location":"api/stores/#ayn_ml.stores.memory.InMemoryStore.read_history","title":"read_history","text":"<pre><code>read_history(model_id: str, model_version: str | None = None, metric_name: str | None = None, limit: int | None = None, get_metadata: bool = False, metric_type: str | None = None) -&gt; list[dict[str, Any]]\n</code></pre> <p>Retrieve historical metric results as flat dictionaries.</p> <p>Iterates over stored reports newest-first, filters by the given criteria, and returns one dict per matching row.  Both MetricResult-driven rows and profile rows (when present) are included unless filtered by <code>metric_type</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_id</code> <code>str</code> <p>Filter by model identifier.</p> required <code>model_version</code> <code>str | None</code> <p>Optional filter by model version.</p> <code>None</code> <code>metric_name</code> <code>str | None</code> <p>Optional filter by metric name (e.g. <code>\"auc\"</code>).</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Maximum number of rows to return.  <code>None</code> returns all matching rows.</p> <code>None</code> <code>get_metadata</code> <code>bool</code> <p>When <code>True</code>, enrich each row with plan and run metadata fields (<code>plan_*</code> and <code>run_*</code> prefixes).</p> <code>False</code> <code>metric_type</code> <code>str | None</code> <p>Optional filter by metric type (e.g. <code>\"performance\"</code>, <code>\"drift\"</code>, <code>\"profile\"</code>).  <code>None</code> returns all types.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of flat dicts, newest first.  Pass directly to</p> <code>list[dict[str, Any]]</code> <p><code>pd.DataFrame()</code> for tabular analysis.</p>"},{"location":"api/stores/#sqlitestore","title":"SqliteStore","text":"<p>A cloud S3-compatible store is available in the commercial edition.</p>"},{"location":"api/stores/#ayn_ml.stores.sqlite.SqliteStore","title":"SqliteStore","text":"<p>ResultStore backed by a local SQLite database.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Path to the SQLite database file.  The file is created if it does not exist.  Pass <code>\":memory:\"</code> for a transient in-process database (useful for testing when you need SQL query behaviour without a file on disk).</p> required <p>Example::</p> <pre><code>store = SqliteStore(\"monitoring.db\")\nrunner = Runner()\nreport = runner.run(plan, df, store=store)\n\nrows = store.read_history(\"fraud_v2\", metric_name=\"auc\")\ndf = pd.DataFrame(rows)\n</code></pre>"},{"location":"api/stores/#ayn_ml.stores.sqlite.SqliteStore.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying SQLite connection.</p> <p>Call explicitly when the store is no longer needed.  After <code>close()</code>, all further method calls will raise <code>sqlite3.ProgrammingError</code>.</p>"},{"location":"api/stores/#ayn_ml.stores.sqlite.SqliteStore.write","title":"write","text":"<pre><code>write(report: MonitoringReport) -&gt; None\n</code></pre> <p>Persist a MonitoringReport to the SQLite database.</p> <p>Inserts one row into <code>monitoring_runs</code>; one row per <code>MetricResult</code> into <code>metric_results</code> (with <code>metric_type</code> from the spec); one row per <code>(column, stat)</code> pair from <code>report.profile</code> into <code>metric_results</code> (with <code>metric_type = \"profile\"</code>); one row per <code>MetricError</code> into <code>metric_errors</code>; and one row per <code>FiredAlert</code> into <code>fired_alerts</code>.  All inserts are wrapped in a single transaction.</p> <p><code>run_id</code> is used as the primary key.  Duplicate run IDs are detected by a pre-flight <code>SELECT</code> and silently ignored \u2014 the second call returns immediately without inserting any rows.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>MonitoringReport</code> <p>The completed MonitoringReport from a Runner execution.</p> required"},{"location":"api/stores/#ayn_ml.stores.sqlite.SqliteStore.get_report","title":"get_report","text":"<pre><code>get_report(run_id: str) -&gt; MonitoringReport | None\n</code></pre> <p>Retrieve a complete MonitoringReport by run identifier.</p> <p>Reconstructs the full object from four tables.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>The <code>ExecutionContext.run_id</code> to look up.</p> required <p>Returns:</p> Type Description <code>MonitoringReport | None</code> <p>The reconstructed <code>MonitoringReport</code>, or <code>None</code> if not found.</p>"},{"location":"api/stores/#ayn_ml.stores.sqlite.SqliteStore.read_history","title":"read_history","text":"<pre><code>read_history(model_id: str, model_version: str | None = None, metric_name: str | None = None, limit: int | None = None, get_metadata: bool = False, metric_type: str | None = None) -&gt; list[dict[str, Any]]\n</code></pre> <p>Retrieve historical metric results as flat dictionaries.</p> <p>Queries <code>metric_results</code> directly (no JOIN when <code>get_metadata=False</code>).  When <code>get_metadata=True</code>, a LEFT JOIN with <code>monitoring_runs</code> is performed and plan fields are extracted and prefixed with <code>plan_</code> / <code>run_</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_id</code> <code>str</code> <p>Filter by model identifier.</p> required <code>model_version</code> <code>str | None</code> <p>Optional filter by model version.</p> <code>None</code> <code>metric_name</code> <code>str | None</code> <p>Optional filter by metric name (e.g. <code>\"auc\"</code>).</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Maximum number of rows to return.  <code>None</code> returns all matching rows.</p> <code>None</code> <code>get_metadata</code> <code>bool</code> <p>When <code>True</code>, JOIN with <code>monitoring_runs</code> and include plan and run metadata fields.</p> <code>False</code> <code>metric_type</code> <code>str | None</code> <p>Optional filter by metric type (e.g. <code>\"performance\"</code>, <code>\"drift\"</code>, <code>\"profile\"</code>).  <code>None</code> returns all types.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>List of flat dicts, newest first (ordered by <code>period_start</code></p> <code>list[dict[str, Any]]</code> <p>DESC).  Pass directly to <code>pd.DataFrame()</code> for analysis.</p>"}]}